---
title: "Instalación"
description: "Instala el módulo Nuxt I18n Micro y sus paquetes opcionales del ecosistema."
---

## Requisitos

- **Node.js**: 18 o superior.
- **Nuxt**: 3.x o 4.x.

El módulo admite tanto la estructura de proyecto de Nuxt 3 (`pages/**/*.vue`) como la de Nuxt 4 (`app/pages/**/*.vue`). Por convención, las traducciones residen en la raíz del proyecto (`locales/`), incluso cuando se usa el directorio `app/` de Nuxt 4.

## Instalar el módulo de Nuxt

<CodeGroup>
```bash npm
npm install nuxt-i18n-micro
```

```bash pnpm
pnpm add nuxt-i18n-micro
```

```bash yarn
yarn add nuxt-i18n-micro
```

```bash bun
bun add nuxt-i18n-micro
```
</CodeGroup>

Regístralo en `nuxt.config.ts`:

```typescript
export default defineNuxtConfig({
  modules: ['nuxt-i18n-micro'],
  i18n: {
    locales: [
      { code: 'en', iso: 'en-US', dir: 'ltr' },
      { code: 'fr', iso: 'fr-FR', dir: 'ltr' },
    ],
    defaultLocale: 'en',
    translationDir: 'locales',
  },
})
```

Consulta el [Inicio rápido](/quickstart) para un recorrido completo o la [Referencia de configuración](/guides/configuration) para conocer todas las opciones.

## Estructura del proyecto

De forma predeterminada, el módulo espera:

```text
my-project/
├── locales/
│   ├── en.json
│   ├── fr.json
│   └── pages/
│       └── index/
│           ├── en.json
│           └── fr.json
├── pages/
│   └── index.vue
└── nuxt.config.ts
```

Los archivos de nivel raíz se fusionan con cada archivo específico de página en tiempo de compilación. Los archivos específicos de página se crean automáticamente cuando faltan durante `nuxt dev`.

<Info>

</Info>

## Estructura de Nuxt 4

```text
my-project/
├── app/
│   └── pages/
│       └── index.vue
├── locales/
│   └── en.json
└── nuxt.config.ts
```

Para colocar las traducciones dentro de `app/`, establece `translationDir: 'app/locales'`.

## Paquetes del ecosistema

| Paquete | Propósito |
| --- | --- |
| [`@i18n-micro/vue`](/integrations/vue) | Integración con Vue 3 (independiente, sin Nuxt). |
| [`@i18n-micro/react`](/integrations/react) | Integración con React. |
| [`@i18n-micro/preact`](/integrations/preact) | Integración con Preact. |
| [`@i18n-micro/solid`](/integrations/solid) | Integración con SolidJS. |
| [`@i18n-micro/astro`](/integrations/astro) | Integración con Astro. |
| [`@i18n-micro/node`](/integrations/nodejs) | Cargador del lado del servidor para Node.js. |
| [`@i18n-micro/types-generator`](/integrations/types-generator) | Genera tipos de TypeScript a partir de los archivos de traducción. |
| [`@i18n-micro/devtools-ui`](/integrations/devtools-ui) | Panel de DevTools independiente del framework. |

Los paquetes internos compartidos se incluyen de forma transitiva.

## TypeScript

El módulo genera tipos globales (ambient) para `useI18n`, `$t` y helpers relacionados a partir de los archivos de tu locale predeterminado. Esto está habilitado por defecto (`types: true`).

Para un tipado más estricto de las claves de traducción, consulta [`@i18n-micro/types-generator`](/integrations/types-generator).
