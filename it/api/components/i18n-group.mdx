---
title: "Componente `<i18n-group>`"
description: "Il componente Vue <i18n-group> raggruppa le traduzioni sotto un prefisso di chiave condiviso, in modo che etichette annidate e sezioni ripetute restino concise nei template."
---

# 🌍 Componente `<i18n-group>`

Il componente `<i18n-group>` in Nuxt I18n Micro fornisce un modo comodo per raggruppare le traduzioni sotto un prefisso comune, riducendo la ripetizione e migliorando l'organizzazione del codice.

## ⚙️ Props

{/* generated:component:i18n-group — do not edit; run `pnpm run docs:generate` */}

| Prop | Tipo | Predefinito | Descrizione |
| --- | --- | --- | --- |
| `prefix` **\*** | `string` | — | Prefisso di chiave rispetto al quale viene risolto ogni lookup all'interno dello slot. |
| `groupClass` | `string` | `''` | Classe aggiuntiva sull'elemento wrapper. |

**\*** obbligatorio.

### Slot

| Slot | Binding | Descrizione |
| --- | --- | --- |
| `default` | `prefix`, `t` | Riceve `prefix` e una funzione `t` con ambito su di esso. |

{/* /generated:component:i18n-group */}

## 🎯 Slot

Lo slot predefinito riceve `prefix` e una funzione `t` con ambito su di esso, in modo che le chiavi all'interno siano
scritte in modo relativo al prefisso:

```vue
<i18n-group prefix="checkout">
  <template #default="{ t }">
    <h2>{{ t('title') }}</h2>
    <p>{{ t('summary', { count: 3 }) }}</p>
  </template>
</i18n-group>
```

## 🛠️ Esempi d'uso

### Utilizzo di base

```vue
<template>
  <i18n-group prefix="product.details">
    <template #default="{ t }">
      <h1>{{ t('title') }}</h1>
      <p>{{ t('description') }}</p>
      <div class="price">{{ t('price', { value: 99.99 }) }}</div>
    </template>
  </i18n-group>
</template>
```

File di traduzione:

```json
{
  "product": {
    "details": {
      "title": "Premium Product",
      "description": "This is a premium product",
      "price": "Price: ${value}"
    }
  }
}
```

### Con classe personalizzata

```vue
<template>
  <i18n-group prefix="user.profile" group-class="profile-section">
    <template #default="{ t }">
      <div class="user-info">
        <h2>{{ t('title') }}</h2>
        <p>{{ t('bio') }}</p>
        <div class="stats">
          <span>{{ t('stats.followers') }}</span>
          <span>{{ t('stats.following') }}</span>
        </div>
      </div>
    </template>
  </i18n-group>
</template>
```

File di traduzione:

```json
{
  "user": {
    "profile": {
      "title": "User Profile",
      "bio": "User biography goes here",
      "stats": {
        "followers": "Followers: {count}",
        "following": "Following: {count}"
      }
    }
  }
}
```

### Con contenuto dinamico

```vue
<template>
  <i18n-group prefix="blog.post">
    <template #default="{ t }">
      <article>
        <h1>{{ t('title') }}</h1>
        <div class="meta">
          {{ t('meta.author', { name: author }) }} |
          {{ t('meta.date', { date: publishDate }) }}
        </div>
        <div v-for="(section, index) in sections" :key="index">
          <h2>{{ t(`sections.${index}.title`) }}</h2>
          <p>{{ t(`sections.${index}.content`) }}</p>
        </div>
      </article>
    </template>
  </i18n-group>
</template>

<script setup>
const author = 'John Doe'
const publishDate = '2024-01-01'
const sections = ['intro', 'main', 'conclusion']
</script>
```

## 🎨 Stile

Il componente racchiude il suo contenuto in un div con la classe `i18n-group` e qualsiasi classe aggiuntiva specificata nella prop `groupClass`:

```css
.i18n-group {
  /* Your base styles */
}

.profile-section {
  /* Additional styles for profile sections */
}
```

## 🚀 Best practice

1. **Prefissi coerenti**: usa prefissi coerenti e logici che riflettano la struttura della tua applicazione

```vue
<i18n-group prefix="features.pricing">
<i18n-group prefix="features.security">
```

2. **Organizzazione modulare**: raggruppa insieme le traduzioni correlate sotto prefissi significativi

```vue
<i18n-group prefix="checkout.payment">
<i18n-group prefix="checkout.shipping">
```

3. **Componenti riutilizzabili**: crea componenti riutilizzabili con i propri gruppi di traduzione

```vue
<!-- UserProfile.vue -->
<i18n-group prefix="user.profile">

<!-- UserSettings.vue -->
<i18n-group prefix="user.settings">
```

4. **Gruppi annidati**: evita di annidare troppo in profondità i gruppi per mantenere la chiarezza

```vue
<!-- Good -->
<i18n-group prefix="shop.product">

<!-- Avoid -->
<i18n-group prefix="shop.category.product.details.specs">
```
