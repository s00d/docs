---
title: "Nuxt I18n Micro 範例與用法"
description: "語系切換與 i18n 元件的範例。"
---

# 📚 Nuxt I18n Micro 範例與用法

本節提供多個範例,示範如何在 Nuxt.js 應用中使用 `Nuxt I18n Micro`。你會看到如何切換語系、使用 `<i18n-link>`、`<i18n-switcher>` 與 `<i18n-t>` 元件,以及動態處理翻譯鍵。

## 🛠️ 基本設定

以下是使用 `Nuxt I18n Micro` 的基本設定:

```typescript
export default defineNuxtConfig({
  modules: ['nuxt-i18n-micro'],
  i18n: {
    locales: [
      { code: 'en', iso: 'en-US', dir: 'ltr', disabled: false, displayName: 'English' },
      { code: 'fr', iso: 'fr-FR', dir: 'ltr', disabled: false, displayName: 'Français' },
      { code: 'de', iso: 'de-DE', dir: 'ltr', disabled: false, displayName: 'Deutsch' },
    ],
    defaultLocale: 'en',
    translationDir: 'locales',
    meta: true,
  },
})
```

## 🌍 語系切換

### 以程式方式切換語系

以下範例示範如何透過按鈕以程式方式切換語系:

```vue
<template>
  <div>
    <p>Current Locale: {{ $getLocale() }}</p>
    <div>
      <button v-for="locale in $getLocales()" :key="locale.code" :disabled="locale.code === $getLocale()" @click="() => $switchLocale(locale.code)">
        Switch to {{ $t(locale.code) }}
      </button>
    </div>
  </div>
</template>

<script setup>
import { useNuxtApp } from '#imports'

const { $getLocale, $switchLocale, $getLocales, $t } = useNuxtApp()
</script>
```

### 語系切換的 JSON 範例

```json
{
  "en": "English",
  "fr": "Français",
  "de": "Deutsch"
}
```

### 使用 `<i18n-switcher>` 元件

`<i18n-switcher>` 元件提供一個下拉式選單來切換語系,並可自訂標籤:

```vue
<template>
  <div>
    <i18n-switcher :custom-labels="{ en: 'English', fr: 'Français', de: 'Deutsch' }" />
  </div>
</template>
```

## 🌐 使用 `<i18n-link>` 進行本地化導覽

`<i18n-link>` 元件會自動處理各語系對應的路由:

```vue
<template>
  <div>
    <i18n-link to="/about">{{ $t('about') }}</i18n-link>
    <i18n-link :to="{ name: 'index' }">{{ $t('home') }}</i18n-link>
  </div>
</template>

<script setup>
import { useNuxtApp } from '#imports'

const { $getLocale, $switchLocale, $getLocales, $t } = useNuxtApp()
</script>
```

### 導覽連結的 JSON 範例

```json
{
  "about": "About Us",
  "home": "Home"
}
```

### 具有 Active 樣式的連結範例

```vue
<template>
  <div>
    <i18n-link to="/about" :activeStyle="{ fontWeight: 'bold' }">About Us</i18n-link>
  </div>
</template>
```

### Active 樣式的 JSON 範例

可使用與前一範例相同的 JSON 檔案。

## 📝 從翻譯檔案渲染動態鍵

在某些情境下,你可能需要迭代儲存在翻譯檔中的動態鍵,並有條件地渲染其值。以下範例示範如何用 `Nuxt I18n Micro` 達成此需求。

### 範例:渲染動態鍵

此範例從特定翻譯路徑(這裡是 `dynamic`)取得一組鍵並進行迭代。每個鍵在用 `$t` 渲染其值之前,會先用 `$has` 檢查是否存在。

```vue
<template>
  <div>
    <div v-for="key in $t('dynamic')" :key="key">
      <p>
        {{ key }}: <span v-if="$has(key)">{{ $t(key) }}</span>
      </p>
    </div>
  </div>
</template>

<script setup>
import { useNuxtApp } from '#imports'

const { $t, $has } = useNuxtApp()
</script>
```

### 翻譯檔範例(`en.json`)

```json
{
  "dynamic": ["key1", "key2", "key3"],
  "key1": "This is the first key's value",
  "key2": "This is the second key's value",
  "key3": "This is the third key's value"
}
```

### 範例:從物件渲染動態鍵

在此範例中,我們處理儲存在翻譯檔內的物件。我們取得並迭代物件的鍵,動態渲染鍵名與對應值。

```vue
<template>
  <div>
    <div v-for="(value, key) in $t('dynamicObject')" :key="key">
      <p>{{ key }}: {{ value }}</p>
    </div>
  </div>
</template>

<script setup>
import { useNuxtApp } from '#imports'

const { $t } = useNuxtApp()
</script>
```

### 翻譯檔範例(`en.json`)

```json
{
  "dynamicObject": {
    "title": "Welcome to our site",
    "description": "This is a brief description of our services.",
    "footerNote": "Thank you for visiting!"
  }
}
```

## 🌟 使用 `<i18n-t>` 搭配插槽與插值

`<i18n-t>` 元件適合用來渲染帶有動態內容與 HTML 標籤的翻譯:

```vue
<template>
  <i18n-t keypath="greeting" tag="h1">
    <template #default="{ translation }">
      <strong>{{ translation.replace('page', 'page replace') }}</strong> <i>!!!</i>
    </template>
  </i18n-t>
</template>
```

### `<i18n-t>` 的 JSON 範例

```json
{
  "greeting": "Welcome to the page"
}
```

### 搭配插值

```vue
<template>
  <i18n-t keypath="welcome" :params="{ username: 'Alice', unreadCount: 5 }"></i18n-t>
</template>
```

### 插值的 JSON 範例

```json
{
  "welcome": "Hello {username}, you have {unreadCount} unread messages."
}
```

## 📝 具有巢狀區塊的綜合範例

以下是在單一頁面中示範多種翻譯用法的複雜結構:

```vue
<template>
  <div>
    <h1>{{ $t('mainHeader') }}</h1>

    <nav>
      <ul>
        <li>
          <a href="#">{{ $t('nav.home') }}</a>
        </li>
        <li>
          <a href="#">{{ $t('nav.about') }}</a>
        </li>
        <li>
          <a href="#">{{ $t('nav.services') }}</a>
        </li>
        <li>
          <a href="#">{{ $t('nav.contact') }}</a>
        </li>
      </ul>
    </nav>

    <section>
      <h2>{{ $t('section1.header') }}</h2>
      <p>{{ $t('section1.intro') }}</p>

      <div>
        <h3>{{ $t('section1.subsection1.header') }}</h3>
        <p>{{ $t('section1.subsection1.content') }}</p>
      </div>

      <div>
        <h3>{{ $t('section1.subsection2.header') }}</h3>
        <ul>
          <li>{{ $t('section1.subsection2.item1') }}</li>
          <li>{{ $t('section1.subsection2.item2') }}</li>
          <li>{{ $t('section1.subsection2.item3') }}</li>
        </ul>
      </div>
    </section>

    <footer>
      <h4>{{ $t('footer.contact.header') }}</h4>
      <address>
        {{ $t('footer.contact.address') }}<br />
        {{ $t('footer.contact.city') }}<br />
        {{ $t('footer.contact.phone') }}
      </address>
    </footer>

    <div>
      <button v-for="locale in $getLocales()" :key="locale.code" :disabled="locale.code === $getLocale()" @click="() => $switchLocale(locale.code)">
        Switch to {{ locale.code }}
      </button>
    </div>
  </div>
</template>

<script setup>
import { useNuxtApp } from '#imports'

const { $getLocale, $switchLocale, $getLocales, $t } = useNuxtApp()
</script>
```

### 巢狀區塊的 JSON 範例

```json
{
  "mainHeader": "Welcome to Our Services",
  "nav": {
    "home": "Home",
    "about": "About Us",
    "services": "Services",
    "contact": "Contact"
  },
  "section1": {
    "header": "Our Expertise",
    "intro": "We provide a wide range of services to meet your needs.",
    "subsection1": {
      "header": "Consulting",
      "content": "Our team offers expert consulting services in various domains."
    },
    "subsection2": {
      "header": "Development",
      "item1": "Web Development",
      "item2": "Mobile Apps",
      "item3": "Custom Software"
    }
  },
  "footer": {
    "contact": {
      "header": "Contact Us",
      "address": "123 Main Street",
      "city": "Anytown, USA",
      "phone": "+1 (555) 123-4567"
    }
  }
}
```

## 🌟 使用 `$tc` 處理複數形式

`Nuxt I18n Micro` 的 `$tc` 函式會依據計數與語系設定處理複數形式。這對於動態調整涉及數量的訊息(例如項目、通知或其他數量可變的實體)非常有用。

### 範例:使用 `$tc` 處理複數形式

在以下範例中,我們使用 `$tc` 顯示蘋果數量的訊息。翻譯鍵會根據提供的計數處理多種複數形式。

```vue
<template>
  <div>
    <!-- Display a pluralized message about the number of apples -->
    <p>{{ $tc('apples', 0) }}</p>
    <!-- Outputs: no apples -->
    <p>{{ $tc('apples', 1) }}</p>
    <!-- Outputs: one apple -->
    <p>{{ $tc('apples', 10) }}</p>
    <!-- Outputs: 10 apples -->
  </div>
</template>

<script setup>
import { useNuxtApp } from '#imports'

const { $tc } = useNuxtApp()
</script>
```

### 複數形式的 JSON 範例

以下是在 JSON 檔中定義翻譯以處理不同複數形式的方式:

```json
{
  "apples": "no apples | one apple | {count} apples"
}
```

### 說明

- **`$tc('apples', 0)`**:回傳第一種形式,計數為零時使用(`"no apples"`)。
- **`$tc('apples', 1)`**:回傳第二種形式,計數為 1 時使用(`"one apple"`)。
- **`$tc('apples', 10)`**:回傳含計數值的第三種形式,計數大於等於 2 時使用(`"10 apples"`)。

### 進階複數規則範例

若你的應用需要處理更複雜的複數規則(例如針對 zero、one、two、few、many、other 的個別情形),可以將翻譯字串延伸如下:

```json
{
  "apples": "no apples | one apple | two apples | a few apples | many apples | {count} apples"
}
```

- **`$tc('apples', 2)`**:可設定為回傳 `"two apples"`。
- **`$tc('apples', 3)`**:可回傳 `"a few apples"`,取決於為該計數定義的規則。

## 🌐 使用 `$tn` 進行數字格式化

`$tn` 函式使用 `Intl.NumberFormat` API,依當前語系格式化數字。這對於以符合使用者地區設定的方式顯示數字(例如貨幣、百分比或其他數字格式)非常有用。

### 範例:使用 `$tn` 進行數字格式化

```vue
<template>
  <div>
    <!-- Format a number as currency -->
    <p>{{ $tn(1234567.89, { style: 'currency', currency: 'USD' }) }}</p>
    <!-- Outputs: $1,234,567.89 in 'en-US' locale -->

    <!-- Format a number with custom options -->
    <p>{{ $tn(0.567, { style: 'percent', minimumFractionDigits: 1 }) }}</p>
    <!-- Outputs: 56.7% in 'en-US' locale -->
  </div>
</template>

<script setup>
import { useNuxtApp } from '#imports'

const { $tn } = useNuxtApp()
</script>
```

## 🗓️ 使用 `$td` 進行日期與時間格式化

`$td` 函式使用 `Intl.DateTimeFormat` API,依當前語系格式化日期與時間。這對於以使用者熟悉的地區格式顯示日期與時間非常有用。

### 範例:使用 `$td` 進行日期與時間格式化

```vue
<template>
  <div>
    <!-- Format a date with full options -->
    <p>{{ $td(new Date(), { weekday: 'long', year: 'numeric', month: 'long', day: 'numeric' }) }}</p>
    <!-- Outputs: "Friday, September 1, 2023" in 'en-US' locale -->

    <!-- Format a date with time -->
    <p>{{ $td(new Date(), { hour: '2-digit', minute: '2-digit', second: '2-digit' }) }}</p>
    <!-- Outputs: "10:15:30 AM" in 'en-US' locale -->
  </div>
</template>

<script setup>
import { useNuxtApp } from '#imports'

const { $td } = useNuxtApp()
</script>
```

## 🗓️ 使用 `$tdr` 進行相對日期格式化

`$tdr` 函式使用 `Intl.RelativeTimeFormat` API,依當前語系將日期格式化為相對時間(例如「5 分鐘前」、「2 天前」)。這對於顯示相對於當前時間的時間差非常有用。

### 範例:使用 `$tdr` 進行相對日期格式化

```vue
<template>
  <div>
    <!-- Format a date as a relative time -->
    <p>{{ $tdr(new Date(Date.now() - 1000 * 60 * 5)) }}</p>
    <!-- Outputs: "5 minutes ago" in 'en-US' locale -->

    <!-- Format a date that is in the future -->
    <p>{{ $tdr(new Date(Date.now() + 1000 * 60 * 60 * 24)) }}</p>
    <!-- Outputs: "in 1 day" in 'en-US' locale -->
  </div>
</template>

<script setup>
import { useNuxtApp } from '#imports'

const { $tdr } = useNuxtApp()
</script>
```
