---
title: "@i18n-micro/utils"
description: "Экспортируемый API @i18n-micro/utils, сгенерированный из исходного кода."
---

# `@i18n-micro/utils`

102 экспортов в 24 точках входа.
Сгенерировано из снапшота API, который [`pnpm run api:surface`](https://github.com/s00d/nuxt-i18n-micro/blob/main/docs/guide/maintenance-commands.md#api-surface)
сверяет с исходниками TypeScript.

## `@i18n-micro/utils/accept-language`

```ts
import { /* … */ } from '@i18n-micro/utils/accept-language'
```

| Экспорт | Вид | Сигнатура |
| --- | --- | --- |
| `applyAutoDetectLanguage` | функция | `(locale: string, hasExplicitPreference: boolean, autoDetectLanguage: boolean \| undefined, acceptLanguageHeader: string \| undefined, validLocales: string[]) => string` |
| `detectLocaleFromAcceptLanguage` | функция | `(header: string \| undefined, validLocales: string[]) => string \| null` |
| `parseAcceptLanguage` | функция | `(header: string \| undefined) => string[]` |

## `@i18n-micro/utils/active-locales`

```ts
import { /* … */ } from '@i18n-micro/utils/active-locales'
```

| Экспорт | Вид | Сигнатура |
| --- | --- | --- |
| `getEnabledLocaleCodes` | функция | `(locales?: Locale[] \| null) => string[]` |
| `getEnabledLocales` | функция | `(locales?: Locale[] \| null) => Locale[]` |
| `isEnabledLocale` | функция | `(locales: Locale[] \| null \| undefined, code: string) => boolean` |

## `@i18n-micro/utils/app-path`

```ts
import { /* … */ } from '@i18n-micro/utils/app-path'
```

| Экспорт | Вид | Сигнатура |
| --- | --- | --- |
| `withoutAppBaseURL` | функция | `(pathname: string, baseURL?: string \| null) => string` |

## `@i18n-micro/utils/auto-detect-path`

```ts
import { /* … */ } from '@i18n-micro/utils/auto-detect-path'
```

| Экспорт | Вид | Сигнатура |
| --- | --- | --- |
| `shouldAttemptLocaleRedirect` | функция | `(path: string, options?: \{ autoDetectPath?: string; hasLocalePrefix?: boolean; \}) => boolean` |

## `@i18n-micro/utils/build`

```ts
import { /* … */ } from '@i18n-micro/utils/build'
```

| Экспорт | Вид | Сигнатура |
| --- | --- | --- |
| `buildTranslationSourceLayers` | функция | `(rootDirs: string[], translationDirName: string, outputDir: string) => Promise<void>` |
| `PreMergeLocaleInfo` | интерфейс | 2 членов |
| `preMergeLocales` | функция | `(rootDirs: string[], translationDirName: string, outputDir: string, locales: PreMergeLocaleInfo[], globalFallbackLocale?: string, disablePageLocales?: boolean) => Promise<void>` |

<details>
<summary><code>PreMergeLocaleInfo</code> — 2 членов</summary>

| Член | Тип |
| --- | --- |
| `code` | `string` |
| `fallbackLocale?` | `string \| undefined` |

</details>
## `@i18n-micro/utils/cache-control`

```ts
import { /* … */ } from '@i18n-micro/utils/cache-control'
```

| Экспорт | Вид | Сигнатура |
| --- | --- | --- |
| `CacheControl` | класс | `<T>` |
| `CacheControlOptions` | интерфейс | 2 членов |

<details>
<summary><code>CacheControl</code> — 13 членов</summary>

| Член | Тип |
| --- | --- |
| `cache` | `private Map<string, T>` |
| `clear` | `() => void` |
| `configure` | `(options: CacheControlOptions) => void` |
| `delete` | `(key: string) => boolean` |
| `expiry` | `private Map<string, number>` |
| `get` | `(key: string) => T \| undefined` |
| `has` | `(key: string) => boolean` |
| `keys` | `() => IterableIterator<string>` |
| `maxSize` | `private number` |
| `new` | `<T>(options?: CacheControlOptions \| undefined): CacheControl<T>` |
| `set` | `(key: string, value: T) => void` |
| `size` | `number` |
| `ttlMs` | `private number` |

</details>
<details>
<summary><code>CacheControlOptions</code> — 2 членов</summary>

| Член | Тип |
| --- | --- |
| `maxSize?` | `number \| undefined` |
| `ttl?` | `number \| undefined` |

</details>
## `@i18n-micro/utils/cookie`

```ts
import { /* … */ } from '@i18n-micro/utils/cookie'
```

| Экспорт | Вид | Сигнатура |
| --- | --- | --- |
| `DEFAULT_COOKIE_MAX_AGE` | константа | `number` |
| `DEFAULT_COOKIE_NAME` | константа | `"user-locale"` |
| `DEFAULT_HASH_COOKIE_NAME` | константа | `"hash-locale"` |
| `getHashCookieName` | функция | `(config: ModuleOptionsExtend) => string \| null` |
| `getLocaleCookieName` | функция | `(config: ModuleOptionsExtend) => string \| null` |
| `getLocaleCookieOptions` | функция | `() => \{ expires: Date; maxAge: number; path: string; watch: boolean; sameSite: "lax"; \}` |

## `@i18n-micro/utils/deep-merge`

```ts
import { /* … */ } from '@i18n-micro/utils/deep-merge'
```

| Экспорт | Вид | Сигнатура |
| --- | --- | --- |
| `deepMergeTranslations` | функция | `(target: Record<string, unknown>, source: Record<string, unknown>) => Record<string, unknown>` |
| `deepMergeTranslationsRecursive` | функция | `(target: Record<string, unknown>, source: Record<string, unknown>) => Record<string, unknown>` |

## `@i18n-micro/utils/merge-i18n-head`

```ts
import { /* … */ } from '@i18n-micro/utils/merge-i18n-head'
```

| Экспорт | Вид | Сигнатура |
| --- | --- | --- |
| `I18nHeadObject` | интерфейс | 3 членов |
| `mergeI18nHead` | функция | `(base: I18nHeadObject, override: I18nHeadInput \| null \| undefined, options?: MergeI18nHeadOptions) => I18nHeadObject` |
| `MergeI18nHeadOptions` | интерфейс | 3 членов |

<details>
<summary><code>I18nHeadObject</code> — 3 членов</summary>

| Член | Тип |
| --- | --- |
| `htmlAttrs` | `Record<string, string \| undefined>` |
| `link` | `I18nHeadLink[]` |
| `meta` | `I18nHeadMeta[]` |

</details>
<details>
<summary><code>MergeI18nHeadOptions</code> — 3 членов</summary>

| Член | Тип |
| --- | --- |
| `currentLocale?` | `string \| undefined` |
| `identifierAttribute?` | `string \| undefined` |
| `locales?` | `Locale[] \| undefined` |

</details>
## `@i18n-micro/utils/merge-source`

```ts
import { /* … */ } from '@i18n-micro/utils/merge-source'
```

| Экспорт | Вид | Сигнатура |
| --- | --- | --- |
| `buildFallbackLocaleChain` | функция | `(locale: string, locales: SourceLocaleInfo[], globalFallbackLocale?: string) => string[]` |
| `buildSourcePagePath` | функция | `(pageName: string, localeCode: string) => string` |
| `buildSourceRootPath` | функция | `(localeCode: string) => string` |
| `mergeSourceTranslations` | функция | `(input: MergeSourceTranslationsInput) => Promise<Record<string, unknown>>` |
| `MergeSourceTranslationsInput` | интерфейс | 6 членов |
| `mergeTranslationsFromFallbackChain` | функция | `(chain: string[], readLocaleFile: (relativePath: string) => Record<string, unknown>, relativePathForLocale: (localeCode: string) => string) => Record<string, unknown>` |
| `mergeTranslationsFromFallbackChainAsync` | функция | `(chain: string[], readLocaleFile: (relativePath: string) => Record<string, unknown> \| Promise<Record<string, unknown>>, relativePathForLocale: (localeCode: string) => string) => Promise<Record<string, unknown>>` |
| `normalizeConfiguredLocales` | функция | `(locales: Array<string \| Locale> \| undefined) => SourceLocaleInfo[]` |
| `resolveSourcePageName` | функция | `(pageName: string, disablePageLocales?: boolean) => string` |
| `SourceLocaleInfo` | интерфейс | 2 членов |
| `toPremergedStorageKey` | функция | `(pageName: string, locale: string) => string` |
| `toSourceStorageKey` | функция | `(relativePath: string) => string` |

<details>
<summary><code>MergeSourceTranslationsInput</code> — 6 членов</summary>

| Член | Тип |
| --- | --- |
| `disablePageLocales?` | `boolean \| undefined` |
| `globalFallbackLocale?` | `string \| undefined` |
| `locale` | `string` |
| `locales` | `SourceLocaleInfo[]` |
| `pageName` | `string` |
| `readLocaleFile` | `(relativePath: string) => Record<string, unknown> \| Promise<Record<string, unknown>>` |

</details>
<code>SourceLocaleInfo</code> — 2 members, identical to `PreMergeLocaleInfo` above.
## `@i18n-micro/utils/normalize`

```ts
import { /* … */ } from '@i18n-micro/utils/normalize'
```

| Экспорт | Вид | Сигнатура |
| --- | --- | --- |
| `toTranslationRecord` | функция | `(data: unknown) => Record<string, unknown>` |
| `toTranslations` | функция | `(data: unknown) => Translations` |

## `@i18n-micro/utils/parse-path`

```ts
import { /* … */ } from '@i18n-micro/utils/parse-path'
```

| Экспорт | Вид | Сигнатура |
| --- | --- | --- |
| `classifyTranslationRelativePath` | функция | `(relativePath: string, disablePageLocales?: boolean) => ParsedTranslationRelativePath` |
| `mergeRouteTranslationsWithRoot` | функция | `<T extends Record<string, unknown>>(rootTranslations: T \| undefined, routeTranslations: T) => T` |
| `pagePathSegmentsToRouteName` | функция | `(segments: string[]) => string \| null` |
| `ParsedTranslationRelativePath` | тип | `\{ type: 'page'; pageName: string; locale: string \} \| \{ type: 'root'; locale: string \} \| \{ type: 'ignore' \}` |
| `parseTranslationRelativePath` | функция | `(relativePath: string) => ParsedTranslationRelativePath` |
| `storeLoadedTranslationFile` | функция | `<T extends Record<string, unknown>>(buckets: TranslationFileBuckets<T>, relativePath: string, translations: T, disablePageLocales?: boolean) => void` |
| `TranslationFileBuckets` | интерфейс | `<T = Record<string, unknown>>` |

<details>
<summary><code>TranslationFileBuckets</code> — 2 членов</summary>

| Член | Тип |
| --- | --- |
| `root` | `Record<string, T>` |
| `routes` | `Record<string, Record<string, T>>` |

</details>
## `@i18n-micro/utils/payload-config`

```ts
import { /* … */ } from '@i18n-micro/utils/payload-config'
```

| Экспорт | Вид | Сигнатура |
| --- | --- | --- |
| `DEFAULT_TRANSLATION_PAYLOAD_WARN_FILE_COUNT` | константа | `500` |
| `DEFAULT_TRANSLATION_PAYLOAD_WARN_SIZE_BYTES` | константа | `number` |
| `formatBytes` | функция | `(bytes: number) => string` |
| `getTranslationPayloadMisconfigurationWarnings` | функция | `(input: TranslationPayloadMisconfigurationInput) => string[]` |
| `getTranslationPayloadSizeWarning` | функция | `(stats: TranslationPayloadStats, thresholds?: TranslationPayloadSizeThresholds) => string \| null` |
| `hasLocalTranslationPayloadOutput` | функция | `(translationPayloads: ResolvedTranslationPayloadOptions) => boolean` |
| `ResolvedTranslationPayloadOptions` | интерфейс | 8 членов |
| `resolveTranslationPayloadMode` | функция | `(options: ModuleOptions) => TranslationPayloadMode` |
| `resolveTranslationPayloadOptions` | функция | `(options: ModuleOptions) => ResolvedTranslationPayloadOptions` |
| `resolveTranslationPayloadPublicDir` | функция | `(outputPublicDir: string \| undefined, options: ModuleOptions, apiBaseUrl?: string) => string` |
| `resolveTranslationPayloadPublicRel` | функция | `(options: ModuleOptions, apiBaseUrl?: string) => string` |
| `resolveTranslationPayloadWarningThresholds` | функция | `(options?: TranslationPayloadOptions) => Required<TranslationPayloadSizeThresholds>` |
| `shouldCopyTranslationPayloadsToPublic` | функция | `(translationPayloads: ResolvedTranslationPayloadOptions, isNode: boolean) => boolean` |
| `shouldRegisterNitroServerAssets` | функция | `(translationPayloads: ResolvedTranslationPayloadOptions, isNode: boolean) => boolean` |
| `TranslationPayloadMisconfigurationInput` | интерфейс | 3 членов |
| `TranslationPayloadMode` | тип | `'premerged' \| 'source'` |
| `TranslationPayloadSizeThresholds` | интерфейс | 2 членов |
| `TranslationPayloadStats` | интерфейс | 2 членов |

<details>
<summary><code>ResolvedTranslationPayloadOptions</code> — 8 членов</summary>

| Член | Тип |
| --- | --- |
| `mode` | `TranslationPayloadMode` |
| `prerenderRoutes` | `boolean` |
| `publicAssets` | `boolean` |
| `publicDir?` | `string \| undefined` |
| `serverAssets` | `boolean` |
| `serverHandler` | `boolean` |
| `warnFileCount?` | `number \| undefined` |
| `warnSizeBytes?` | `number \| undefined` |

</details>
<details>
<summary><code>TranslationPayloadMisconfigurationInput</code> — 3 членов</summary>

| Член | Тип |
| --- | --- |
| `apiBaseClientHost?` | `string \| undefined` |
| `apiBaseServerHost?` | `string \| undefined` |
| `translationPayloads` | `ResolvedTranslationPayloadOptions` |

</details>
<details>
<summary><code>TranslationPayloadSizeThresholds</code> — 2 членов</summary>

| Член | Тип |
| --- | --- |
| `warnFileCount?` | `number \| undefined` |
| `warnSizeBytes?` | `number \| undefined` |

</details>
<details>
<summary><code>TranslationPayloadStats</code> — 2 членов</summary>

| Член | Тип |
| --- | --- |
| `fileCount` | `number` |
| `totalBytes` | `number` |

</details>
## `@i18n-micro/utils/payload-fetch`

```ts
import { /* … */ } from '@i18n-micro/utils/payload-fetch'
```

| Экспорт | Вид | Сигнатура |
| --- | --- | --- |
| `fetchTranslationPayloadFromHost` | функция | `(config: TranslationPayloadHostConfig, locale: string, page: string, fetcher: TranslationPayloadFetcher) => Promise<Translations>` |
| `TranslationPayloadFetcher` | тип | `<T>(path: string, options: \{ baseURL: string; params?: Record<string, string \| number> \}) => Promise<T>` |
| `TranslationPayloadHostConfig` | интерфейс | 3 членов |

<details>
<summary><code>TranslationPayloadHostConfig</code> — 3 членов</summary>

| Член | Тип |
| --- | --- |
| `apiBaseServerHost?` | `string \| undefined` |
| `apiBaseUrl?` | `string \| undefined` |
| `dateBuild?` | `string \| number \| undefined` |

</details>
## `@i18n-micro/utils/payload-stats`

```ts
import { /* … */ } from '@i18n-micro/utils/payload-stats'
```

| Экспорт | Вид | Сигнатура |
| --- | --- | --- |
| `compressTranslationPayloads` | функция | `(dir: string, compression: PublicAssetCompression) => number` |
| `hashTranslationSources` | функция | `(rootDirs: string[], translationDirName: string) => string \| null` |
| `PublicAssetCompression` | тип | `boolean \| \{ gzip?: boolean; brotli?: boolean \} \| undefined` |
| `scanTranslationPayloadDirectory` | функция | `(dir: string) => TranslationPayloadStats` |

## `@i18n-micro/utils/payload-url`

```ts
import { /* … */ } from '@i18n-micro/utils/payload-url'
```

| Экспорт | Вид | Сигнатура |
| --- | --- | --- |
| `buildTranslationPayloadCacheControl` | функция | `(durationSeconds: number \| undefined, hasCacheBuster?: boolean) => string \| null` |
| `buildTranslationPayloadFetchRequest` | функция | `(input: TranslationPayloadFetchRequestInput) => \{ path: string; baseURL: string; params?: Record<string, string \| number>; \}` |
| `buildTranslationPayloadPath` | функция | `(apiBaseUrl: string, page: string, locale: string) => string` |
| `resolveTranslationPayloadBaseURL` | функция | `(options: \{ isServer: boolean; baseURL: string; apiBaseClientHost?: string; apiBaseServerHost?: string; \}) => string` |
| `resolveTranslationPayloadPage` | функция | `(routeName: string \| undefined, routesLocaleLinks?: Record<string, string>) => string` |
| `TranslationPayloadFetchRequestInput` | интерфейс | 9 членов |

<details>
<summary><code>TranslationPayloadFetchRequestInput</code> — 9 членов</summary>

| Член | Тип |
| --- | --- |
| `apiBaseClientHost?` | `string \| undefined` |
| `apiBaseServerHost?` | `string \| undefined` |
| `apiBaseUrl` | `string` |
| `baseURL` | `string` |
| `dateBuild?` | `string \| number \| undefined` |
| `isServer` | `boolean` |
| `locale` | `string` |
| `routeName?` | `string \| undefined` |
| `routesLocaleLinks?` | `Record<string, string> \| undefined` |

</details>
## `@i18n-micro/utils/resolve-hreflang`

```ts
import { /* … */ } from '@i18n-micro/utils/resolve-hreflang'
```

| Экспорт | Вид | Сигнатура |
| --- | --- | --- |
| `HreflangAlternate` | интерфейс | 2 членов |
| `HreflangLocaleInput` | тип | `Pick<Locale, 'code' \| 'iso'>` |
| `resolveHreflangAlternates` | функция | `(locales: HreflangLocaleInput[], options?: ResolveHreflangAlternatesOptions) => HreflangAlternate[]` |
| `ResolveHreflangAlternatesOptions` | интерфейс | 1 членов |

<details>
<summary><code>HreflangAlternate</code> — 2 членов</summary>

| Член | Тип |
| --- | --- |
| `hreflang` | `string` |
| `localeCode` | `string` |

</details>
<details>
<summary><code>ResolveHreflangAlternatesOptions</code> — 1 членов</summary>

| Член | Тип |
| --- | --- |
| `hreflangBaseLanguage?` | `boolean \| undefined` |

</details>
## `@i18n-micro/utils/resolve-locale`

```ts
import { /* … */ } from '@i18n-micro/utils/resolve-locale'
```

| Экспорт | Вид | Сигнатура |
| --- | --- | --- |
| `resolvePreferredLocale` | функция | `(input: ResolvePreferredLocaleInput) => string` |
| `ResolvePreferredLocaleInput` | интерфейс | 7 членов |
| `resolveServerLocale` | функция | `(input: ResolveServerLocaleInput) => string` |
| `ResolveServerLocaleInput` | интерфейс | 8 членов |

<details>
<summary><code>ResolvePreferredLocaleInput</code> — 7 членов</summary>

| Член | Тип |
| --- | --- |
| `acceptLanguageHeader?` | `string \| null \| undefined` |
| `autoDetectLanguage?` | `boolean \| undefined` |
| `cookieLocale?` | `string \| null \| undefined` |
| `defaultLocale` | `string` |
| `ignoreStateLocale?` | `boolean \| undefined` |
| `stateLocale?` | `string \| null \| undefined` |
| `validLocales` | `string[]` |

</details>
<details>
<summary><code>ResolveServerLocaleInput</code> — 8 членов</summary>

| Член | Тип |
| --- | --- |
| `acceptLanguageHeader?` | `string \| null \| undefined` |
| `autoDetectLanguage?` | `boolean \| undefined` |
| `cookieLocale?` | `string \| null \| undefined` |
| `defaultLocale` | `string` |
| `hasLocaleInUrl` | `boolean` |
| `queryLocale?` | `string \| null \| undefined` |
| `urlLocale?` | `string \| null \| undefined` |
| `validLocales` | `string[]` |

</details>
## `@i18n-micro/utils/resolve-og-locale`

```ts
import { /* … */ } from '@i18n-micro/utils/resolve-og-locale'
```

| Экспорт | Вид | Сигнатура |
| --- | --- | --- |
| `resolveOgLocale` | функция | `(locale: OgLocaleInput) => string \| null` |
| `warnUnresolvedOgLocale` | функция | `(locale: OgLocaleInput, options?: WarnUnresolvedOgLocaleOptions) => void` |
| `WarnUnresolvedOgLocaleOptions` | интерфейс | 2 членов |

<details>
<summary><code>WarnUnresolvedOgLocaleOptions</code> — 2 членов</summary>

| Член | Тип |
| --- | --- |
| `missingWarn?` | `boolean \| undefined` |
| `tag?` | `"og:locale" \| "og:locale:alternate" \| undefined` |

</details>
## `@i18n-micro/utils/route`

```ts
import { /* … */ } from '@i18n-micro/utils/route'
```

| Экспорт | Вид | Сигнатура |
| --- | --- | --- |
| `findAllowedLocalesForRoute` | функция | `(route: I18nRouteContext, routeLocales: Record<string, string[]> \| undefined, localizedRouteNamePrefix?: string, localeCodes?: string[]) => string[] \| null` |
| `I18nRouteContext` | интерфейс | 3 членов |
| `isMetaDisabledForRoute` | функция | `(route: I18nRouteContext, routeDisableMeta: Record<string, boolean \| string[]> \| undefined, currentLocale?: string, localizedRouteNamePrefix?: string) => boolean` |

<details>
<summary><code>I18nRouteContext</code> — 3 членов</summary>

| Член | Тип |
| --- | --- |
| `matched?` | `\{ path: string; \}[] \| undefined` |
| `name?` | `string \| symbol \| null \| undefined` |
| `path` | `string` |

</details>
## `@i18n-micro/utils/route-pattern`

```ts
import { /* … */ } from '@i18n-micro/utils/route-pattern'
```

| Экспорт | Вид | Сигнатура |
| --- | --- | --- |
| `extractBaseRoutePattern` | функция | `(matchedPath: string) => string` |
| `routeNameToPath` | функция | `(normalizedRouteName: string \| undefined) => string \| undefined` |
| `stripLeadingSlash` | функция | `(path: string) => string` |
| `stripLocalizedRouteNamePrefix` | функция | `(routeName: string \| undefined \| null, localizedRouteNamePrefix?: string) => string \| undefined` |

## `@i18n-micro/utils/runtime-config`

```ts
import { /* … */ } from '@i18n-micro/utils/runtime-config'
```

| Экспорт | Вид | Сигнатура |
| --- | --- | --- |
| `resolveI18nConfigWithRuntimeOverrides` | функция | `(baseConfig: ModuleOptionsExtend, runtimePublic?: Record<string, unknown>, warn?: (message: string) => void) => ModuleOptionsExtend` |
| `RuntimeI18nOverrides` | интерфейс | 4 членов |

<details>
<summary><code>RuntimeI18nOverrides</code> — 4 членов</summary>

| Член | Тип |
| --- | --- |
| `defaultLocale?` | `string \| undefined` |
| `disabledLocales?` | `string[] \| undefined` |
| `fallbackLocale?` | `string \| undefined` |
| `strategy?` | `string \| undefined` |

</details>
## `@i18n-micro/utils/source-loader`

```ts
import { /* … */ } from '@i18n-micro/utils/source-loader'
```

| Экспорт | Вид | Сигнатура |
| --- | --- | --- |
| `loadSourceTranslationsFromStorage` | функция | `(storage: TranslationStorageReader, input: Omit<MergeSourceTranslationsInput, "readLocaleFile">) => Promise<Translations>` |
| `TranslationStorageReader` | интерфейс | 1 членов |

<details>
<summary><code>TranslationStorageReader</code> — 1 членов</summary>

| Член | Тип |
| --- | --- |
| `getItem` | `(key: string) => Promise<unknown>` |

</details>
## `@i18n-micro/utils/split-locale-routes`

```ts
import { /* … */ } from '@i18n-micro/utils/split-locale-routes'
```

| Экспорт | Вид | Сигнатура |
| --- | --- | --- |
| `SplitLocaleRouteEntry` | интерфейс | 4 членов |
| `SplitLocaleRoutePage` | интерфейс | 3 членов |
| `splitLocaleRoutes` | функция | `(entries: readonly SplitLocaleRouteEntry[]) => SplitLocaleRoutesResult` |
| `SplitLocaleRoutesResult` | интерфейс | 2 членов |

<details>
<summary><code>SplitLocaleRouteEntry</code> — 4 членов</summary>

| Член | Тип |
| --- | --- |
| `file` | `string` |
| `name` | `string` |
| `path` | `string` |
| `paths?` | `false \| Record<string, string> \| undefined` |

</details>
<details>
<summary><code>SplitLocaleRoutePage</code> — 3 членов</summary>

| Член | Тип |
| --- | --- |
| `file` | `string` |
| `name` | `string` |
| `path` | `string` |

</details>
<details>
<summary><code>SplitLocaleRoutesResult</code> — 2 членов</summary>

| Член | Тип |
| --- | --- |
| `globalLocaleRoutes` | `Record<string, boolean \| Record<string, string>>` |
| `pages` | `SplitLocaleRoutePage[]` |

</details>

Вернуться ко [всем пакетам](/api/packages/overview) · [Руководства по интеграции](/integrations/overview)
