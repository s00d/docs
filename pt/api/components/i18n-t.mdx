---
title: "Componente `<i18n-t>`"
description: "O componente <i18n-t> renderiza traduções com interpolação baseada em slots, permitindo injetar componentes Vue e markup rico nas chaves de locale."
---

# 🌍 Componente `<i18n-t>`

O componente `<i18n-t>` do `Nuxt I18n Micro` é um componente de tradução flexível que suporta a inserção de conteúdo dinâmico via slots. Permite interpolar traduções com componentes Vue personalizados ou conteúdo HTML, viabilizando cenários avançados de localização.

## ⚙️ Props

{/* generated:component:i18n-t — do not edit; run `pnpm run docs:generate` */}

| Prop | Tipo | Predefinição | Descrição |
| --- | --- | --- | --- |
| `keypath` **\*** | `string` | — | Chave de tradução a renderizar. |
| `plural` | `number \| string` | — | Contagem que seleciona a forma plural. Defina para utilizar `$tc` em vez de `$t`. |
| `tag` | `string` | `'span'` | Elemento a renderizar. |
| `params` | `Record<string, string \| number \| boolean>` | `() => (\{\})` | Valores de interpolação substituídos na tradução. |
| `defaultValue` | `string` | `''` | Renderizado em vez do componente quando `hideIfEmpty` suprime uma tradução vazia. |
| `html` | `boolean` | `false` | Renderiza a tradução como HTML em vez de texto. Utilize apenas com conteúdo sob o seu controlo. |
| `hideIfEmpty` | `boolean` | `false` | Não renderiza nada quando a tradução resolve para uma string vazia. |
| `customPluralRule` | `PluralFunc` | `null` | Regra plural apenas para este elemento, substituindo a configurada. |
| `number` | `number \| string` | — | Formata o valor com `$tn` e passa-o à tradução como `\{number\}`. |
| `date` | `Date \| string \| number` | — | Formata o valor com `$td` e passa-o à tradução como `\{date\}`. |
| `relativeDate` | `Date \| string \| number` | — | Formata o valor com `$tdr` e passa-o à tradução como `\{relativeDate\}`. |

**\*** obrigatório.

{/* /generated:component:i18n-t */}

Apenas um de `plural`, `number`, `date` e `relativeDate` se aplica de cada vez; quando vários são definidos, prevalece o primeiro nessa lista.

### Exemplos por prop

```vue
<!-- keypath: the translation to render -->
<i18n-t keypath="feedback.text" />

<!-- plural: selects the plural form -->
<i18n-t keypath="items" :plural="10" />

<!-- params: interpolated into the translation -->
<i18n-t keypath="greeting" :params="{ name: 'Ada' }" />

<!-- tag: the element to render -->
<i18n-t keypath="feedback.text" tag="p" />

<!-- number / date / relativeDate: formatted, then interpolated -->
<i18n-t keypath="cart.total" :number="1234.5" />
<i18n-t keypath="post.published" :date="post.createdAt" />
<i18n-t keypath="post.ago" :relative-date="post.createdAt" />

<!-- hideIfEmpty with a fallback -->
<i18n-t keypath="promo.banner" hide-if-empty default-value="Nothing to show" />
```

```json
{
  "items": "Nothing|You have {count} item|You have {count} items"
}
```

## 🛠️ Exemplos de utilização

### Utilização básica

```vue
<i18n-t keypath="feedback.text" />
```

Isto renderiza a tradução de `feedback.text` dentro de uma tag `<span>`.

### Utilizar slots para conteúdo dinâmico

O componente `<i18n-t>` suporta a utilização de slots para inserir dinamicamente componentes Vue ou outro conteúdo em partes específicas da tradução.

```vue
<i18n-t keypath="feedback.text">
  <template #link>
    <nuxt-link :to="{ name: 'index' }">
      <i18n-t keypath="feedback.link" />
    </nuxt-link>
  </template>
</i18n-t>
```

Neste exemplo, o marcador `\{link\}` na string de tradução `feedback.text` é substituído pelo componente `<nuxt-link>`, que por sua vez contém outro componente de tradução.

### Pluralização

```vue
<i18n-t keypath="items.count" :plural="itemCount" />
```

Isto aplica automaticamente regras de pluralização com base no valor de `itemCount`.

## 🚀 Funcionalidades adicionais

### Slot predefinido

Se não for fornecido nenhum slot específico, a tradução pode ser personalizada através do slot predefinido, que fornece a string traduzida completa:

```vue
<i18n-t keypath="welcome.message">
  <template #default="{ translation }">
    {{ translation.replace('Nuxt', 'Vue') }}
  </template>
</i18n-t>
```

### Renderização condicional

Não renderize nada se a string de tradução estiver vazia utilizando a prop `hideIfEmpty`.

```vue
<i18n-t keypath="optionalMessage" :hideIfEmpty="true"></i18n-t>
```

### Regra de pluralização personalizada

Utilize uma função personalizada para tratar a pluralização.

```vue
<i18n-t
  keypath="items"
  :plural="itemCount"
  :customPluralRule="
    (key, count, params, locale, t) => {
      const raw = t(key, params)
      if (!raw) return null
      return count === 1 ? 'One item' : `${count} items`
    }
  "
/>
```

### Exemplo avançado com slots

Utilize slots para personalizar como o conteúdo da tradução é renderizado.

```vue
<i18n-t keypath="welcomeMessage">
  <template #default="{ translation }">
    <strong>{{ translation }}</strong>
  </template>
</i18n-t>
```
