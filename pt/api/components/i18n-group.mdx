---
title: "Componente `<i18n-group>`"
description: "O componente Vue <i18n-group> agrupa traduções sob um prefixo de chave partilhado para manter concisos os rótulos aninhados e secções repetidas nos templates."
---

# 🌍 Componente `<i18n-group>`

O componente `<i18n-group>` do Nuxt I18n Micro oferece uma forma conveniente de agrupar traduções sob um prefixo comum, reduzindo repetição e melhorando a organização do código.

## ⚙️ Props

{/* generated:component:i18n-group — do not edit; run `pnpm run docs:generate` */}

| Prop | Tipo | Predefinição | Descrição |
| --- | --- | --- | --- |
| `prefix` **\*** | `string` | — | Prefixo de chave contra o qual todas as pesquisas dentro do slot são resolvidas. |
| `groupClass` | `string` | `''` | Classe extra no elemento envolvente. |

**\*** obrigatório.

### Slots

| Slot | Bindings | Descrição |
| --- | --- | --- |
| `default` | `prefix`, `t` | Recebe `prefix` e uma função `t` com âmbito nele. |

{/* /generated:component:i18n-group */}

## 🎯 Slots

O slot predefinido recebe `prefix` e uma função `t` com âmbito nele, para que as chaves usadas dentro sejam
escritas relativamente ao prefixo:

```vue
<i18n-group prefix="checkout">
  <template #default="{ t }">
    <h2>{{ t('title') }}</h2>
    <p>{{ t('summary', { count: 3 }) }}</p>
  </template>
</i18n-group>
```

## 🛠️ Exemplo de utilização

### Utilização básica

```vue
<template>
  <i18n-group prefix="product.details">
    <template #default="{ t }">
      <h1>{{ t('title') }}</h1>
      <p>{{ t('description') }}</p>
      <div class="price">{{ t('price', { value: 99.99 }) }}</div>
    </template>
  </i18n-group>
</template>
```

Ficheiro de tradução:

```json
{
  "product": {
    "details": {
      "title": "Premium Product",
      "description": "This is a premium product",
      "price": "Price: ${value}"
    }
  }
}
```

### Com classe personalizada

```vue
<template>
  <i18n-group prefix="user.profile" group-class="profile-section">
    <template #default="{ t }">
      <div class="user-info">
        <h2>{{ t('title') }}</h2>
        <p>{{ t('bio') }}</p>
        <div class="stats">
          <span>{{ t('stats.followers') }}</span>
          <span>{{ t('stats.following') }}</span>
        </div>
      </div>
    </template>
  </i18n-group>
</template>
```

Ficheiro de tradução:

```json
{
  "user": {
    "profile": {
      "title": "User Profile",
      "bio": "User biography goes here",
      "stats": {
        "followers": "Followers: {count}",
        "following": "Following: {count}"
      }
    }
  }
}
```

### Com conteúdo dinâmico

```vue
<template>
  <i18n-group prefix="blog.post">
    <template #default="{ t }">
      <article>
        <h1>{{ t('title') }}</h1>
        <div class="meta">
          {{ t('meta.author', { name: author }) }} |
          {{ t('meta.date', { date: publishDate }) }}
        </div>
        <div v-for="(section, index) in sections" :key="index">
          <h2>{{ t(`sections.${index}.title`) }}</h2>
          <p>{{ t(`sections.${index}.content`) }}</p>
        </div>
      </article>
    </template>
  </i18n-group>
</template>

<script setup>
const author = 'John Doe'
const publishDate = '2024-01-01'
const sections = ['intro', 'main', 'conclusion']
</script>
```

## 🎨 Estilo

O componente envolve o seu conteúdo numa div com a classe `i18n-group` e quaisquer classes adicionais especificadas na prop `groupClass`:

```css
.i18n-group {
  /* Your base styles */
}

.profile-section {
  /* Additional styles for profile sections */
}
```

## 🚀 Boas práticas

1. **Prefixos consistentes**: use prefixos consistentes e lógicos que reflitam a estrutura da sua aplicação

```vue
<i18n-group prefix="features.pricing">
<i18n-group prefix="features.security">
```

2. **Organização modular**: agrupe traduções relacionadas sob prefixos significativos

```vue
<i18n-group prefix="checkout.payment">
<i18n-group prefix="checkout.shipping">
```

3. **Componentes reutilizáveis**: crie componentes reutilizáveis com os seus próprios grupos de tradução

```vue
<!-- UserProfile.vue -->
<i18n-group prefix="user.profile">

<!-- UserSettings.vue -->
<i18n-group prefix="user.settings">
```

4. **Grupos aninhados**: evite aninhar grupos em demasia para manter clareza

```vue
<!-- Good -->
<i18n-group prefix="shop.product">

<!-- Avoid -->
<i18n-group prefix="shop.category.product.details.specs">
```
