---
title: "useI18nLocale"
description: "Centralized locale state, cookies, and sync."
---

# useI18nLocale

## 签名

{/* generated:symbol:useI18nLocale — do not edit; run `pnpm run docs:generate` */}

```ts
useI18nLocale(): { getEffectiveLocale: (route: unknown, getLocaleFromRoute: GetLocaleFromRoute) => string; getLocale: () => string | null; getLocaleWithServerFallback: (serverLocale?: string | null) => string | null; getPreferredLocale: () => string | null; hashCookie: any; isValidLocale: (locale: string | null | undefined) => locale is string; locale: any; localeCookie: any; resolveInitialLocale: (options: ResolveInitialLocaleOptions) => string; setLocale: (locale: string | null) => void; syncLocale: (locale: string | null) => void; validLocales: string[] }
```

i18n 区域管理的集中入口。
组合了 useState('i18n-locale')、区域 cookie 和同步工具。

{/* /generated:symbol:useI18nLocale */}

## 用法

```vue
<script setup>
const {
  locale,
  localeCookie,
  hashCookie,
  validLocales,
  getPreferredLocale,
  getLocale,
  getLocaleWithServerFallback,
  getEffectiveLocale,
  resolveInitialLocale,
  setLocale,
  syncLocale,
  isValidLocale,
} = useI18nLocale()

// Set locale (updates state and cookies)
setLocale('de')

// Get current locale (state → cookie)
const current = getLocale()

// Get valid preferred locale
const preferred = getPreferredLocale()
</script>
```

## API

| 属性/方法                                 | 类型                                | 说明                                                                          |
| ----------------------------------------------- | ----------------------------------- | ------------------------------------------------------------------------------------ |
| `locale`                                        | `Ref<string \| null>`               | 响应式区域状态（`useState('i18n-locale')`）                                    |
| `localeCookie`                                  | `Ref`                               | 区域 cookie（当 `localeCookie` 选项启用时）                                |
| `hashCookie`                                    | `Ref`                               | `hashMode` 激活时使用的 cookie                                                |
| `validLocales`                                  | `string[]`                          | 来自模块配置的已启用区域代码（排除 `disabled`）                        |
| `getLocale()`                                   | `() => string \| null`              | 当前区域：state → cookie（hash 模式下使用 hash cookie）                         |
| `getPreferredLocale()`                          | `() => string \| null`              | state/cookie 中的区域，经由 `validLocales` **校验**后返回，或返回 `null`             |
| `getLocaleWithServerFallback(serverLocale?)`    | `(serverLocale?) => string \| null` | state → cookie → 可选的服务端上下文（用于 `no_prefix` 初始化）              |
| `getEffectiveLocale(route, getLocaleFromRoute)` | `(route, fn) => string`             | 用于加载翻译的区域：hash 模式偏好 state；否则从路由读取 |
| `resolveInitialLocale(options)`                 | `(options) => string`               | 解析区域：state → `serverLocale` → 路由；需要时同步 state         |
| `setLocale(locale)`                             | `(locale: string \| null) => void`  | 设置区域并同步到 cookie                                                       |
| `syncLocale(locale)`                            | `(locale: string \| null) => void`  | 仅同步 cookie（不更新 state）                                                  |
| `isValidLocale(locale)`                         | `(locale) => boolean`               | 检查字符串是否在 `validLocales` 中                                                 |

### `resolveInitialLocale` 选项

```typescript
interface ResolveInitialLocaleOptions {
  route: unknown
  serverLocale?: string | null
  getLocaleFromRoute: (route?: unknown) => string
}
```

主插件在启动时内部使用。自定义插件在实现替代检测流程时可以调用它。

### `getEffectiveLocale`

```typescript
const { getEffectiveLocale } = useI18nLocale()
const { $getLocale } = useNuxtApp()

const locale = getEffectiveLocale(route, (r) => $getLocale(r))
```

返回用于加载翻译并为给定路由对象格式化消息的区域。

## 编程式区域设置

优先使用 `useI18nLocale()` 而非直接使用 `useState('i18n-locale')`：

```ts
// plugins/i18n-loader.server.ts
import { defineNuxtPlugin } from '#imports'

export default defineNuxtPlugin({
  name: 'i18n-custom-loader',
  enforce: 'pre',
  order: -10,

  setup() {
    const { setLocale } = useI18nLocale()
    const detectedLocale = 'ja' // Your detection logic here
    setLocale(detectedLocale)
  },
})
```

<Tip>
`setLocale()` 仅更新区域偏好（state + cookie）。若要同时在客户端重新加载活动翻译上下文，请使用来自 `useNuxtApp()` 或 `useI18n()` 的 `$switchLocale()`。这对 `no_prefix` 尤为重要，因为 URL 不会变化。参见 [FAQ — 切换区域时不修改 URL](/guides/faq#-switch-locale-without-changing-the-url)。
</Tip>

## 另请参见

- [自定义语言检测](/guides/custom-language-detection) — 配置自动检测
- [Strategy](/guides/routing-strategies) — 路由策略与区域优先级
- [方法 — useI18nLocale 部分](/api/methods#-usei18nlocale-composable) — 与运行时插件 API 的重叠
