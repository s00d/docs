---
title: "在元件中使用翻譯"
description: "使用 Nuxt I18n Micro 的根層與頁面專屬語系檔案，在 Vue 元件、樣板與 script 中操作 $t、$ts 等翻譯輔助方法。"
---

# 在元件中使用翻譯

模組安裝並設定完成後，你可以在 Vue 樣板與 script 中呼叫 `$t('your_key')` 來顯示翻譯。

## 1. 根層與頁面專屬翻譯檔

1. **根層**翻譯直接放在 `/locales` 資料夾下（例如 `/locales/en.json`、`/locales/fr.json`）。這些翻譯會在建置期自動合併到每個頁面專屬檔案中，因此在所有頁面上都可用。
2. **頁面專屬**翻譯放在 `/locales/pages/...` 下，對應你的 Nuxt pages 目錄結構：

```text
locales/
├── en.json
├── fr.json
└── pages/
    └── dir1/
        ├── en.json
        └── fr.json

```

## 2. 樣板中使用 `$t`

在 `.vue` 檔案中，你可以直接在樣板裡使用 `$t`：

```vue
<template>
  <div>
    <!-- Root-level translation (available on all pages) -->
    <p>{{ $t('test_key') }}</p>
    <!-- Displays: Hello World! -->

    <!-- Page-specific translation (if this page is /dir1) -->
    <p>{{ $t('local_key') }}</p>
    <!-- Displays: This is a page-specific translation for /dir1. -->

    <!-- Nested key -->
    <p>{{ $t('nested.key.deeper') }}</p>
    <!-- Displays: Nested translation here -->

    <!-- Placeholder usage -->
    <p>{{ $t('greeting', { name: 'Alice' }) }}</p>
    <!-- Displays: Hello, Alice! -->
  </div>
</template>

<script setup lang="ts">
// No need to import anything from 'nuxt-i18n-micro'.
// The plugin automatically provides `useI18n` and `$t`.
const { $t } = useI18n()

// You can now use $t in your script, for example:
console.log($t('test_key')) // "Hello World!"
</script>
```

<Tip>
若鍵指向巢狀 JSON 節點，`$t` 可能會回傳**物件**（例如 `$t('header')` → `\{ title: "Hi" \}`）。在樣板中會呈現為 `[object Object]`。若需要保證得到字串，請使用 `$ts`；或改用更精確的鍵，例如 `$t('header.title')`。詳見 [API 參考—$t 與 $ts](/api/methods#t)。
</Tip>

## 3. 切換目前語系

要在應用中變更語言，可以從 `useI18n()` 取得 `$locale` 並指派新的代碼：

```vue
<template>
  <div>
    <button @click="$switchLocale('en')">English</button>
    <button @click="$switchLocale('fr')">Français</button>
  </div>
</template>

<script setup lang="ts">
// The plugin auto-injects these references
const { $t, $switchLocale } = useI18n()
</script>
```

## 4. 巢狀鍵與 placeholder

- **巢狀鍵**使用點記法，例如 `nested.key.deeper`。
- **Placeholder** 讓你插入動態值：
  ```json
  { "greeting": "Hello, {name}!" }
  ```
  ```vue
  <p>{{ $t('greeting', { name: 'Alice' }) }}</p>
  <!-- Displays: Hello, Alice! -->
  ```

## 5. 提示與最佳實務

- **衝突的鍵**：若頁面專屬檔案中的鍵與根層檔案相同，頁面專屬版本會優先（根層翻譯在建置期作為基底合併）。
- **停用頁面檔案**：若你不想使用頁面專屬翻譯，可在 Nuxt 設定中將 `disablePageLocales: true`。這樣就只使用 `/locales/*.json` 檔案。
- **無需直接匯入**：你不需要 `import \{ useI18n \} from 'nuxt-i18n-micro'`，模組的自動注入功能已幫你處理。

以上，只要這些步驟你就能快速從根層或頁面專屬 JSON **取得翻譯**，並在 Nuxt 應用中透過 `$t` **顯示**它們。

## 其他資源

若要探索更多進階功能，請參閱：

1. **`<i18n-t>` 元件**
   `<i18n-t>` 元件是 `$t` 的強大替代方案，特別適合處理巢狀元素、進階 placeholder 用法與更複雜的翻譯結構。

- **文件**：[i18n-t](/api/components/i18n-t)

2. **`useI18n` composable**
   `useI18n` composable 提供完整的函式與響應式屬性，用於管理翻譯、語系切換、格式化等。

- **文件**：[useI18n](/api/composables/use-i18n)
