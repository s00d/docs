---
title: "Testowanie modułu `Nuxt I18n Micro`"
description: "Testuj zachowanie i18n w aplikacji Nuxt."
---

# 🧪 Testowanie modułu `Nuxt I18n Micro`

## 📖 Wprowadzenie do testowania

Testowanie modułu `Nuxt I18n Micro` jest kluczowe, aby zapewnić, że funkcje lokalizacji Twojej aplikacji działają zgodnie z oczekiwaniami. Ta dokumentacja poprowadzi Cię przez konfigurację środowiska testowego, tworzenie konfiguracji mock dla vitest oraz pisanie testów komponentów. Praktyczny przykład znajdziesz w [przykładowym projekcie na GitHubie](https://github.com/s00d/nuxt-i18n-micro/tree/main/packages/test-utils/example).

Zobacz [Testowanie strategii routingu](https://github.com/s00d/nuxt-i18n-micro/blob/main/docs/guide/testing-strategies.md), aby dowiedzieć się, jak to repozytorium uruchamia własne suity — projekty Vitest `unit` / `integration` / `e2e`, pliki per-strategia (`test/strategies-*.test.ts`) oraz regresje generowane.

## 🛠️ Konfiguracja środowiska testowego

### 1. Zainstaluj `@i18n-micro/test-utils`

Najpierw musisz zainstalować narzędzia testowe dla `Nuxt I18n Micro`. Ten pakiet dostarcza niezbędne narzędzia do mockowania i testowania Twoich konfiguracji i18n.

```bash
npm install @i18n-micro/test-utils --save-dev
```

### 2. Utwórz plik konfiguracji mock

Następnie utwórz plik do skonfigurowania mock konfiguracji i18n. Plik ten będzie używany do mockowania funkcji i18n i narzędzi podczas testów.

```typescript
// tests/unit-setup.ts

import { mockNuxtImport } from '@nuxt/test-utils/runtime'
import { vi } from 'vitest'
import { i18nUtils } from '@i18n-micro/test-utils'

export function createFakeI18n() {
  return {
    $getLocale: vi.fn(i18nUtils.getLocale),
    $t: vi.fn(i18nUtils.t),
    $tc: vi.fn(i18nUtils.tc),
    $setLocale: vi.fn(i18nUtils.setLocale),
    $getLocaleName: vi.fn(i18nUtils.getLocaleName),
    $setLocaleName: vi.fn(i18nUtils.setLocaleName),
    $getLocales: vi.fn(i18nUtils.getLocales),
    $setLocales: vi.fn(i18nUtils.setLocales),
    $defaultLocale: vi.fn(i18nUtils.defaultLocale),
    $setDefaultLocale: vi.fn(i18nUtils.setDefaultLocale),
    $getRouteName: vi.fn(i18nUtils.getRouteName),
    $settRouteName: vi.fn(i18nUtils.settRouteName),
    $ts: vi.fn(i18nUtils.ts),
    $tn: vi.fn(i18nUtils.tn),
    $td: vi.fn(i18nUtils.td),
    $has: vi.fn(i18nUtils.has),
    $resolveTranslations: vi.fn(i18nUtils.resolveTranslations),
    $setTranslation: vi.fn(i18nUtils.setTranslation),
    $mergeTranslations: vi.fn(i18nUtils.mergeTranslations),
    $switchLocaleRoute: vi.fn(i18nUtils.switchLocaleRoute),
    $switchLocalePath: vi.fn(i18nUtils.switchLocalePath),
    $switchLocale: vi.fn(i18nUtils.switchLocale),
    $switchRoute: vi.fn(i18nUtils.switchRoute),
    $localeRoute: vi.fn(i18nUtils.localeRoute),
    $localePath: vi.fn(i18nUtils.localePath),
    $setI18nRouteParams: vi.fn(i18nUtils.setI18nRouteParams),
  }
}

mockNuxtImport<() => ReturnType<typeof createFakeI18n>>('useI18n', () => vi.fn(() => createFakeI18n()))

export const setTranslationsFromJson = i18nUtils.setTranslationsFromJson
```

### 3. Skonfiguruj Vitest

Teraz skonfiguruj Vitest, aby używał pliku konfiguracji mock i ustawił środowisko testowe.

```typescript
// vitest.config.ts

import { defineVitestConfig } from '@nuxt/test-utils/config'

export default defineVitestConfig({
  test: {
    watch: false,
    setupFiles: ['./tests/unit-setup.ts'],
    include: ['./**/*.spec.ts'],
  },
})
```

## 🧪 Pisanie testów

### Przykładowy komponent

Oto przykład prostego komponentu, który używa kompozycji `useI18n` do tłumaczenia komunikatów.

```vue
<script setup lang="ts">
const { $t } = useI18n()

const props = defineProps({
  message: {
    type: String,
    default: null,
  },
})

// Test to see that $t works in script setup as well as the template
const message = props.message || $t('defaultMessage')
</script>

<template>
  <div>
    <p>Test from component: {{ $t('welcome') }}</p>
    <p data-testid="message">Test message from props: {{ message }}</p>
  </div>
</template>
```

### Plik testu

Napiszmy teraz test dla tego komponentu.

```typescript
// tests/unit/example.spec.ts

import { describe, it, expect, beforeEach } from 'vitest'
import { mount } from '@vue/test-utils'
import ExampleComponent from '@/components/ExampleComponent.vue'
import fs from 'fs'
import path from 'path'
import { setTranslationsFromJson } from './unit-setup'

describe('ExampleComponent', () => {
  beforeEach(async () => {
    const fileContent = fs.readFileSync(path.join(__dirname, '../../locales/en-GB.json')).toString()
    await setTranslationsFromJson('en', JSON.parse(fileContent))
  })

  it('renders the welcome message correctly', () => {
    const wrapper = mount(ExampleComponent)
    expect(wrapper.text()).toContain('Test from component: Welcome')
  })

  it('renders the default message correctly', () => {
    const wrapper = mount(ExampleComponent)
    expect(wrapper.find('[data-testid="message"]').text()).toContain('Test message from props: Default Message')
  })

  it('renders the custom message correctly', () => {
    const wrapper = mount(ExampleComponent, {
      props: {
        message: 'Custom Message',
      },
    })
    expect(wrapper.find('[data-testid="message"]').text()).toContain('Test message from props: Custom Message')
  })
})
```

## 📝 Dobre praktyki testowania

- **🔧 Mockuj funkcje i18n:** Zawsze mockuj funkcje i18n za pomocą `@i18n-micro/test-utils`, aby zapewnić spójne i przewidywalne wyniki testów.
- **⚙️ Używaj Vitest do testów jednostkowych:** Vitest to potężny framework testowy dla aplikacji Vue. Używaj go do pisania testów jednostkowych komponentów.
- **📚 Dokumentuj swoje testy:** Jasno dokumentuj cel i oczekiwane wyniki każdego testu. Pomoże to zachować przejrzystość i ułatwi innym (lub Tobie w przyszłości) zrozumienie testów.

## 📊 Metody narzędzi i18n

Poniżej tabela opisująca wszystkie metody narzędziowe dostarczane przez `@i18n-micro/test-utils`.

| Metoda                                          | Opis                                                                                                                                        |
| ----------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------ |
| `t(key, params, defaultValue)`                  | Tłumaczy klucz z opcjonalnymi parametrami i wartością domyślną.                                                                             |
| `tc(key, params, defaultValue)`                 | Tłumaczy klucz z obsługą liczby mnogiej.                                                                                                    |
| `setTranslationsFromJson(locale, translations)` | Ładuje tłumaczenia z obiektu JSON dla konkretnego locale.                                                                                    |
| `getLocale()`                                   | Zwraca bieżące locale.                                                                                                                       |
| `setLocale(val)`                                | Ustawia bieżące locale.                                                                                                                      |
| `getLocaleName()`                               | Zwraca nazwę bieżącego locale.                                                                                                               |
| `setLocaleName(val)`                            | Ustawia nazwę bieżącego locale.                                                                                                              |
| `getLocales()`                                  | Zwraca listę dostępnych locale.                                                                                                              |
| `setLocales(val)`                               | Ustawia listę dostępnych locale.                                                                                                             |
| `defaultLocale()`                               | Zwraca domyślne locale.                                                                                                                       |
| `setDefaultLocale(val)`                         | Ustawia domyślne locale.                                                                                                                      |
| `getRouteName()`                                | Zwraca nazwę bieżącej trasy.                                                                                                                  |
| `settRouteName(val)`                            | Ustawia nazwę bieżącej trasy.                                                                                                                 |
| `ts(key, params, defaultValue)`                 | Tłumaczy klucz i zwraca wynik jako string.                                                                                                    |
| `tn(value, options?)`                           | Formatuje liczbę z inline'owymi `Intl.NumberFormatOptions`. Nazwane formaty wymagają konfiguracji modułu i nie są dostępne w tym samodzielnym helperze. |
| `td(value, options?)`                           | Formatuje datę z inline'owymi `Intl.DateTimeFormatOptions`. Nazwane formaty wymagają konfiguracji modułu i nie są dostępne w tym samodzielnym helperze. |
| `has(key)`                                      | Sprawdza, czy klucz tłumaczenia istnieje.                                                                                                     |
| `resolveTranslations()`                             | Zwraca aktywne drzewo tłumaczeń dla bieżącego locale i trasy.                                                                                 |
| `setTranslation(key, value)`                    | Zastępuje wartość pod `key` w aktywnym słowniku (zastępuje, nie scala).                                                                       |
| `mergeTranslations(newTranslations)`              | Scala tłumaczenia w aktywne locale w runtime.                                                                                                 |

Postępując zgodnie z tymi krokami, możesz skutecznie testować moduł `Nuxt I18n Micro` i zapewnić, że funkcje lokalizacji Twojej aplikacji działają zgodnie z oczekiwaniami.
