---
title: "Komponent `<i18n-group>`"
description: "Grupuj tłumaczenia pod wspólnym prefiksem klucza."
---

# 🌍 Komponent `<i18n-group>`

Komponent `<i18n-group>` w Nuxt I18n Micro zapewnia wygodny sposób na grupowanie tłumaczeń pod wspólnym prefiksem, zmniejszając powtórzenia i poprawiając organizację kodu.

## ⚙️ Właściwości

{/* generated:component:i18n-group — do not edit; run `pnpm run docs:generate` */}

| Prop | Typ | Domyślnie | Opis |
| --- | --- | --- | --- |
| `prefix` **\*** | `string` | — | Prefiks klucza, względem którego rozstrzygane jest każde odczytanie wewnątrz slota. |
| `groupClass` | `string` | `''` | Dodatkowa klasa na elemencie wrappera. |

**\*** wymagany.

### Sloty

| Slot | Bindingi | Opis |
| --- | --- | --- |
| `default` | `prefix`, `t` | Otrzymuje `prefix` oraz funkcję `t` powiązaną z tym prefiksem. |

{/* /generated:component:i18n-group */}

## 🎯 Sloty

Domyślny slot otrzymuje `prefix` oraz funkcję `t` powiązaną z tym prefiksem, więc klucze wewnątrz zapisywane są względem prefiksu:

```vue
<i18n-group prefix="checkout">
  <template #default="{ t }">
    <h2>{{ t('title') }}</h2>
    <p>{{ t('summary', { count: 3 }) }}</p>
  </template>
</i18n-group>
```

## 🛠️ Przykład użycia

### Podstawowe użycie

```vue
<template>
  <i18n-group prefix="product.details">
    <template #default="{ t }">
      <h1>{{ t('title') }}</h1>
      <p>{{ t('description') }}</p>
      <div class="price">{{ t('price', { value: 99.99 }) }}</div>
    </template>
  </i18n-group>
</template>
```

Plik tłumaczeń:

```json
{
  "product": {
    "details": {
      "title": "Premium Product",
      "description": "This is a premium product",
      "price": "Price: ${value}"
    }
  }
}
```

### Z niestandardową klasą

```vue
<template>
  <i18n-group prefix="user.profile" group-class="profile-section">
    <template #default="{ t }">
      <div class="user-info">
        <h2>{{ t('title') }}</h2>
        <p>{{ t('bio') }}</p>
        <div class="stats">
          <span>{{ t('stats.followers') }}</span>
          <span>{{ t('stats.following') }}</span>
        </div>
      </div>
    </template>
  </i18n-group>
</template>
```

Plik tłumaczeń:

```json
{
  "user": {
    "profile": {
      "title": "User Profile",
      "bio": "User biography goes here",
      "stats": {
        "followers": "Followers: {count}",
        "following": "Following: {count}"
      }
    }
  }
}
```

### Z dynamiczną zawartością

```vue
<template>
  <i18n-group prefix="blog.post">
    <template #default="{ t }">
      <article>
        <h1>{{ t('title') }}</h1>
        <div class="meta">
          {{ t('meta.author', { name: author }) }} |
          {{ t('meta.date', { date: publishDate }) }}
        </div>
        <div v-for="(section, index) in sections" :key="index">
          <h2>{{ t(`sections.${index}.title`) }}</h2>
          <p>{{ t(`sections.${index}.content`) }}</p>
        </div>
      </article>
    </template>
  </i18n-group>
</template>

<script setup>
const author = 'John Doe'
const publishDate = '2024-01-01'
const sections = ['intro', 'main', 'conclusion']
</script>
```

## 🎨 Stylizacja

Komponent opakowuje zawartość w div z klasą `i18n-group` oraz dowolnymi dodatkowymi klasami wskazanymi we właściwości `groupClass`:

```css
.i18n-group {
  /* Your base styles */
}

.profile-section {
  /* Additional styles for profile sections */
}
```

## 🚀 Dobre praktyki

1. **Spójne prefiksy**: Używaj spójnych i logicznych prefiksów odzwierciedlających strukturę Twojej aplikacji.

```vue
<i18n-group prefix="features.pricing">
<i18n-group prefix="features.security">
```

2. **Modułowa organizacja**: Grupuj powiązane tłumaczenia razem pod znaczącymi prefiksami.

```vue
<i18n-group prefix="checkout.payment">
<i18n-group prefix="checkout.shipping">
```

3. **Reużywalne komponenty**: Twórz reużywalne komponenty z własnymi grupami tłumaczeń.

```vue
<!-- UserProfile.vue -->
<i18n-group prefix="user.profile">

<!-- UserSettings.vue -->
<i18n-group prefix="user.settings">
```

4. **Zagnieżdżone grupy**: Unikaj głębokiego zagnieżdżania grup, aby zachować przejrzystość.

```vue
<!-- Good -->
<i18n-group prefix="shop.product">

<!-- Avoid -->
<i18n-group prefix="shop.category.product.details.specs">
```
