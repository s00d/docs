---
title: "useI18nLocale-composable"
description: "Composablen useI18nLocale eksponerer sentralisert lokale-state, cookie-synkronisering og hooks for egendefinert lokale-gjenkjenning i Nuxt I18n Micro-prosjekter."
---

# useI18nLocale

## Signatur

{/* generated:symbol:useI18nLocale — do not edit; run `pnpm run docs:generate` */}

```ts
useI18nLocale(): { getEffectiveLocale: (route: unknown, getLocaleFromRoute: GetLocaleFromRoute) => string; getLocale: () => string | null; getLocaleWithServerFallback: (serverLocale?: string | null) => string | null; getPreferredLocale: () => string | null; hashCookie: any; isValidLocale: (locale: string | null | undefined) => locale is string; locale: any; localeCookie: any; resolveInitialLocale: (options: ResolveInitialLocaleOptions) => string; setLocale: (locale: string | null) => void; syncLocale: (locale: string | null) => void; validLocales: string[] }
```

Sentralisert inngangspunkt for i18n-lokale-håndtering.
Kombinerer useState('i18n-locale'), lokale-cookie og synkroniseringsverktøy.

{/* /generated:symbol:useI18nLocale */}

## Bruk

```vue
<script setup>
const {
  locale,
  localeCookie,
  hashCookie,
  validLocales,
  getPreferredLocale,
  getLocale,
  getLocaleWithServerFallback,
  getEffectiveLocale,
  resolveInitialLocale,
  setLocale,
  syncLocale,
  isValidLocale,
} = useI18nLocale()

// Set locale (updates state and cookies)
setLocale('de')

// Get current locale (state → cookie)
const current = getLocale()

// Get valid preferred locale
const preferred = getPreferredLocale()
</script>
```

## API

| Egenskap/metode                                 | Type                                | Beskrivelse                                                                          |
| ----------------------------------------------- | ----------------------------------- | ------------------------------------------------------------------------------------ |
| `locale`                                        | `Ref<string \| null>`               | Reaktiv lokale-tilstand (`useState('i18n-locale')`)                                  |
| `localeCookie`                                  | `Ref`                               | Lokale-cookie (når alternativet `localeCookie` er aktivert)                          |
| `hashCookie`                                    | `Ref`                               | Cookie brukt når `hashMode` er aktiv                                                 |
| `validLocales`                                  | `string[]`                          | Aktiverte lokale-koder fra modulkonfigurasjon (utelater `disabled`)                  |
| `getLocale()`                                   | `() => string \| null`              | Gjeldende lokale: state → cookie (eller hash-cookie i hash-modus)                    |
| `getPreferredLocale()`                          | `() => string \| null`              | Lokale fra state/cookie **validert** mot `validLocales`, eller `null`                |
| `getLocaleWithServerFallback(serverLocale?)`    | `(serverLocale?) => string \| null` | State → cookie → valgfri server-kontekst (brukes under `no_prefix`-initialisering)   |
| `getEffectiveLocale(route, getLocaleFromRoute)` | `(route, fn) => string`             | Lokale for lasting av oversettelser: hash-modus foretrekker state; ellers leses fra rute |
| `resolveInitialLocale(options)`                 | `(options) => string`               | Løser lokale: state → `serverLocale` → rute; synkroniserer state ved behov            |
| `setLocale(locale)`                             | `(locale: string \| null) => void`  | Sett lokale og synkroniser til cookies                                               |
| `syncLocale(locale)`                            | `(locale: string \| null) => void`  | Synkroniser bare cookies (ingen state-oppdatering)                                   |
| `isValidLocale(locale)`                         | `(locale) => boolean`               | Sjekk om strengen er i `validLocales`                                                |

### Alternativer for `resolveInitialLocale`

```typescript
interface ResolveInitialLocaleOptions {
  route: unknown
  serverLocale?: string | null
  getLocaleFromRoute: (route?: unknown) => string
}
```

Brukes internt av hoved-pluginen ved oppstart. Egendefinerte plugins kan kalle den når de implementerer alternative deteksjonsflyter.

### `getEffectiveLocale`

```typescript
const { getEffectiveLocale } = useI18nLocale()
const { $getLocale } = useNuxtApp()

const locale = getEffectiveLocale(route, (r) => $getLocale(r))
```

Returnerer lokalen som brukes til å laste oversettelser og formatere meldinger for et gitt rute-objekt.

## Programmatisk lokale-setting

Foretrekk `useI18nLocale()` fremfor direkte bruk av `useState('i18n-locale')`:

```ts
// plugins/i18n-loader.server.ts
import { defineNuxtPlugin } from '#imports'

export default defineNuxtPlugin({
  name: 'i18n-custom-loader',
  enforce: 'pre',
  order: -10,

  setup() {
    const { setLocale } = useI18nLocale()
    const detectedLocale = 'ja' // Your detection logic here
    setLocale(detectedLocale)
  },
})
```

<Tip>
`setLocale()` oppdaterer bare lokale-preferansen (state + cookie). For også å laste inn den aktive oversettelseskonteksten på klienten på nytt, bruk `$switchLocale()` fra `useNuxtApp()` eller `useI18n()`. Dette betyr spesielt for `no_prefix`, der URL-en ikke endres. Se [FAQ — bytt lokale uten å endre URL](/guides/faq#-switch-locale-without-changing-the-url).
</Tip>

## Se også

- [Egendefinert språkgjenkjenning](/guides/custom-language-detection) — konfigurering av autodeteksjon
- [Strategi](/guides/routing-strategies) — rutingsstrategier og lokale-prioritet
- [Metoder — useI18nLocale-seksjon](/api/methods#-usei18nlocale-composable) — overlapp med runtime-plugin-API
