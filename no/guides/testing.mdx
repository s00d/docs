---
title: "Testing av `Nuxt I18n Micro`-modulen"
description: "Test i18n-adferd i Nuxt-appen din."
---

# 🧪 Testing av `Nuxt I18n Micro`-modulen

## 📖 Introduksjon til testing

Testing av `Nuxt I18n Micro`-modulen er avgjørende for å sikre at applikasjonens lokaliseringsfunksjoner fungerer som forventet. Denne dokumentasjonen viser hvordan du setter opp testmiljøet, oppretter mock-konfigurasjoner for vitest og skriver tester for komponentene dine. For et praktisk eksempel kan du se [eksempelprosjektet på GitHub](https://github.com/s00d/nuxt-i18n-micro/tree/main/packages/test-utils/example).

Se [Testing routing strategies](https://github.com/s00d/nuxt-i18n-micro/blob/main/docs/guide/testing-strategies.md) for hvordan dette repositoriet kjører sine egne testsuiter — Vitest-prosjektene `unit` / `integration` / `e2e`, filene per strategi (`test/strategies-*.test.ts`) og generate-regresjonene.

## 🛠️ Sette opp testmiljøet

### 1. Installer `@i18n-micro/test-utils`

Først må du installere testverktøyene for `Nuxt I18n Micro`. Denne pakken gir de nødvendige verktøyene for å mocke og teste i18n-konfigurasjonene dine.

```bash
npm install @i18n-micro/test-utils --save-dev
```

### 2. Opprett en mock-konfigurasjonsfil

Deretter oppretter du en fil som setter opp mock-i18n-konfigurasjonen. Denne filen brukes til å mocke i18n-funksjonene og -verktøyene under testene.

```typescript
// tests/unit-setup.ts

import { mockNuxtImport } from '@nuxt/test-utils/runtime'
import { vi } from 'vitest'
import { i18nUtils } from '@i18n-micro/test-utils'

export function createFakeI18n() {
  return {
    $getLocale: vi.fn(i18nUtils.getLocale),
    $t: vi.fn(i18nUtils.t),
    $tc: vi.fn(i18nUtils.tc),
    $setLocale: vi.fn(i18nUtils.setLocale),
    $getLocaleName: vi.fn(i18nUtils.getLocaleName),
    $setLocaleName: vi.fn(i18nUtils.setLocaleName),
    $getLocales: vi.fn(i18nUtils.getLocales),
    $setLocales: vi.fn(i18nUtils.setLocales),
    $defaultLocale: vi.fn(i18nUtils.defaultLocale),
    $setDefaultLocale: vi.fn(i18nUtils.setDefaultLocale),
    $getRouteName: vi.fn(i18nUtils.getRouteName),
    $settRouteName: vi.fn(i18nUtils.settRouteName),
    $ts: vi.fn(i18nUtils.ts),
    $tn: vi.fn(i18nUtils.tn),
    $td: vi.fn(i18nUtils.td),
    $has: vi.fn(i18nUtils.has),
    $resolveTranslations: vi.fn(i18nUtils.resolveTranslations),
    $setTranslation: vi.fn(i18nUtils.setTranslation),
    $mergeTranslations: vi.fn(i18nUtils.mergeTranslations),
    $switchLocaleRoute: vi.fn(i18nUtils.switchLocaleRoute),
    $switchLocalePath: vi.fn(i18nUtils.switchLocalePath),
    $switchLocale: vi.fn(i18nUtils.switchLocale),
    $switchRoute: vi.fn(i18nUtils.switchRoute),
    $localeRoute: vi.fn(i18nUtils.localeRoute),
    $localePath: vi.fn(i18nUtils.localePath),
    $setI18nRouteParams: vi.fn(i18nUtils.setI18nRouteParams),
  }
}

mockNuxtImport<() => ReturnType<typeof createFakeI18n>>('useI18n', () => vi.fn(() => createFakeI18n()))

export const setTranslationsFromJson = i18nUtils.setTranslationsFromJson
```

### 3. Konfigurer Vitest

Konfigurer nå Vitest til å bruke mock-konfigurasjonsfilen og sett opp testmiljøet.

```typescript
// vitest.config.ts

import { defineVitestConfig } from '@nuxt/test-utils/config'

export default defineVitestConfig({
  test: {
    watch: false,
    setupFiles: ['./tests/unit-setup.ts'],
    include: ['./**/*.spec.ts'],
  },
})
```

## 🧪 Skrive tester

### Eksempelkomponent

Her er et eksempel på en enkel komponent som bruker `useI18n`-composablen til å oversette meldinger.

```vue
<script setup lang="ts">
const { $t } = useI18n()

const props = defineProps({
  message: {
    type: String,
    default: null,
  },
})

// Test to see that $t works in script setup as well as the template
const message = props.message || $t('defaultMessage')
</script>

<template>
  <div>
    <p>Test from component: {{ $t('welcome') }}</p>
    <p data-testid="message">Test message from props: {{ message }}</p>
  </div>
</template>
```

### Testfil

La oss nå skrive en test for denne komponenten.

```typescript
// tests/unit/example.spec.ts

import { describe, it, expect, beforeEach } from 'vitest'
import { mount } from '@vue/test-utils'
import ExampleComponent from '@/components/ExampleComponent.vue'
import fs from 'fs'
import path from 'path'
import { setTranslationsFromJson } from './unit-setup'

describe('ExampleComponent', () => {
  beforeEach(async () => {
    const fileContent = fs.readFileSync(path.join(__dirname, '../../locales/en-GB.json')).toString()
    await setTranslationsFromJson('en', JSON.parse(fileContent))
  })

  it('renders the welcome message correctly', () => {
    const wrapper = mount(ExampleComponent)
    expect(wrapper.text()).toContain('Test from component: Welcome')
  })

  it('renders the default message correctly', () => {
    const wrapper = mount(ExampleComponent)
    expect(wrapper.find('[data-testid="message"]').text()).toContain('Test message from props: Default Message')
  })

  it('renders the custom message correctly', () => {
    const wrapper = mount(ExampleComponent, {
      props: {
        message: 'Custom Message',
      },
    })
    expect(wrapper.find('[data-testid="message"]').text()).toContain('Test message from props: Custom Message')
  })
})
```

## 📝 Beste praksis for testing

- **🔧 Mock i18n-funksjonene:** Mock alltid i18n-funksjonene med `@i18n-micro/test-utils` for å sikre konsistente og forutsigbare testresultater.
- **⚙️ Bruk Vitest for enhetstester:** Vitest er et kraftig testrammeverk for Vue-applikasjoner. Bruk det til å skrive enhetstester for komponentene dine.
- **📚 Dokumenter testene dine:** Dokumenter tydelig formålet og forventede resultater for hver test. Dette bidrar til klarhet og gjør det enklere for andre (eller for deg selv senere) å forstå testene.

## 📊 i18n-verktøymetoder

Under er en tabell som beskriver alle verktøymetodene som tilbys av `@i18n-micro/test-utils`.

| Metode                                          | Beskrivelse                                                                                                                                |
| ----------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------ |
| `t(key, params, defaultValue)`                  | Oversetter en nøkkel med valgfrie parametere og en standardverdi.                                                                             |
| `tc(key, params, defaultValue)`                 | Oversetter en nøkkel med pluraliseringsstøtte.                                                                                               |
| `setTranslationsFromJson(locale, translations)` | Laster oversettelser fra et JSON-objekt for en bestemt locale.                                                                               |
| `getLocale()`                                   | Returnerer gjeldende locale.                                                                                                                |
| `setLocale(val)`                                | Setter gjeldende locale.                                                                                                                   |
| `getLocaleName()`                               | Returnerer gjeldende locale-navn.                                                                                                           |
| `setLocaleName(val)`                            | Setter gjeldende locale-navn.                                                                                                              |
| `getLocales()`                                  | Returnerer listen over tilgjengelige locales.                                                                                                     |
| `setLocales(val)`                               | Setter listen over tilgjengelige locales.                                                                                                        |
| `defaultLocale()`                               | Returnerer standard-locale.                                                                                                                |
| `setDefaultLocale(val)`                         | Setter standard-locale.                                                                                                                   |
| `getRouteName()`                                | Returnerer gjeldende rutenavn.                                                                                                            |
| `settRouteName(val)`                            | Setter gjeldende rutenavn.                                                                                                               |
| `ts(key, params, defaultValue)`                 | Oversetter en nøkkel og returnerer resultatet som en streng.                                                                                       |
| `tn(value, options?)`                           | Formaterer et tall med inline `Intl.NumberFormatOptions`. Navngitte formater krever modulkonfigurasjon og er ikke tilgjengelig i denne frittstående hjelperen. |
| `td(value, options?)`                           | Formaterer en dato med inline `Intl.DateTimeFormatOptions`. Navngitte formater krever modulkonfigurasjon og er ikke tilgjengelig i denne frittstående hjelperen. |
| `has(key)`                                      | Sjekker om en oversettelsesnøkkel finnes.                                                                                                        |
| `resolveTranslations()`                             | Returnerer det aktive oversettelsestreet for gjeldende locale og rute.                                                                      |
| `setTranslation(key, value)`                    | Erstatter verdien for `key` i den aktive ordboken (erstatt, ikke merge).                                                                 |
| `mergeTranslations(newTranslations)`              | Slår sammen oversettelser inn i den aktive locale-en ved kjøretid.                                                                                     |

Ved å følge disse trinnene kan du effektivt teste `Nuxt I18n Micro`-modulen og sikre at applikasjonens lokaliseringsfunksjoner fungerer som forventet.
