---
title: "`Nuxt I18n Micro` 모듈 테스트하기"
description: "Nuxt 앱에서 i18n 동작을 테스트합니다."
---

# 🧪 `Nuxt I18n Micro` 모듈 테스트하기

## 📖 테스트 소개

`Nuxt I18n Micro` 모듈을 테스트하는 것은 애플리케이션의 지역화 기능이 예상대로 작동하는지 확인하는 데 매우 중요합니다. 이 문서는 테스트 환경 설정, vitest용 모의 구성 생성, 컴포넌트 테스트 작성을 안내합니다. 실용적인 예제는 [GitHub의 예제 프로젝트](https://github.com/s00d/nuxt-i18n-micro/tree/main/packages/test-utils/example)를 참조할 수 있습니다.

이 저장소가 자체 테스트 스위트를 실행하는 방법 — `unit` / `integration` / `e2e` Vitest 프로젝트, 전략별 파일(`test/strategies-*.test.ts`), 생성 회귀 테스트 — 은 [Testing routing strategies](https://github.com/s00d/nuxt-i18n-micro/blob/main/docs/guide/testing-strategies.md)를 참조하세요.

## 🛠️ 테스트 환경 설정

### 1. `@i18n-micro/test-utils` 설치

먼저 `Nuxt I18n Micro`용 테스트 유틸리티를 설치해야 합니다. 이 패키지는 i18n 구성을 모의하고 테스트하는 데 필요한 도구를 제공합니다.

```bash
npm install @i18n-micro/test-utils --save-dev
```

### 2. 모의 구성 파일 만들기

다음으로 모의 i18n 구성을 설정하는 파일을 만듭니다. 이 파일은 테스트 중에 i18n 함수와 유틸리티를 모의하는 데 사용됩니다.

```typescript
// tests/unit-setup.ts

import { mockNuxtImport } from '@nuxt/test-utils/runtime'
import { vi } from 'vitest'
import { i18nUtils } from '@i18n-micro/test-utils'

export function createFakeI18n() {
  return {
    $getLocale: vi.fn(i18nUtils.getLocale),
    $t: vi.fn(i18nUtils.t),
    $tc: vi.fn(i18nUtils.tc),
    $setLocale: vi.fn(i18nUtils.setLocale),
    $getLocaleName: vi.fn(i18nUtils.getLocaleName),
    $setLocaleName: vi.fn(i18nUtils.setLocaleName),
    $getLocales: vi.fn(i18nUtils.getLocales),
    $setLocales: vi.fn(i18nUtils.setLocales),
    $defaultLocale: vi.fn(i18nUtils.defaultLocale),
    $setDefaultLocale: vi.fn(i18nUtils.setDefaultLocale),
    $getRouteName: vi.fn(i18nUtils.getRouteName),
    $settRouteName: vi.fn(i18nUtils.settRouteName),
    $ts: vi.fn(i18nUtils.ts),
    $tn: vi.fn(i18nUtils.tn),
    $td: vi.fn(i18nUtils.td),
    $has: vi.fn(i18nUtils.has),
    $resolveTranslations: vi.fn(i18nUtils.resolveTranslations),
    $setTranslation: vi.fn(i18nUtils.setTranslation),
    $mergeTranslations: vi.fn(i18nUtils.mergeTranslations),
    $switchLocaleRoute: vi.fn(i18nUtils.switchLocaleRoute),
    $switchLocalePath: vi.fn(i18nUtils.switchLocalePath),
    $switchLocale: vi.fn(i18nUtils.switchLocale),
    $switchRoute: vi.fn(i18nUtils.switchRoute),
    $localeRoute: vi.fn(i18nUtils.localeRoute),
    $localePath: vi.fn(i18nUtils.localePath),
    $setI18nRouteParams: vi.fn(i18nUtils.setI18nRouteParams),
  }
}

mockNuxtImport<() => ReturnType<typeof createFakeI18n>>('useI18n', () => vi.fn(() => createFakeI18n()))

export const setTranslationsFromJson = i18nUtils.setTranslationsFromJson
```

### 3. Vitest 구성

이제 모의 구성 파일을 사용하도록 Vitest를 구성하고 테스트 환경을 설정합니다.

```typescript
// vitest.config.ts

import { defineVitestConfig } from '@nuxt/test-utils/config'

export default defineVitestConfig({
  test: {
    watch: false,
    setupFiles: ['./tests/unit-setup.ts'],
    include: ['./**/*.spec.ts'],
  },
})
```

## 🧪 테스트 작성

### 예제 컴포넌트

메시지를 번역하기 위해 `useI18n` 컴포저블을 사용하는 간단한 컴포넌트 예제입니다.

```vue
<script setup lang="ts">
const { $t } = useI18n()

const props = defineProps({
  message: {
    type: String,
    default: null,
  },
})

// Test to see that $t works in script setup as well as the template
const message = props.message || $t('defaultMessage')
</script>

<template>
  <div>
    <p>Test from component: {{ $t('welcome') }}</p>
    <p data-testid="message">Test message from props: {{ message }}</p>
  </div>
</template>
```

### 테스트 파일

이제 이 컴포넌트에 대한 테스트를 작성해봅시다.

```typescript
// tests/unit/example.spec.ts

import { describe, it, expect, beforeEach } from 'vitest'
import { mount } from '@vue/test-utils'
import ExampleComponent from '@/components/ExampleComponent.vue'
import fs from 'fs'
import path from 'path'
import { setTranslationsFromJson } from './unit-setup'

describe('ExampleComponent', () => {
  beforeEach(async () => {
    const fileContent = fs.readFileSync(path.join(__dirname, '../../locales/en-GB.json')).toString()
    await setTranslationsFromJson('en', JSON.parse(fileContent))
  })

  it('renders the welcome message correctly', () => {
    const wrapper = mount(ExampleComponent)
    expect(wrapper.text()).toContain('Test from component: Welcome')
  })

  it('renders the default message correctly', () => {
    const wrapper = mount(ExampleComponent)
    expect(wrapper.find('[data-testid="message"]').text()).toContain('Test message from props: Default Message')
  })

  it('renders the custom message correctly', () => {
    const wrapper = mount(ExampleComponent, {
      props: {
        message: 'Custom Message',
      },
    })
    expect(wrapper.find('[data-testid="message"]').text()).toContain('Test message from props: Custom Message')
  })
})
```

## 📝 테스트 모범 사례

- **🔧 i18n 함수 모의화:** 일관되고 예측 가능한 테스트 결과를 보장하기 위해 항상 `@i18n-micro/test-utils`를 사용하여 i18n 함수를 모의합니다.
- **⚙️ 단위 테스트에 Vitest 사용:** Vitest는 Vue 애플리케이션을 위한 강력한 테스트 프레임워크입니다. 컴포넌트의 단위 테스트 작성에 사용하세요.
- **📚 테스트 문서화:** 각 테스트의 목적과 예상 결과를 명확하게 문서화하세요. 이는 명확성을 유지하고 다른 사람(또는 미래의 여러분)이 테스트를 더 쉽게 이해하는 데 도움이 됩니다.

## 📊 i18n 유틸리티 메서드

아래는 `@i18n-micro/test-utils`에서 제공하는 모든 유틸리티 메서드를 설명하는 표입니다.

| 메서드                                          | 설명                                                                                                                                |
| ----------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------ |
| `t(key, params, defaultValue)`                  | 선택적 매개변수와 기본값을 사용하여 키를 번역합니다.                                                                             |
| `tc(key, params, defaultValue)`                 | 복수형 지원과 함께 키를 번역합니다.                                                                                               |
| `setTranslationsFromJson(locale, translations)` | 특정 로케일에 대해 JSON 객체에서 번역을 로드합니다.                                                                               |
| `getLocale()`                                   | 현재 로케일을 반환합니다.                                                                                                                |
| `setLocale(val)`                                | 현재 로케일을 설정합니다.                                                                                                                   |
| `getLocaleName()`                               | 현재 로케일 이름을 반환합니다.                                                                                                           |
| `setLocaleName(val)`                            | 현재 로케일 이름을 설정합니다.                                                                                                              |
| `getLocales()`                                  | 사용 가능한 로케일 목록을 반환합니다.                                                                                                     |
| `setLocales(val)`                               | 사용 가능한 로케일 목록을 설정합니다.                                                                                                        |
| `defaultLocale()`                               | 기본 로케일을 반환합니다.                                                                                                                |
| `setDefaultLocale(val)`                         | 기본 로케일을 설정합니다.                                                                                                                   |
| `getRouteName()`                                | 현재 라우트 이름을 반환합니다.                                                                                                            |
| `settRouteName(val)`                            | 현재 라우트 이름을 설정합니다.                                                                                                               |
| `ts(key, params, defaultValue)`                 | 키를 번역하고 결과를 문자열로 반환합니다.                                                                                       |
| `tn(value, options?)`                           | 인라인 `Intl.NumberFormatOptions`로 숫자를 형식화합니다. 명명된 형식은 모듈 구성이 필요하며 이 독립 실행형 헬퍼에서는 사용할 수 없습니다. |
| `td(value, options?)`                           | 인라인 `Intl.DateTimeFormatOptions`로 날짜를 형식화합니다. 명명된 형식은 모듈 구성이 필요하며 이 독립 실행형 헬퍼에서는 사용할 수 없습니다. |
| `has(key)`                                      | 번역 키가 존재하는지 확인합니다.                                                                                                        |
| `resolveTranslations()`                             | 현재 로케일 및 라우트에 대한 활성 번역 트리를 반환합니다.                                                                      |
| `setTranslation(key, value)`                    | 활성 사전에서 `key`의 값을 교체합니다(병합이 아닌 교체).                                                                 |
| `mergeTranslations(newTranslations)`              | 런타임에 활성 로케일에 번역을 병합합니다.                                                                                     |

이러한 단계에 따라 `Nuxt I18n Micro` 모듈을 효과적으로 테스트하고, 애플리케이션의 지역화 기능이 예상대로 작동하는지 확인할 수 있습니다.
