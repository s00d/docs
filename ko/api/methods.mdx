---
title: "런타임 메서드 레퍼런스"
description: "Nuxt I18n Micro가 주입하는 모든 런타임 헬퍼에 대한 레퍼런스: $t, $localePath, $switchLocale, 캐시 유틸리티, 라우트 메서드."
---

# 🛠️ 메서드

이 페이지는 nuxt-i18n-micro가 제공하는 사용 가능한 모든 메서드를 문서화합니다. 탐색을 쉽게 하기 위해 메서드는 기능별로 정리되어 있습니다.

## 📊 API 개요

플러그인이 주입하는 모든 헬퍼는 `PluginsInjections` 인터페이스에서 읽어들이므로, 이 목록은 실제로 사용 가능한 것과 어긋날 수 없습니다.

{/* generated:methods-index — do not edit; run `pnpm run docs:generate` */}

| 헬퍼 | 시그니처 | 목적 |
| --- | --- | --- |
| [`$_t`](#_t) | `(route: RouteLocationNormalizedLoaded) => (key: string, params?: Params, defaultValue?: string \| null) => CleanTranslation` | 특정 라우트에 `$t`를 바인딩합니다. 현재 페이지 외부에서 번역할 때 사용합니다. 예를 들어 레이아웃이 다른 라우트로 이동하는 링크를 렌더링할 때 유용합니다. |
| [`$_ts`](#_ts) | `(route: RouteLocationNormalizedLoaded) => (key: string, params?: Params, defaultValue?: string \| null) => string` | 특정 라우트에 `$ts`를 바인딩합니다. |
| [`$defaultLocale`](#defaultlocale) | `() => string \| undefined` | 설정된 기본 로케일의 코드. |
| [`$getI18nConfig`](#geti18nconfig) | `() => ModuleOptionsExtend` | 런타임이 보는 대로 해결된 모듈 설정. |
| [`$getLocale`](#getlocale) | `(route?: RouteLocationNormalizedLoaded \| RouteLocationResolvedGeneric) => string` | 활성 로케일의 코드. |
| [`$getLocaleName`](#getlocalename) | `() => string \| null` | 설정에서 활성 로케일의 `displayName`. 없는 경우 `null`. |
| [`$getLocales`](#getlocales) | `() => Locale[]` | 메타데이터와 함께 설정된 모든 로케일. |
| [`$getRouteName`](#getroutename) | `(route?: RouteLocationNamedRaw \| RouteLocationResolvedGeneric, locale?: string) => string` | 로케일 접두사가 제거된 라우트 이름 — 번역이 키로 사용하는 이름. |
| [`$has`](#has) | `(key: string) => boolean` | 활성 로케일에서 키가 해결되는지 여부. |
| `$i18nStrategy` | `PathStrategy` | 경로와 로케일을 상호 해결하는 활성 라우팅 전략. |
| [`$loadPageTranslations`](#loadpagetranslations) | `(locale: string, routeName: string, translations: Translations) => Promise<void>` | 빌드 타임에 키를 알 수 없는 콘텐츠를 위해 런타임에 페이지 번역을 로드합니다. |
| [`$localePath`](#localepath) | `(to: RouteLocationNamedRaw \| RouteLocationResolvedGeneric \| string, locale?: string) => string` | 지정된 로케일 또는 활성 로케일로 경로를 해결합니다. |
| [`$localeRoute`](#localeroute) | `(to: RouteLocationNamedRaw \| RouteLocationResolvedGeneric \| string, locale?: string) => RouteLocationResolved` | 지정된 로케일 또는 활성 로케일로 라우트를 해결합니다. |
| [`$mergeTranslations`](#mergetranslations) | `(newTranslations: Translations) => void` | 런타임에 활성 로케일에 번역을 병합하여 로드된 내용을 재정의합니다. |
| [`$resolveTranslations`](#resolvetranslations) | `() => Translations` | 활성 로케일과 라우트에 대해 현재 메모리에 있는 모든 번역을 트리로 반환합니다. |
| [`$setI18nRouteParams`](#seti18nrouteparams) | `(value: I18nRouteParams) => I18nRouteParams` | 현재 라우트에 대한 로케일별 매개변수를 설정하여 동적 세그먼트가 언어별로 다를 수 있도록 합니다. |
| [`$setMissingHandler`](#setmissinghandler) | `(handler: MissingHandler \| null) => void` | 해결되지 않는 모든 키에 대해 호출되는 콜백을 설치합니다. |
| [`$setTranslation`](#settranslation) | `(key: string, value: unknown) => void` | 활성 사전에서 `key`의 값을 대체합니다. |
| [`$switchLocale`](#switchlocale) | `(locale: string) => void` | 현재 페이지를 다른 로케일로 이동합니다. |
| [`$switchLocalePath`](#switchlocalepath) | `(locale: string) => string` | 이동하지 않고 현재 페이지의 다른 로케일 경로. |
| [`$switchLocaleRoute`](#switchlocaleroute) | `(locale: string) => RouteLocationRaw` | 이동하지 않고 현재 페이지의 다른 로케일 라우트 객체. |
| [`$switchRoute`](#switchroute) | `(route: RouteLocationNamedRaw \| RouteLocationResolvedGeneric \| string, toLocale?: string) => void` | 활성 로케일을 유지하거나 `toLocale`로 전환하면서 다른 라우트로 이동합니다. |
| [`$t`](#t) | `(key: string, params?: Params, defaultValue?: string \| null) => CleanTranslation` | 키를 번역하고 `params`를 그 안에 보간합니다. |
| [`$tc`](#tc) | `(key: string, params: number \| Params, defaultValue?: string) => string` | 복수형을 사용하여 번역합니다. |
| [`$td`](#td) | `\{ (value: Date \| number \| string, options?: Intl.DateTimeFormatOptions): string (value: Date \| number \| string, key: string, overrides?: Intl.DateTimeFormatOptions): string (value: Date \| number \| string, key: string, locale: string, overrides?: Intl.DateTimeFormatOptions): string \}` | 활성 로케일에서 `Intl.DateTimeFormat`으로 날짜 형식을 지정합니다. |
| [`$tdr`](#tdr) | `(value: Date \| number \| string, options?: Intl.RelativeTimeFormatOptions) => string` | `Intl.RelativeTimeFormat`으로 날짜를 상대 시간("3일 전")으로 형식화합니다. |
| [`$tn`](#tn) | `\{ (value: number, options?: Intl.NumberFormatOptions): string (value: number, key: string, overrides?: Intl.NumberFormatOptions): string (value: number, key: string, locale: string, overrides?: Intl.NumberFormatOptions): string \}` | 활성 로케일에서 `Intl.NumberFormat`으로 숫자 형식을 지정합니다. |
| [`$ts`](#ts) | `(key: string, params?: Params, defaultValue?: string) => string` | `$t`와 같지만 항상 문자열을 반환합니다. 객체 또는 배열 값은 있는 그대로 반환되지 않고 문자열화됩니다. |

{/* /generated:methods-index */}

<Info>
`$defineI18nRoute`와 `$clearCache`는 메인 i18n 플러그인이 `useNuxtApp()`에서 제공하지만, `useI18n()` 컴포저블에서는 반환되지 **않으므로** 위 표에서 빠져 있습니다. (`$getI18nConfig`는 **두 곳 모두에서** 사용 가능합니다.) 아래 각 섹션을 참고하세요.
</Info>

### `$getI18nConfig`

{/* generated:method:$getI18nConfig — do not edit; run `pnpm run docs:generate` */}

```ts
() => ModuleOptionsExtend
```

런타임이 보는 대로 해결된 모듈 설정.

{/* /generated:method:$getI18nConfig */}

```typescript
const nuxtApp = useNuxtApp()
const { localeCookie, strategy, hooks } = nuxtApp.$getI18nConfig()
```

빌드 타임 또는 Nuxt 이외의 컨텍스트의 경우 대신 `#build/i18n.strategy.mjs`의 `getI18nConfig()`를 사용하세요.

<Info>
`serverTranslationPreload`는 서버 비공개 설정(`#i18n-internal/config`)에만 노출되는 비공개 모듈 옵션입니다. 공개 런타임 API의 일부가 아니며 예고 없이 변경될 수 있습니다.
</Info>

## 🌍 로케일 관리

로케일 정보를 가져오고 관리하기 위한 메서드.

### `$getLocale`

{/* generated:method:$getLocale — do not edit; run `pnpm run docs:generate` */}

```ts
(route?: RouteLocationNormalizedLoaded | RouteLocationResolvedGeneric) => string
```

활성 로케일의 코드. 라우트를 전달하면 해당 라우트가 속한 로케일을 읽어옵니다.

{/* /generated:method:$getLocale */}

```typescript
const locale = $getLocale()
// Output: 'en' (assuming the current locale is English)
```

### `$getLocaleName`

{/* generated:method:$getLocaleName — do not edit; run `pnpm run docs:generate` */}

```ts
() => string | null
```

설정에서 활성 로케일의 `displayName`. 없는 경우 `null`.

{/* /generated:method:$getLocaleName */}

```typescript
const locale = $getLocaleName()
// Output: 'English'
```

### `$getLocales`

{/* generated:method:$getLocales — do not edit; run `pnpm run docs:generate` */}

```ts
() => Locale[]
```

메타데이터와 함께 설정된 모든 로케일.

{/* /generated:method:$getLocales */}

```typescript
const locales = $getLocales()
// Output: [{ code: 'en', iso: 'en-US', dir: 'ltr' }, { code: 'fr', iso: 'fr-FR', dir: 'ltr' }]
```

### `$defaultLocale`

{/* generated:method:$defaultLocale — do not edit; run `pnpm run docs:generate` */}

```ts
() => string | undefined
```

설정된 기본 로케일의 코드.

{/* /generated:method:$defaultLocale */}

```typescript
const defaultLocale = $defaultLocale()
// Output: 'en'
```

## 🔍 번역 메서드

번역을 검색하고 관리하기 위한 핵심 메서드.

### `$t`

{/* generated:method:$t — do not edit; run `pnpm run docs:generate` */}

```ts
(key: string, params?: Params, defaultValue?: string | null) => CleanTranslation
```

키를 번역하고 `params`를 그 안에 보간합니다. 키가 누락된 경우 `defaultValue`를 반환하거나 기본값이 제공되지 않으면 키 자체를 반환합니다.

{/* /generated:method:$t */}

```typescript
const welcomeMessage = $t('welcome', { username: 'Alice', unreadCount: 5 })
// Output: "Welcome, Alice! You have 5 unread messages."
```

<Warning>
`$t`는 `CleanTranslation`을 반환합니다. 이는 `string | number | boolean | Translations | PluralTranslations | null`입니다. 키가 JSON에서 **중첩 객체**를 가리키는 경우(예: JSON에 `\{ "header": \{ "title": "Hi" \} \}`가 포함된 상태에서 `$t('header')`), 반환 값은 문자열이 아닌 그 **객체**가 됩니다. Vue 템플릿(<code v-pre>\{\{ $t('header') \}\}</code>)에서 직접 사용하면 `[object Object]`로 렌더링됩니다.

**이를 방지하는 방법:**

- 더 구체적인 키 사용: `$t('header.title')` → `"Hi"`
- 항상 문자열을 반환하는 `$ts()` 사용(문자열이 아닌 값에 `.toString()` 호출)
- 프로그래매틱 사용을 위해 하위 객체에 의도적으로 접근할 때만 중첩 키로 `$t` 사용
</Warning>

### `$ts`

{/* generated:method:$ts — do not edit; run `pnpm run docs:generate` */}

```ts
(key: string, params?: Params, defaultValue?: string) => string
```

`$t`와 같지만 항상 문자열을 반환합니다: 객체 또는 배열 값은 있는 그대로 반환되지 않고 문자열화됩니다.

{/* /generated:method:$ts */}

```typescript
const welcomeMessage = $ts('welcome', { username: 'Alice', unreadCount: 5 })
// Output: "Welcome, Alice! You have 5 unread messages."
```

### `$_t`와 `$_ts`

{/* generated:method:$_t — do not edit; run `pnpm run docs:generate` */}

```ts
(route: RouteLocationNormalizedLoaded) => (key: string, params?: Params, defaultValue?: string | null) => CleanTranslation
```

특정 라우트에 `$t`를 바인딩합니다. 현재 페이지 외부에서 번역할 때 사용합니다. 예를 들어 레이아웃이 다른 라우트로 이동하는 링크를 렌더링할 때 유용합니다.

{/* /generated:method:$_t */}

{/* generated:method:$_ts — do not edit; run `pnpm run docs:generate` */}

```ts
(route: RouteLocationNormalizedLoaded) => (key: string, params?: Params, defaultValue?: string | null) => string
```

특정 라우트에 `$ts`를 바인딩합니다. `$_t`를 참고하세요.

{/* /generated:method:$_ts */}

`$t`와 `$ts`의 라우트 바인딩 변형입니다. 이들은 먼저 **라우트**를 받고, 해당 라우트의 로케일과 페이지 컨텍스트에 잠긴 번역 함수를 반환합니다.

- **접근**: `useNuxtApp()` (`useI18n()`이 `$_t` / `$_ts`로 재내보내기도 함)

SSR이나 전환 중 활성 라우트가 `router.currentRoute`와 다를 때 사용합니다. 예: `<i18n-t>`, `<i18n-group>` 내부, 또는 특정 `route` 객체에 대한 콘텐츠를 렌더링할 때.

```typescript
import { useRoute, useNuxtApp } from '#imports'

const route = useRoute()
const { $_t, $_ts } = useNuxtApp()

const $t = $_t(route)
const title = $t('page.title')

// String-safe variant
const label = $_ts(route)('page.label')
```


### `$tc`

{/* generated:method:$tc — do not edit; run `pnpm run docs:generate` */}

```ts
(key: string, params: number | Params, defaultValue?: string) => string
```

복수형을 사용하여 번역합니다. `params`는 개수 자체이거나 `count`를 포함하는 객체일 수 있습니다.

{/* /generated:method:$tc */}

**번역 형식**: `|`로 구분된 폼. **각** 폼에 자리 표시자를 넣으세요:

```json
{
  "apples": "no apples | one apple | {count} apples",
  "cart": "no items for {name} | one item for {name} | {count} items for {name}"
}
```

```typescript
$tc('apples', 0) // "no apples"
$tc('apples', 1) // "one apple"
$tc('apples', 10) // "10 apples"

// count + other params (second argument must be an object)
$tc('cart', { count: 10, name: 'Alice' }) // "10 items for Alice"
```


**컴포넌트 대안** — `<i18n-t keypath="cart" :plural="count" :params="\{ name \}" />` (내부적으로 `count`를 `params`와 병합).


### `$mergeTranslations`

{/* generated:method:$mergeTranslations — do not edit; run `pnpm run docs:generate` */}

```ts
(newTranslations: Translations) => void
```

런타임에 활성 로케일에 번역을 병합하여 로드된 내용을 재정의합니다.

{/* /generated:method:$mergeTranslations */}

```typescript
$mergeTranslations({
  welcome: 'Bienvenue, {username}!',
})
// Output: Updates the translation cache with the new French translation
```

### `$resolveTranslations`

{/* generated:method:$resolveTranslations — do not edit; run `pnpm run docs:generate` */}

```ts
() => Translations
```

활성 로케일과 라우트에 대해 현재 메모리에 있는 모든 번역을 트리로 반환합니다.
`$t()`가 지금 해결할 수 있는 것의 읽기 전용 뷰.

{/* /generated:method:$resolveTranslations */}

```typescript
const tree = $resolveTranslations()
// { aaa: { bbb: 'ccc' }, ddd: 1111 } — the same dictionary $t() reads from
```

전체 메모리 내 사전이 필요할 때 사용합니다(DevTools, 테스트, 런타임 편집기). 반환 값을 읽기 전용으로 취급하세요. 값을 변경하려면 `$setTranslation`을 사용하거나 `$mergeTranslations`로 패치를 병합하세요.

### `$setTranslation`

{/* generated:method:$setTranslation — do not edit; run `pnpm run docs:generate` */}

```ts
(key: string, value: unknown) => void
```

활성 사전에서 `key`의 값을 대체합니다. 이는 병합이 아닌 대체입니다.
기존 형제 요소가 유지되어야 할 때는 `$mergeTranslations`를 사용하세요.

{/* /generated:method:$setTranslation */}

```typescript
$setTranslation('aaa', { fff: 'ggg' }) // replaces the whole `aaa` subtree
$setTranslation('aaa', 'text')          // replaces `aaa` with a string
$setTranslation('ddd', 1111)            // scalar at the top level
$setTranslation('aaa.bbb', 'nested')    // dotted path
```

이것은 병합이 아닌 **대체**입니다. 동일한 키 아래의 기존 형제 요소는 제거됩니다. 형제 요소를 잃지 않고 패치하려면 `$mergeTranslations`를 사용하세요.

### `$setMissingHandler`

{/* generated:method:$setMissingHandler — do not edit; run `pnpm run docs:generate` */}

```ts
(handler: MissingHandler | null) => void
```

해결되지 않는 모든 키에 대해 호출되는 콜백을 설치합니다. 제거하려면 `null`을 전달하세요.

{/* /generated:method:$setMissingHandler */}

**타입 정의**:

```typescript
type MissingHandler = (locale: string, key: string, routeName: string, instance?: unknown, type?: string) => void
```

```typescript
// Set a custom handler
$setMissingHandler((locale, key, routeName) => {
  console.error(`Missing translation: ${key} in ${locale} for route ${routeName}`)
  // Send to Sentry or other error tracking service
  // Sentry.captureMessage(`Missing translation: ${key}`)
})

// Remove the handler
$setMissingHandler(null)
```

**사용 사례**:

- 오류 추적 서비스(Sentry, LogRocket 등)에 누락된 번역 로깅
- 누락된 번역에 대한 분석 수집
- 누락된 번역 키에 대한 사용자 정의 오류 처리

## 🔢 숫자 및 날짜 형식화

로케일 규칙에 따라 숫자와 날짜를 형식화하기 위한 메서드.

### `$tn`

{/* generated:method:$tn — do not edit; run `pnpm run docs:generate` */}

```ts
{ (value: number, options?: Intl.NumberFormatOptions): string (value: number, key: string, overrides?: Intl.NumberFormatOptions): string (value: number, key: string, locale: string, overrides?: Intl.NumberFormatOptions): string }
```

활성 로케일에서 `Intl.NumberFormat`으로 숫자 형식을 지정합니다. 키를 사용하여 설정에서 명명된 형식을 선택합니다.

{/* /generated:method:$tn */}

```typescript
// Inline options
const formattedNumber = $tn(1234567.89, { style: 'currency', currency: 'USD' })
// "$1,234,567.89"

// Named format (from nuxt.config i18n.numberFormats)
const price = $tn(1000, 'currency')
const priceDe = $tn(1000, 'currency', 'de')
const compact = $tn(1000, 'currency', { notation: 'compact' })
```

알 수 없는 명명된 형식 키는 기본 `Intl.NumberFormat` 옵션으로 대체됩니다. 개발 중(클라이언트) `missingWarn`이 활성화되어 있으면(기본값) `console.warn`이 방출됩니다.

**설정 예제**:

```ts
export default defineNuxtConfig({
  i18n: {
    numberFormats: {
      en: {
        currency: { style: 'currency', currency: 'USD' },
        decimal: { style: 'decimal', minimumFractionDigits: 2, maximumFractionDigits: 2 },
      },
      de: {
        currency: { style: 'currency', currency: 'EUR' },
      },
    },
  },
})
```

**사용 사례**:

- 적절한 로케일 형식으로 숫자를 통화, 백분율 또는 소수로 형식화
- 앱 전체에서 명명된 형식 재사용 (Vue I18n 마이그레이션 호환성)

### `$td`

{/* generated:method:$td — do not edit; run `pnpm run docs:generate` */}

```ts
{ (value: Date | number | string, options?: Intl.DateTimeFormatOptions): string (value: Date | number | string, key: string, overrides?: Intl.DateTimeFormatOptions): string (value: Date | number | string, key: string, locale: string, overrides?: Intl.DateTimeFormatOptions): string }
```

활성 로케일에서 `Intl.DateTimeFormat`으로 날짜 형식을 지정합니다. 키를 사용하여 설정에서 명명된 형식을 선택합니다.

{/* /generated:method:$td */}

```typescript
const formattedDate = $td(new Date(), { weekday: 'long', year: 'numeric', month: 'long', day: 'numeric' })
// "Friday, September 1, 2023"

const short = $td(new Date(), 'short')
const longDe = $td(new Date(), 'long', 'de')
```

알 수 없는 명명된 형식 키는 기본 `Intl.DateTimeFormat` 옵션으로 대체됩니다. 개발 중(클라이언트) `missingWarn`이 활성화되어 있으면(기본값) `console.warn`이 방출됩니다.

**설정 예제**:

```ts
export default defineNuxtConfig({
  i18n: {
    datetimeFormats: {
      en: {
        short: { year: 'numeric', month: 'short', day: 'numeric' },
        long: { year: 'numeric', month: 'long', day: 'numeric', weekday: 'long' },
      },
    },
  },
})
```

**사용 사례**:

- 사용자 로케일에 맞는 형식으로 날짜 표시
- 짧은/긴/날짜-시간 사전 설정을 앱 전체에서 공유

### `$tdr`

{/* generated:method:$tdr — do not edit; run `pnpm run docs:generate` */}

```ts
(value: Date | number | string, options?: Intl.RelativeTimeFormatOptions) => string
```

`Intl.RelativeTimeFormat`으로 날짜를 상대 시간("3일 전")으로 형식화합니다.

{/* /generated:method:$tdr */}

```typescript
const relativeDate = $tdr(new Date(Date.now() - 1000 * 60 * 5))
// Output: "5 minutes ago" in the 'en-US' locale
```

## 🔄 라우트 및 로케일 전환

로케일과 라우트 간 전환을 위한 메서드.

### `$switchLocale`

{/* generated:method:$switchLocale — do not edit; run `pnpm run docs:generate` */}

```ts
(locale: string) => void
```

현재 페이지를 다른 로케일로 이동합니다.

{/* /generated:method:$switchLocale */}

```typescript
$switchLocale('fr')
// Prefix strategies: navigates to the French version of the route
// no_prefix: updates locale and translations without changing the URL path
```

<Tip>
[FAQ — URL을 변경하지 않고 로케일 전환](/guides/faq#-switch-locale-without-changing-the-url)을 참고하세요. 접두사 전략의 경우 링크 대상만 필요할 때 `$switchLocalePath()`를 사용하세요.
</Tip>

### `$switchLocaleRoute`

{/* generated:method:$switchLocaleRoute — do not edit; run `pnpm run docs:generate` */}

```ts
(locale: string) => RouteLocationRaw
```

이동하지 않고 현재 페이지의 다른 로케일 라우트 객체.

{/* /generated:method:$switchLocaleRoute */}

```typescript
// on /en/news
const routeFr = $switchLocaleRoute('fr')
// Output: A route object with the new locale applied, e.g., { name: 'localized-news', params: { locale: 'fr' } }
```

### `$switchLocalePath`

{/* generated:method:$switchLocalePath — do not edit; run `pnpm run docs:generate` */}

```ts
(locale: string) => string
```

이동하지 않고 현재 페이지의 다른 로케일 경로.

{/* /generated:method:$switchLocalePath */}

```typescript
// on /en/news
const routeFr = $switchLocalePath('fr')
window.location.href = routeFr
// Output: url with new locale applied, e.g., '/fr/nouvelles'
```

### `$switchRoute`

{/* generated:method:$switchRoute — do not edit; run `pnpm run docs:generate` */}

```ts
(route: RouteLocationNamedRaw | RouteLocationResolvedGeneric | string, toLocale?: string) => void
```

활성 로케일을 유지하거나 `toLocale`로 전환하면서 다른 라우트로 이동합니다.

{/* /generated:method:$switchRoute */}

**예제**:

<CodeGroup>
```typescript String Path
// Switches to the given path with the current locale
$switchRoute('/about')
```

```typescript String Path with Locale
// Switches to the given path with French locale
$switchRoute('/about', 'fr')
```

```typescript Named Route
// Switches to a named route with the current locale
$switchRoute({ name: 'page' })
```

```typescript Named Route with Locale
// Switches to a named route and changes the locale to Spanish
$switchRoute({ name: 'page' }, 'es')
```
</CodeGroup>

## 🌐 라우트 생성

지역화된 라우트와 경로를 생성하기 위한 메서드.

### `$localeRoute`

{/* generated:method:$localeRoute — do not edit; run `pnpm run docs:generate` */}

```ts
(to: RouteLocationNamedRaw | RouteLocationResolvedGeneric | string, locale?: string) => RouteLocationResolved
```

지정된 로케일 또는 활성 로케일로 라우트를 해결합니다.

{/* /generated:method:$localeRoute */}

```typescript
const localizedRoute = $localeRoute({ name: 'index' })
// Output: A route object with the current locale applied, e.g., { name: 'index', params: { locale: 'fr' } }
```

### `$localePath`

{/* generated:method:$localePath — do not edit; run `pnpm run docs:generate` */}

```ts
(to: RouteLocationNamedRaw | RouteLocationResolvedGeneric | string, locale?: string) => string
```

지정된 로케일 또는 활성 로케일로 경로를 해결합니다.

{/* /generated:method:$localePath */}

```typescript
const localizedPath = $localePath({ name: 'news' })
// Output: path with current (or specified) locale applied, e.g., '/en/nouvelles'
```

## 🔍 라우트 정보

라우트 정보와 이름을 가져오기 위한 메서드.

### `$getRouteName`

{/* generated:method:$getRouteName — do not edit; run `pnpm run docs:generate` */}

```ts
(route?: RouteLocationNamedRaw | RouteLocationResolvedGeneric, locale?: string) => string
```

로케일 접두사가 제거된 라우트 이름 — 번역이 키로 사용하는 이름.

{/* /generated:method:$getRouteName */}

```typescript
const routeName = $getRouteName(routeObject, 'fr')
// Output: 'index' (assuming the base route name is 'index')
```

## 🚦 라우트 설정

라우트 동작과 접근 제어를 설정하기 위한 메서드.

### `$defineI18nRoute`

{/* generated:method:$defineI18nRoute — do not edit; run `pnpm run docs:generate` */}

```ts
(routeDefinition: DefineI18nRouteConfig) => Promise<void>
```

컴포넌트 내부에서 페이지별 로케일 설정과 번역을 등록합니다.
활성 로케일에 병합되고 로케일이 변경되면 다시 적용됩니다.

{/* /generated:method:$defineI18nRoute */}

> [!IMPORTANT]
> `$defineI18nRoute`는 **define 플러그인**이 제공하며 `useNuxtApp()`에서만 사용할 수 있습니다. `useI18n()` 반환 객체의 일부가 **아닙니다**.
> `script setup` 내에서 항상 `useNuxtApp()`에서 구조 분해하세요. `$defineI18nRoute(...)`를 순수 전역으로 호출하면 SSR/prerender 중에 `"$defineI18nRoute is not defined"`가 발생합니다.

**매개변수**:

- **locales**: `string[] | Record<string, Record<string, string>>` — 라우트에 사용 가능한 로케일
- **localeRoutes**: `Record<string, string>` — 선택 사항. 특정 로케일에 대한 사용자 정의 라우트
- **disableMeta**: `boolean | string[]` — 선택 사항. 모든 또는 특정 로케일에 대해 i18n 메타 태그를 비활성화합니다.

**기본 예제**:

```typescript
import { useNuxtApp } from '#imports'

const { $defineI18nRoute } = useNuxtApp()

$defineI18nRoute({
  locales: ['en', 'fr', 'de'],
  localeRoutes: {
    en: '/welcome',
    fr: '/bienvenue',
    de: '/willkommen',
  },
  disableMeta: false,
})
```

> 📖 **자세한 사용 예제, 구성 형식, 모범 사례는 [컴포넌트별 번역 가이드](/guides/per-component-translations)를 참고하세요.**

### `$setI18nRouteParams`

{/* generated:method:$setI18nRouteParams — do not edit; run `pnpm run docs:generate` */}

```ts
(value: I18nRouteParams) => I18nRouteParams
```

현재 라우트에 대한 로케일별 매개변수를 설정하여 동적 세그먼트가 언어별로 다를 수 있도록 합니다.
head가 렌더링되기 전에 SSR 중에 호출하세요.

{/* /generated:method:$setI18nRouteParams */}

```typescript
// in pages/news/[id].vue
// for en/news/1-first-article
const { $switchLocaleRoute, $setI18nRouteParams, $defineI18nRoute } = useNuxtApp()
$defineI18nRoute({
  localeRoutes: {
    en: '/news/:id()',
    fr: '/nouvelles/:id()',
    de: '/Nachricht/:id()',
  },
})
const { data: news } = await useAsyncData(`news-${params.id}`, async () => {
  let response = await $fetch("/api/getNews", {
    query: {
      id: params.id,
    },
  });
  if (response?.localeSlugs) {
    response.localeSlugs = {
      en: {
        id: '1-first-article'
      }
      fr: {
        id: '1-premier-article'
      }
      de: {
        id: '1-erster-Artikel'
      }
    }
    $setI18nRouteParams(response?.localeSlugs);
  }
  return response;
});
$switchLocalePath('fr') // === 'fr/nouvelles/1-premier-article'
$switchLocalePath('de') // === 'de/Nachricht/1-erster-Artikel'
```

## 💻 사용 예제

### 기본 컴포넌트 사용

```vue
<template>
  <div>
    <p>{{ $t('key2.key2.key2.key2.key2') }}</p>
    <p>Current Locale: {{ $getLocale() }}</p>

    <div>
      {{ $t('welcome', { username: 'Alice', unreadCount: 5 }) }}
    </div>
    <div>
      {{ $tc('apples', 10) }}
    </div>

    <div>
      <button v-for="locale in $getLocales()" :key="locale.code" :disabled="locale.code === $getLocale()" @click="() => $switchLocale(locale.code)">
        Switch to {{ locale.code }}
      </button>
    </div>

    <div>
      <NuxtLink :to="$localeRoute({ name: 'index' })"> Go to Index </NuxtLink>
    </div>
  </div>
</template>

<script setup>
import { useI18n } from '#imports'

const { $getLocale, $switchLocale, $getLocales, $localeRoute, $t, $tc } = useI18n()
</script>
```

### useNuxtApp과 함께 사용

```typescript
import { useNuxtApp } from '#imports'

const { $getLocale, $switchLocale, $getLocales, $localeRoute, $t } = useNuxtApp()
```

### useI18n 컴포저블과 함께 사용

```typescript
import { useI18n } from '#imports'

const { $getLocale, $switchLocale, $getLocales, $localeRoute, $t } = useI18n()
// or
const i18n = useI18n()
```

## 🔧 캐시 및 유틸리티 메서드

### `$has`

{/* generated:method:$has — do not edit; run `pnpm run docs:generate` */}

```ts
(key: string) => boolean
```

활성 로케일에서 키가 해결되는지 여부. 원시 키를 렌더링하는 대신 선택적 카피에 대해 분기할 때 사용하세요.

{/* /generated:method:$has */}

동일한 로케일 페이지 전환 중, v3는 전환이 완료될 때까지 떠나는 페이지의 번역을 이 사전에 자동으로 깊게 병합합니다. 따라서 이전 페이지의 키가 잠시 `true`를 반환할 수 있습니다. `previousPageFallback` 옵션은 없습니다. 이 동작은 내장되어 있습니다. [FAQ — 페이지 전환](/guides/faq#-why-do-translations-break-during-page-transitions-especially-with-defineasynccomponent)을 참고하세요.

```typescript
if ($has('welcome')) {
  console.log($t('welcome'))
} else {
  console.log('Key not found')
}
```

### `$clearCache`

{/* generated:method:$clearCache — do not edit; run `pnpm run docs:generate` */}

```ts
() => void
```

클라이언트와 활성 사전의 모든 캐시된 청크를 삭제합니다.

{/* /generated:method:$clearCache */}

**접근**: `useNuxtApp().$clearCache`. `PluginsInjections`가 아닌 `NuxtAppOnlyInjections`에 선언되어 있으므로, 타입은 지정되지만 `useI18n()` 헬퍼 객체에서는 의도적으로 제외됩니다:

```typescript
const { $clearCache } = useNuxtApp()
// All cached translations are removed; next render will re-fetch them
$clearCache()
```

### `$loadPageTranslations`

{/* generated:method:$loadPageTranslations — do not edit; run `pnpm run docs:generate` */}

```ts
(locale: string, routeName: string, translations: Translations) => Promise<void>
```

빌드 타임에 키를 알 수 없는 콘텐츠를 위해 런타임에 페이지 번역을 로드합니다.

{/* /generated:method:$loadPageTranslations */}

```typescript
await $loadPageTranslations('en', 'about', {
  title: 'About Us',
  description: 'Learn more about our company',
})
```

## 🧭 `useI18nLocale` 컴포저블

로케일 상태 관리를 위한 중앙 집중식 컴포저블입니다. `useState('i18n-locale')`이나 `useCookie('user-locale')`을 직접 조작하는 대신 이것을 사용하세요.

```typescript
const {
  setLocale, // (locale: string) => void — updates state + cookie
  getLocale, // () => string | null — from state or cookie
  getPreferredLocale, // () => string | null — validated against locales list
  getEffectiveLocale, // (route, getLocaleFromRoute) => string
  resolveInitialLocale, // (options) => string
  isValidLocale, // (locale) => boolean
  locale, // Ref<string | null> — reactive state
  localeCookie, // CookieRef — reactive cookie
  syncLocale, // (locale) => void — sync to cookie only
  validLocales, // string[] — list of valid locale codes
} = useI18nLocale()
```

### 주요 메서드

| 메서드                    | 설명                                                          |
| ----------------------- | ----------------------------------------------------------- |
| `setLocale(locale)`     | `useState`와 쿠키 모두에서 원자적으로 로케일을 설정합니다           |
| `getLocale()`           | 상태 또는 쿠키에서 현재 로케일을 반환합니다                     |
| `getPreferredLocale()`  | `locales` 목록에 대해 검증된 로케일을 반환하거나 `null`을 반환합니다 |
| `isValidLocale(locale)` | 로케일 코드가 설정된 `locales` 목록에 있는지 확인합니다              |

### 사용자 정의 플러그인에서 사용

```typescript
// plugins/i18n-loader.server.ts
export default defineNuxtPlugin({
  name: 'i18n-custom-loader',
  enforce: 'pre',
  order: -10,
  setup() {
    const { setLocale } = useI18nLocale()
    // Detect locale from headers, domain, etc.
    setLocale('de')
  },
})
```

자세한 예제는 [사용자 정의 언어 감지](/guides/custom-language-detection)를 참고하세요.
