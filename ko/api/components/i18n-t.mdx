---
title: "`<i18n-t>` 컴포넌트"
description: "<i18n-t> 컴포넌트는 슬롯 기반 보간으로 번역을 렌더링하여 로케일 키에 Vue 컴포넌트와 리치 마크업을 주입할 수 있게 합니다."
---

# 🌍 `<i18n-t>` 컴포넌트

`Nuxt I18n Micro`의 `<i18n-t>` 컴포넌트는 슬롯을 통한 동적 콘텐츠 삽입을 지원하는 유연한 번역 컴포넌트입니다. 사용자 정의 Vue 컴포넌트나 HTML 콘텐츠로 번역을 보간할 수 있어 고급 지역화 시나리오를 지원합니다.

## ⚙️ Props

{/* generated:component:i18n-t — do not edit; run `pnpm run docs:generate` */}

| Prop | 유형 | 기본값 | 설명 |
| --- | --- | --- | --- |
| `keypath` **\*** | `string` | — | 렌더링할 번역 키. |
| `plural` | `number \| string` | — | 복수형을 선택하는 개수. `$t` 대신 `$tc`를 사용하도록 설정합니다. |
| `tag` | `string` | `'span'` | 렌더링할 요소. |
| `params` | `Record<string, string \| number \| boolean>` | `() => (\{\})` | 번역에 대체되는 보간 값. |
| `defaultValue` | `string` | `''` | `hideIfEmpty`가 빈 번역을 억제할 때 컴포넌트 대신 렌더링됩니다. |
| `html` | `boolean` | `false` | 번역을 텍스트가 아닌 HTML로 렌더링합니다. 제어 가능한 콘텐츠에만 사용하세요. |
| `hideIfEmpty` | `boolean` | `false` | 번역이 빈 문자열로 해결될 때 아무것도 렌더링하지 않습니다. |
| `customPluralRule` | `PluralFunc` | `null` | 설정된 것을 재정의하는 이 요소 전용 복수형 규칙. |
| `number` | `number \| string` | — | 값을 `$tn`으로 형식화하여 `\{number\}`로 번역에 전달합니다. |
| `date` | `Date \| string \| number` | — | 값을 `$td`로 형식화하여 `\{date\}`로 번역에 전달합니다. |
| `relativeDate` | `Date \| string \| number` | — | 값을 `$tdr`로 형식화하여 `\{relativeDate\}`로 번역에 전달합니다. |

**\*** 필수.

{/* /generated:component:i18n-t */}

`plural`, `number`, `date`, `relativeDate` 중 한 번에 하나만 적용됩니다. 여러 개가 설정되면 이 목록에서 첫 번째가 우선합니다.

### prop별 예제

```vue
<!-- keypath: the translation to render -->
<i18n-t keypath="feedback.text" />

<!-- plural: selects the plural form -->
<i18n-t keypath="items" :plural="10" />

<!-- params: interpolated into the translation -->
<i18n-t keypath="greeting" :params="{ name: 'Ada' }" />

<!-- tag: the element to render -->
<i18n-t keypath="feedback.text" tag="p" />

<!-- number / date / relativeDate: formatted, then interpolated -->
<i18n-t keypath="cart.total" :number="1234.5" />
<i18n-t keypath="post.published" :date="post.createdAt" />
<i18n-t keypath="post.ago" :relative-date="post.createdAt" />

<!-- hideIfEmpty with a fallback -->
<i18n-t keypath="promo.banner" hide-if-empty default-value="Nothing to show" />
```

```json
{
  "items": "Nothing|You have {count} item|You have {count} items"
}
```

## 🛠️ 사용 예제

### 기본 사용법

```vue
<i18n-t keypath="feedback.text" />
```

`feedback.text`의 번역을 `<span>` 태그 안에 렌더링합니다.

### 동적 콘텐츠를 위한 슬롯 사용

`<i18n-t>` 컴포넌트는 슬롯을 사용하여 번역의 특정 부분에 Vue 컴포넌트나 다른 콘텐츠를 동적으로 삽입할 수 있게 지원합니다.

```vue
<i18n-t keypath="feedback.text">
  <template #link>
    <nuxt-link :to="{ name: 'index' }">
      <i18n-t keypath="feedback.link" />
    </nuxt-link>
  </template>
</i18n-t>
```

이 예제에서 `feedback.text` 번역 문자열의 `\{link\}` 자리 표시자는 `<nuxt-link>` 컴포넌트로 대체되며, 그 안에 또 다른 번역 컴포넌트를 포함합니다.

### 복수형

```vue
<i18n-t keypath="items.count" :plural="itemCount" />
```

`itemCount` 값을 기반으로 복수형 규칙을 자동으로 적용합니다.

## 🚀 추가 기능

### 기본 슬롯

특정 슬롯이 제공되지 않는 경우, 번역된 전체 문자열을 제공하는 기본 슬롯을 통해 번역을 사용자 정의할 수 있습니다:

```vue
<i18n-t keypath="welcome.message">
  <template #default="{ translation }">
    {{ translation.replace('Nuxt', 'Vue') }}
  </template>
</i18n-t>
```

### 조건부 렌더링

`hideIfEmpty` prop을 사용하여 번역 문자열이 비어 있으면 아무것도 렌더링하지 않습니다.

```vue
<i18n-t keypath="optionalMessage" :hideIfEmpty="true"></i18n-t>
```

### 사용자 정의 복수형 규칙

사용자 정의 함수를 사용하여 복수형을 처리합니다.

```vue
<i18n-t
  keypath="items"
  :plural="itemCount"
  :customPluralRule="
    (key, count, params, locale, t) => {
      const raw = t(key, params)
      if (!raw) return null
      return count === 1 ? 'One item' : `${count} items`
    }
  "
/>
```

### 슬롯을 사용한 고급 예제

슬롯을 활용하여 번역 콘텐츠가 렌더링되는 방식을 사용자 정의합니다.

```vue
<i18n-t keypath="welcomeMessage">
  <template #default="{ translation }">
    <strong>{{ translation }}</strong>
  </template>
</i18n-t>
```
