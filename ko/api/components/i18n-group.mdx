---
title: "`<i18n-group>` 컴포넌트"
description: "<i18n-group> Vue 컴포넌트는 공유 키 접두사 아래 번역을 그룹화하여 중첩된 레이블과 반복되는 섹션이 템플릿에서 간결하게 유지되도록 합니다."
---

# 🌍 `<i18n-group>` 컴포넌트

Nuxt I18n Micro의 `<i18n-group>` 컴포넌트는 공통 접두사 아래 번역을 그룹화하여 반복을 줄이고 코드 조직을 개선하는 편리한 방법을 제공합니다.

## ⚙️ Props

{/* generated:component:i18n-group — do not edit; run `pnpm run docs:generate` */}

| Prop | 유형 | 기본값 | 설명 |
| --- | --- | --- | --- |
| `prefix` **\*** | `string` | — | 슬롯 내부의 모든 조회가 해결되는 키 접두사. |
| `groupClass` | `string` | `''` | 래퍼 요소에 추가되는 클래스. |

**\*** 필수.

### 슬롯

| 슬롯 | 바인딩 | 설명 |
| --- | --- | --- |
| `default` | `prefix`, `t` | `prefix`와 그 접두사로 범위가 지정된 `t` 함수를 받습니다. |

{/* /generated:component:i18n-group */}

## 🎯 슬롯

기본 슬롯은 `prefix`와 그 접두사로 범위가 지정된 `t` 함수를 받으므로, 내부의 키는 접두사에 상대적으로 작성됩니다:

```vue
<i18n-group prefix="checkout">
  <template #default="{ t }">
    <h2>{{ t('title') }}</h2>
    <p>{{ t('summary', { count: 3 }) }}</p>
  </template>
</i18n-group>
```

## 🛠️ 사용 예제

### 기본 사용법

```vue
<template>
  <i18n-group prefix="product.details">
    <template #default="{ t }">
      <h1>{{ t('title') }}</h1>
      <p>{{ t('description') }}</p>
      <div class="price">{{ t('price', { value: 99.99 }) }}</div>
    </template>
  </i18n-group>
</template>
```

번역 파일:

```json
{
  "product": {
    "details": {
      "title": "Premium Product",
      "description": "This is a premium product",
      "price": "Price: ${value}"
    }
  }
}
```

### 사용자 정의 클래스 사용

```vue
<template>
  <i18n-group prefix="user.profile" group-class="profile-section">
    <template #default="{ t }">
      <div class="user-info">
        <h2>{{ t('title') }}</h2>
        <p>{{ t('bio') }}</p>
        <div class="stats">
          <span>{{ t('stats.followers') }}</span>
          <span>{{ t('stats.following') }}</span>
        </div>
      </div>
    </template>
  </i18n-group>
</template>
```

번역 파일:

```json
{
  "user": {
    "profile": {
      "title": "User Profile",
      "bio": "User biography goes here",
      "stats": {
        "followers": "Followers: {count}",
        "following": "Following: {count}"
      }
    }
  }
}
```

### 동적 콘텐츠 사용

```vue
<template>
  <i18n-group prefix="blog.post">
    <template #default="{ t }">
      <article>
        <h1>{{ t('title') }}</h1>
        <div class="meta">
          {{ t('meta.author', { name: author }) }} |
          {{ t('meta.date', { date: publishDate }) }}
        </div>
        <div v-for="(section, index) in sections" :key="index">
          <h2>{{ t(`sections.${index}.title`) }}</h2>
          <p>{{ t(`sections.${index}.content`) }}</p>
        </div>
      </article>
    </template>
  </i18n-group>
</template>

<script setup>
const author = 'John Doe'
const publishDate = '2024-01-01'
const sections = ['intro', 'main', 'conclusion']
</script>
```

## 🎨 스타일링

이 컴포넌트는 콘텐츠를 `i18n-group` 클래스가 있는 div로 감싸며, `groupClass` prop에서 지정한 추가 클래스를 함께 적용합니다:

```css
.i18n-group {
  /* Your base styles */
}

.profile-section {
  /* Additional styles for profile sections */
}
```

## 🚀 모범 사례

1. **일관된 접두사**: 애플리케이션의 구조를 반영하는 일관되고 논리적인 접두사를 사용하세요.

```vue
<i18n-group prefix="features.pricing">
<i18n-group prefix="features.security">
```

2. **모듈식 조직**: 의미 있는 접두사 아래 관련 번역을 함께 그룹화하세요.

```vue
<i18n-group prefix="checkout.payment">
<i18n-group prefix="checkout.shipping">
```

3. **재사용 가능한 컴포넌트**: 자체 번역 그룹이 있는 재사용 가능한 컴포넌트를 생성하세요.

```vue
<!-- UserProfile.vue -->
<i18n-group prefix="user.profile">

<!-- UserSettings.vue -->
<i18n-group prefix="user.settings">
```

4. **중첩된 그룹**: 명료성을 유지하기 위해 그룹을 깊게 중첩하지 마세요.

```vue
<!-- Good -->
<i18n-group prefix="shop.product">

<!-- Avoid -->
<i18n-group prefix="shop.category.product.details.specs">
```
