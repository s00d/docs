---
title: "Installation"
description: "Installera Nuxt I18n Micro-modulen och dess valfria ekosystempaket."
---

## Krav

- **Node.js**: 18 eller nyare.
- **Nuxt**: 3.x eller 4.x.

Modulen stöder både Nuxt 3-projektlayouten (`pages/**/*.vue`) och Nuxt 4-layouten (`app/pages/**/*.vue`). Översättningar placeras enligt konvention i projektets rot (`locales/`), även när du använder Nuxt 4:s `app/`-katalog.

## Installera Nuxt-modulen

<CodeGroup>
```bash npm
npm install nuxt-i18n-micro
```

```bash pnpm
pnpm add nuxt-i18n-micro
```

```bash yarn
yarn add nuxt-i18n-micro
```

```bash bun
bun add nuxt-i18n-micro
```
</CodeGroup>

Registrera den i `nuxt.config.ts`:

```typescript
export default defineNuxtConfig({
  modules: ['nuxt-i18n-micro'],
  i18n: {
    locales: [
      { code: 'en', iso: 'en-US', dir: 'ltr' },
      { code: 'fr', iso: 'fr-FR', dir: 'ltr' },
    ],
    defaultLocale: 'en',
    translationDir: 'locales',
  },
})
```

Se [Snabbstarten](/quickstart) för en fullständig genomgång eller [Konfigurationsreferensen](/guides/configuration) för alla alternativ.

## Projektlayout

Som standard förväntar modulen sig:

```text
my-project/
├── locales/
│   ├── en.json
│   ├── fr.json
│   └── pages/
│       └── index/
│           ├── en.json
│           └── fr.json
├── pages/
│   └── index.vue
└── nuxt.config.ts
```

Rotnivåfiler slås samman i varje sidspecifik fil vid byggtid. Sidspecifika filer skapas automatiskt när de saknas under `nuxt dev`.

<Info>

</Info>

## Nuxt 4-layout

```text
my-project/
├── app/
│   └── pages/
│       └── index.vue
├── locales/
│   └── en.json
└── nuxt.config.ts
```

För att colocera översättningar under `app/`, sätt `translationDir: 'app/locales'`.

## Ekosystempaket

| Paket | Syfte |
| --- | --- |
| [`@i18n-micro/vue`](/integrations/vue) | Vue 3-integration (fristående, utan Nuxt). |
| [`@i18n-micro/react`](/integrations/react) | React-integration. |
| [`@i18n-micro/preact`](/integrations/preact) | Preact-integration. |
| [`@i18n-micro/solid`](/integrations/solid) | SolidJS-integration. |
| [`@i18n-micro/astro`](/integrations/astro) | Astro-integration. |
| [`@i18n-micro/node`](/integrations/nodejs) | Node.js-loader för serversidan. |
| [`@i18n-micro/types-generator`](/integrations/types-generator) | Generera TypeScript-typer från översättningsfiler. |
| [`@i18n-micro/devtools-ui`](/integrations/devtools-ui) | Ramverksoberoende DevTools-panel. |

Delade interna paket dras in transitivt.

## TypeScript

Modulen genererar ambient-typer för `useI18n`, `$t` och relaterade hjälpare baserat på dina standardspråkfiler. Detta är aktiverat som standard (`types: true`).

För starkare typning av översättningsnycklar, se [`@i18n-micro/types-generator`](/integrations/types-generator).
