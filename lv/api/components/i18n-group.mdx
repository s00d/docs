---
title: "`<i18n-group>` komponents"
description: "Sagrupē tulkojumus zem koplietota atslēgas priedēkļa."
---

# 🌍 `<i18n-group>` komponents

Komponents `<i18n-group>` modulī Nuxt I18n Micro nodrošina ērtu veidu, kā sagrupēt tulkojumus zem kopīga priedēkļa, samazinot atkārtošanos un uzlabojot koda organizāciju.

## ⚙️ Prop parametri

{/* generated:component:i18n-group — do not edit; run `pnpm run docs:generate` */}

| Prop | Tips | Noklusējums | Apraksts |
| --- | --- | --- | --- |
| `prefix` **\*** | `string` | — | Atslēgas priedēklis, pret kuru tiek atrisināta katra meklēšana slotā. |
| `groupClass` | `string` | `''` | Papildu klase uz ietinošā elementa. |

**\*** obligāti.

### Sloti

| Slots | Sasaistes | Apraksts |
| --- | --- | --- |
| `default` | `prefix`, `t` | Saņem `prefix` un `t` funkciju, kas ir tam mēroga. |

{/* /generated:component:i18n-group */}

## 🎯 Sloti

Noklusējuma slots saņem `prefix` un `t` funkciju, kas ir tam mēroga, tāpēc atslēgas iekšpusē tiek
rakstītas relatīvi priedēklim:

```vue
<i18n-group prefix="checkout">
  <template #default="{ t }">
    <h2>{{ t('title') }}</h2>
    <p>{{ t('summary', { count: 3 }) }}</p>
  </template>
</i18n-group>
```

## 🛠️ Lietošanas piemērs

### Pamata lietošana

```vue
<template>
  <i18n-group prefix="product.details">
    <template #default="{ t }">
      <h1>{{ t('title') }}</h1>
      <p>{{ t('description') }}</p>
      <div class="price">{{ t('price', { value: 99.99 }) }}</div>
    </template>
  </i18n-group>
</template>
```

Tulkošanas fails:

```json
{
  "product": {
    "details": {
      "title": "Premium Product",
      "description": "This is a premium product",
      "price": "Price: ${value}"
    }
  }
}
```

### Ar pielāgotu klasi

```vue
<template>
  <i18n-group prefix="user.profile" group-class="profile-section">
    <template #default="{ t }">
      <div class="user-info">
        <h2>{{ t('title') }}</h2>
        <p>{{ t('bio') }}</p>
        <div class="stats">
          <span>{{ t('stats.followers') }}</span>
          <span>{{ t('stats.following') }}</span>
        </div>
      </div>
    </template>
  </i18n-group>
</template>
```

Tulkošanas fails:

```json
{
  "user": {
    "profile": {
      "title": "User Profile",
      "bio": "User biography goes here",
      "stats": {
        "followers": "Followers: {count}",
        "following": "Following: {count}"
      }
    }
  }
}
```

### Ar dinamisku saturu

```vue
<template>
  <i18n-group prefix="blog.post">
    <template #default="{ t }">
      <article>
        <h1>{{ t('title') }}</h1>
        <div class="meta">
          {{ t('meta.author', { name: author }) }} |
          {{ t('meta.date', { date: publishDate }) }}
        </div>
        <div v-for="(section, index) in sections" :key="index">
          <h2>{{ t(`sections.${index}.title`) }}</h2>
          <p>{{ t(`sections.${index}.content`) }}</p>
        </div>
      </article>
    </template>
  </i18n-group>
</template>

<script setup>
const author = 'John Doe'
const publishDate = '2024-01-01'
const sections = ['intro', 'main', 'conclusion']
</script>
```

## 🎨 Stilēšana

Komponents ietin savu saturu div elementā ar klasi `i18n-group` un jebkurām papildu klasēm, kas norādītas `groupClass` propā:

```css
.i18n-group {
  /* Your base styles */
}

.profile-section {
  /* Additional styles for profile sections */
}
```

## 🚀 Labākā prakse

1. **Konsekventi priedēkļi**: izmantojiet konsekventus un loģiskus priedēkļus, kas atspoguļo jūsu lietotnes struktūru

```vue
<i18n-group prefix="features.pricing">
<i18n-group prefix="features.security">
```

2. **Moduļu organizācija**: sagrupējiet saistītos tulkojumus zem jēgpilniem priedēkļiem

```vue
<i18n-group prefix="checkout.payment">
<i18n-group prefix="checkout.shipping">
```

3. **Atkārtoti izmantojami komponenti**: izveidojiet atkārtoti izmantojamus komponentus ar savām tulkojumu grupām

```vue
<!-- UserProfile.vue -->
<i18n-group prefix="user.profile">

<!-- UserSettings.vue -->
<i18n-group prefix="user.settings">
```

4. **Ligzdotas grupas**: izvairieties no dziļas grupu ligzdošanas, lai saglabātu skaidrību

```vue
<!-- Good -->
<i18n-group prefix="shop.product">

<!-- Avoid -->
<i18n-group prefix="shop.category.product.details.specs">
```
