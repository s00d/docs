---
title: "Componenta `<i18n-t>`"
description: "Componenta <i18n-t> randează traducerile cu interpolare bazată pe sloturi, astfel încât să poți injecta componente Vue și markup îmbogățit în cheile de locale."
---

# 🌍 Componenta `<i18n-t>`

Componenta `<i18n-t>` din `Nuxt I18n Micro` este o componentă flexibilă de traducere care suportă inserarea dinamică de conținut prin sloturi. Îți permite să interpolezi traduceri cu componente Vue personalizate sau cu conținut HTML, activând scenarii avansate de localizare.

## ⚙️ Props

{/* generated:component:i18n-t — do not edit; run `pnpm run docs:generate` */}

| Prop | Tip | Implicit | Descriere |
| --- | --- | --- | --- |
| `keypath` **\*** | `string` | — | Cheia de traducere de randat. |
| `plural` | `number \| string` | — | Numărul care selectează forma de plural. Setează-l pentru a folosi `$tc` în loc de `$t`. |
| `tag` | `string` | `'span'` | Elementul de randat. |
| `params` | `Record<string, string \| number \| boolean>` | `() => (\{\})` | Valorile de interpolare substituite în traducere. |
| `defaultValue` | `string` | `''` | Randat în locul componentei atunci când `hideIfEmpty` suprimă o traducere goală. |
| `html` | `boolean` | `false` | Randează traducerea ca HTML în loc de text. Doar pentru conținutul pe care îl controlezi. |
| `hideIfEmpty` | `boolean` | `false` | Nu randează nimic atunci când traducerea se rezolvă la un string gol. |
| `customPluralRule` | `PluralFunc` | `null` | Regulă de plural doar pentru acest element, suprascriind pe cea configurată. |
| `number` | `number \| string` | — | Formatează valoarea cu `$tn` și o transmite traducerii ca `\{number\}`. |
| `date` | `Date \| string \| number` | — | Formatează valoarea cu `$td` și o transmite traducerii ca `\{date\}`. |
| `relativeDate` | `Date \| string \| number` | — | Formatează valoarea cu `$tdr` și o transmite traducerii ca `\{relativeDate\}`. |

**\*** obligatoriu.

{/* /generated:component:i18n-t */}

Doar unul dintre `plural`, `number`, `date` și `relativeDate` se aplică la un moment dat; când sunt setate mai multe, câștigă primul din acea listă.

### Exemple per prop

```vue
<!-- keypath: the translation to render -->
<i18n-t keypath="feedback.text" />

<!-- plural: selects the plural form -->
<i18n-t keypath="items" :plural="10" />

<!-- params: interpolated into the translation -->
<i18n-t keypath="greeting" :params="{ name: 'Ada' }" />

<!-- tag: the element to render -->
<i18n-t keypath="feedback.text" tag="p" />

<!-- number / date / relativeDate: formatted, then interpolated -->
<i18n-t keypath="cart.total" :number="1234.5" />
<i18n-t keypath="post.published" :date="post.createdAt" />
<i18n-t keypath="post.ago" :relative-date="post.createdAt" />

<!-- hideIfEmpty with a fallback -->
<i18n-t keypath="promo.banner" hide-if-empty default-value="Nothing to show" />
```

```json
{
  "items": "Nothing|You have {count} item|You have {count} items"
}
```

## 🛠️ Exemple de utilizare

### Utilizare de bază

```vue
<i18n-t keypath="feedback.text" />
```

Aceasta randează traducerea pentru `feedback.text` într-o etichetă `<span>`.

### Utilizarea sloturilor pentru conținut dinamic

Componenta `<i18n-t>` suportă utilizarea sloturilor pentru a insera dinamic componente Vue sau alt conținut în anumite părți ale traducerii.

```vue
<i18n-t keypath="feedback.text">
  <template #link>
    <nuxt-link :to="{ name: 'index' }">
      <i18n-t keypath="feedback.link" />
    </nuxt-link>
  </template>
</i18n-t>
```

În acest exemplu, placeholder-ul `\{link\}` din șirul de traducere `feedback.text` este înlocuit de componenta `<nuxt-link>`, care conține la rândul ei o altă componentă de traducere.

### Pluralizare

```vue
<i18n-t keypath="items.count" :plural="itemCount" />
```

Aceasta aplică automat regulile de pluralizare pe baza valorii `itemCount`.

## 🚀 Funcționalități suplimentare

### Slot implicit

Dacă nu este furnizat un slot specific, traducerea poate fi personalizată prin slotul implicit, care oferă întregul șir tradus:

```vue
<i18n-t keypath="welcome.message">
  <template #default="{ translation }">
    {{ translation.replace('Nuxt', 'Vue') }}
  </template>
</i18n-t>
```

### Randare condițională

Nu randa nimic dacă șirul de traducere este gol folosind prop-ul `hideIfEmpty`.

```vue
<i18n-t keypath="optionalMessage" :hideIfEmpty="true"></i18n-t>
```

### Regulă personalizată de pluralizare

Folosește o funcție personalizată pentru a gestiona pluralizarea.

```vue
<i18n-t
  keypath="items"
  :plural="itemCount"
  :customPluralRule="
    (key, count, params, locale, t) => {
      const raw = t(key, params)
      if (!raw) return null
      return count === 1 ? 'One item' : `${count} items`
    }
  "
/>
```

### Exemplu avansat cu sloturi

Utilizează sloturile pentru a personaliza modul de randare a conținutului de traducere.

```vue
<i18n-t keypath="welcomeMessage">
  <template #default="{ translation }">
    <strong>{{ translation }}</strong>
  </template>
</i18n-t>
```
