---
title: "`<i18n-group>` コンポーネント"
description: "<i18n-group> Vue コンポーネントは、共通のキープレフィックスの下に翻訳をグループ化します。テンプレート内でネストしたラベルや繰り返しのセクションを簡潔に保てます。"
---

# 🌍 `<i18n-group>` コンポーネント

Nuxt I18n Micro の `<i18n-group>` コンポーネントは、共通のプレフィックスの下に翻訳をグループ化する便利な方法を提供し、繰り返しを減らしてコード構成を改善します。

## ⚙️ Props

{/* generated:component:i18n-group — do not edit; run `pnpm run docs:generate` */}

| Prop | 型 | デフォルト | 説明 |
| --- | --- | --- | --- |
| `prefix` **\*** | `string` | — | スロット内のすべてのルックアップで基準となるキープレフィックス。 |
| `groupClass` | `string` | `''` | ラッパー要素に追加するクラス。 |

**\*** 必須。

### スロット

| スロット | バインディング | 説明 |
| --- | --- | --- |
| `default` | `prefix`, `t` | `prefix` と、それにスコープされた `t` 関数を受け取ります。 |

{/* /generated:component:i18n-group */}

## 🎯 スロット

デフォルトスロットは `prefix` とそれにスコープされた `t` 関数を受け取るため、スロット内のキーはプレフィックスからの相対で記述できます。

```vue
<i18n-group prefix="checkout">
  <template #default="{ t }">
    <h2>{{ t('title') }}</h2>
    <p>{{ t('summary', { count: 3 }) }}</p>
  </template>
</i18n-group>
```

## 🛠️ 使用例

### 基本的な使い方

```vue
<template>
  <i18n-group prefix="product.details">
    <template #default="{ t }">
      <h1>{{ t('title') }}</h1>
      <p>{{ t('description') }}</p>
      <div class="price">{{ t('price', { value: 99.99 }) }}</div>
    </template>
  </i18n-group>
</template>
```

翻訳ファイル:

```json
{
  "product": {
    "details": {
      "title": "Premium Product",
      "description": "This is a premium product",
      "price": "Price: ${value}"
    }
  }
}
```

### カスタムクラス付き

```vue
<template>
  <i18n-group prefix="user.profile" group-class="profile-section">
    <template #default="{ t }">
      <div class="user-info">
        <h2>{{ t('title') }}</h2>
        <p>{{ t('bio') }}</p>
        <div class="stats">
          <span>{{ t('stats.followers') }}</span>
          <span>{{ t('stats.following') }}</span>
        </div>
      </div>
    </template>
  </i18n-group>
</template>
```

翻訳ファイル:

```json
{
  "user": {
    "profile": {
      "title": "User Profile",
      "bio": "User biography goes here",
      "stats": {
        "followers": "Followers: {count}",
        "following": "Following: {count}"
      }
    }
  }
}
```

### 動的コンテンツ付き

```vue
<template>
  <i18n-group prefix="blog.post">
    <template #default="{ t }">
      <article>
        <h1>{{ t('title') }}</h1>
        <div class="meta">
          {{ t('meta.author', { name: author }) }} |
          {{ t('meta.date', { date: publishDate }) }}
        </div>
        <div v-for="(section, index) in sections" :key="index">
          <h2>{{ t(`sections.${index}.title`) }}</h2>
          <p>{{ t(`sections.${index}.content`) }}</p>
        </div>
      </article>
    </template>
  </i18n-group>
</template>

<script setup>
const author = 'John Doe'
const publishDate = '2024-01-01'
const sections = ['intro', 'main', 'conclusion']
</script>
```

## 🎨 スタイリング

このコンポーネントはコンテンツを `i18n-group` クラスの div でラップし、`groupClass` prop で指定した追加クラスも付与します。

```css
.i18n-group {
  /* 基本スタイル */
}

.profile-section {
  /* プロフィールセクション用の追加スタイル */
}
```

## 🚀 ベストプラクティス

1. **一貫したプレフィックス**: アプリケーションの構造を反映した、一貫性のある論理的なプレフィックスを使用します。

```vue
<i18n-group prefix="features.pricing">
<i18n-group prefix="features.security">
```

2. **モジュラーな整理**: 関連する翻訳を意味のあるプレフィックスの下にまとめます。

```vue
<i18n-group prefix="checkout.payment">
<i18n-group prefix="checkout.shipping">
```

3. **再利用可能なコンポーネント**: 独自の翻訳グループを持つ再利用可能なコンポーネントを作成します。

```vue
<!-- UserProfile.vue -->
<i18n-group prefix="user.profile">

<!-- UserSettings.vue -->
<i18n-group prefix="user.settings">
```

4. **ネストされたグループ**: 明確さを保つため、グループを深くネストしすぎないようにします。

```vue
<!-- 良い -->
<i18n-group prefix="shop.product">

<!-- 避ける -->
<i18n-group prefix="shop.category.product.details.specs">
```
