---
title: "`<i18n-t>` कंपोनेंट"
description: "<i18n-t> कंपोनेंट स्लॉट-आधारित इंटरपोलेशन के साथ ट्रांसलेशन रेंडर करता है, ताकि आप लोकेल कुंजियों में Vue कंपोनेंट्स और समृद्ध मार्कअप इंजेक्ट कर सकें।"
---

# 🌍 `<i18n-t>` कंपोनेंट

`Nuxt I18n Micro` में `<i18n-t>` कंपोनेंट एक लचीला ट्रांसलेशन कंपोनेंट है जो स्लॉट्स के माध्यम से डायनामिक कंटेंट इंसर्शन का समर्थन करता है। यह आपको कस्टम Vue कंपोनेंट्स या HTML कंटेंट के साथ ट्रांसलेशन में इंटरपोलेट करने की अनुमति देता है, जिससे उन्नत लोकलाइज़ेशन परिदृश्य संभव होते हैं।

## ⚙️ Props

{/* generated:component:i18n-t — do not edit; run `pnpm run docs:generate` */}

| Prop | प्रकार | डिफ़ॉल्ट | विवरण |
| --- | --- | --- | --- |
| `keypath` **\*** | `string` | — | रेंडर करने के लिए ट्रांसलेशन कुंजी। |
| `plural` | `number \| string` | — | प्लूरल फ़ॉर्म चुनने वाली गणना। `$t` के बजाय `$tc` का उपयोग करने के लिए इसे सेट करें। |
| `tag` | `string` | `'span'` | रेंडर करने के लिए एलिमेंट। |
| `params` | `Record<string, string \| number \| boolean>` | `() => (\{\})` | ट्रांसलेशन में प्रतिस्थापित इंटरपोलेशन मान। |
| `defaultValue` | `string` | `''` | जब `hideIfEmpty` एक खाली ट्रांसलेशन को दबाता है, तो कंपोनेंट के बजाय रेंडर किया जाता है। |
| `html` | `boolean` | `false` | ट्रांसलेशन को टेक्स्ट के बजाय HTML के रूप में रेंडर करें। केवल उस कंटेंट के लिए जिसे आप नियंत्रित करते हैं। |
| `hideIfEmpty` | `boolean` | `false` | जब ट्रांसलेशन खाली स्ट्रिंग में हल होता है तो कुछ भी रेंडर नहीं करें। |
| `customPluralRule` | `PluralFunc` | `null` | केवल इस एलिमेंट के लिए प्लूरल नियम, कॉन्फ़िगर किए गए को ओवरराइड करता है। |
| `number` | `number \| string` | — | मान को `$tn` के साथ फ़ॉर्मेट करता है और ट्रांसलेशन को `\{number\}` के रूप में पास करता है। |
| `date` | `Date \| string \| number` | — | मान को `$td` के साथ फ़ॉर्मेट करता है और ट्रांसलेशन को `\{date\}` के रूप में पास करता है। |
| `relativeDate` | `Date \| string \| number` | — | मान को `$tdr` के साथ फ़ॉर्मेट करता है और ट्रांसलेशन को `\{relativeDate\}` के रूप में पास करता है। |

**\*** आवश्यक।

{/* /generated:component:i18n-t */}

एक समय में `plural`, `number`, `date` और `relativeDate` में से केवल एक ही लागू होता है; जब कई
सेट होते हैं, तो उस सूची में पहला जीतता है।

### प्रति prop उदाहरण

```vue
<!-- keypath: the translation to render -->
<i18n-t keypath="feedback.text" />

<!-- plural: selects the plural form -->
<i18n-t keypath="items" :plural="10" />

<!-- params: interpolated into the translation -->
<i18n-t keypath="greeting" :params="{ name: 'Ada' }" />

<!-- tag: the element to render -->
<i18n-t keypath="feedback.text" tag="p" />

<!-- number / date / relativeDate: formatted, then interpolated -->
<i18n-t keypath="cart.total" :number="1234.5" />
<i18n-t keypath="post.published" :date="post.createdAt" />
<i18n-t keypath="post.ago" :relative-date="post.createdAt" />

<!-- hideIfEmpty with a fallback -->
<i18n-t keypath="promo.banner" hide-if-empty default-value="Nothing to show" />
```

```json
{
  "items": "Nothing|You have {count} item|You have {count} items"
}
```

## 🛠️ उदाहरण उपयोग

### मूल उपयोग

```vue
<i18n-t keypath="feedback.text" />
```

यह `feedback.text` के लिए ट्रांसलेशन को `<span>` टैग के भीतर रेंडर करता है।

### डायनामिक कंटेंट के लिए स्लॉट्स का उपयोग

`<i18n-t>` कंपोनेंट स्लॉट्स के उपयोग का समर्थन करता है ताकि ट्रांसलेशन के विशिष्ट हिस्सों में Vue कंपोनेंट्स या अन्य कंटेंट को गतिशील रूप से डाला जा सके।

```vue
<i18n-t keypath="feedback.text">
  <template #link>
    <nuxt-link :to="{ name: 'index' }">
      <i18n-t keypath="feedback.link" />
    </nuxt-link>
  </template>
</i18n-t>
```

इस उदाहरण में, `feedback.text` ट्रांसलेशन स्ट्रिंग में `\{link\}` प्लेसहोल्डर को `<nuxt-link>` कंपोनेंट द्वारा प्रतिस्थापित किया जाता है, जिसमें स्वयं एक और ट्रांसलेशन कंपोनेंट होता है।

### प्लूरलाइज़ेशन

```vue
<i18n-t keypath="items.count" :plural="itemCount" />
```

यह `itemCount` मान के आधार पर स्वचालित रूप से प्लूरलाइज़ेशन नियम लागू करता है।

## 🚀 अतिरिक्त विशेषताएँ

### डिफ़ॉल्ट स्लॉट

यदि कोई विशिष्ट स्लॉट प्रदान नहीं किया गया है, तो ट्रांसलेशन को डिफ़ॉल्ट स्लॉट के माध्यम से कस्टमाइज़ किया जा सकता है, जो पूरी अनुवादित स्ट्रिंग प्रदान करता है:

```vue
<i18n-t keypath="welcome.message">
  <template #default="{ translation }">
    {{ translation.replace('Nuxt', 'Vue') }}
  </template>
</i18n-t>
```

### सशर्त रेंडरिंग

यदि ट्रांसलेशन स्ट्रिंग खाली है तो `hideIfEmpty` prop का उपयोग करके कुछ भी रेंडर न करें।

```vue
<i18n-t keypath="optionalMessage" :hideIfEmpty="true"></i18n-t>
```

### कस्टम प्लूरलाइज़ेशन नियम

प्लूरलाइज़ेशन को हैंडल करने के लिए एक कस्टम फ़ंक्शन का उपयोग करें।

```vue
<i18n-t
  keypath="items"
  :plural="itemCount"
  :customPluralRule="
    (key, count, params, locale, t) => {
      const raw = t(key, params)
      if (!raw) return null
      return count === 1 ? 'One item' : `${count} items`
    }
  "
/>
```

### स्लॉट्स के साथ उन्नत उदाहरण

ट्रांसलेशन कंटेंट के रेंडर होने के तरीके को अनुकूलित करने के लिए स्लॉट्स का उपयोग करें।

```vue
<i18n-t keypath="welcomeMessage">
  <template #default="{ translation }">
    <strong>{{ translation }}</strong>
  </template>
</i18n-t>
```
