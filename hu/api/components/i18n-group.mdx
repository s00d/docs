---
title: "`<i18n-group>` komponens"
description: "Fordítások csoportosítása közös kulcselőtag alatt."
---

# 🌍 `<i18n-group>` komponens

A `Nuxt I18n Micro` `<i18n-group>` komponense kényelmes módot nyújt arra, hogy közös előtag alatt csoportosítsd a fordításokat, csökkentve az ismétlést és javítva a kód szervezettségét.

## ⚙️ Propok

{/* generated:component:i18n-group — do not edit; run `pnpm run docs:generate` */}

| Prop | Típus | Alapérték | Leírás |
| --- | --- | --- | --- |
| `prefix` **\*** | `string` | — | Kulcselőtag, amelyhez a sloton belüli minden keresés viszonyul. |
| `groupClass` | `string` | `''` | Extra osztály a csomagoló elemen. |

**\*** kötelező.

### Slotok

| Slot | Kötések | Leírás |
| --- | --- | --- |
| `default` | `prefix`, `t` | Megkapja a `prefix`-et és egy hozzá szabott `t` függvényt. |

{/* /generated:component:i18n-group */}

## 🎯 Slotok

Az alapértelmezett slot megkapja a `prefix`-et és egy hozzá szabott `t` függvényt, így a benne lévő kulcsok
az előtaghoz képest relatívan írhatók:

```vue
<i18n-group prefix="checkout">
  <template #default="{ t }">
    <h2>{{ t('title') }}</h2>
    <p>{{ t('summary', { count: 3 }) }}</p>
  </template>
</i18n-group>
```

## 🛠️ Példa a használatra

### Alapvető használat

```vue
<template>
  <i18n-group prefix="product.details">
    <template #default="{ t }">
      <h1>{{ t('title') }}</h1>
      <p>{{ t('description') }}</p>
      <div class="price">{{ t('price', { value: 99.99 }) }}</div>
    </template>
  </i18n-group>
</template>
```

Fordítási fájl:

```json
{
  "product": {
    "details": {
      "title": "Premium Product",
      "description": "This is a premium product",
      "price": "Price: ${value}"
    }
  }
}
```

### Egyedi osztállyal

```vue
<template>
  <i18n-group prefix="user.profile" group-class="profile-section">
    <template #default="{ t }">
      <div class="user-info">
        <h2>{{ t('title') }}</h2>
        <p>{{ t('bio') }}</p>
        <div class="stats">
          <span>{{ t('stats.followers') }}</span>
          <span>{{ t('stats.following') }}</span>
        </div>
      </div>
    </template>
  </i18n-group>
</template>
```

Fordítási fájl:

```json
{
  "user": {
    "profile": {
      "title": "User Profile",
      "bio": "User biography goes here",
      "stats": {
        "followers": "Followers: {count}",
        "following": "Following: {count}"
      }
    }
  }
}
```

### Dinamikus tartalommal

```vue
<template>
  <i18n-group prefix="blog.post">
    <template #default="{ t }">
      <article>
        <h1>{{ t('title') }}</h1>
        <div class="meta">
          {{ t('meta.author', { name: author }) }} |
          {{ t('meta.date', { date: publishDate }) }}
        </div>
        <div v-for="(section, index) in sections" :key="index">
          <h2>{{ t(`sections.${index}.title`) }}</h2>
          <p>{{ t(`sections.${index}.content`) }}</p>
        </div>
      </article>
    </template>
  </i18n-group>
</template>

<script setup>
const author = 'John Doe'
const publishDate = '2024-01-01'
const sections = ['intro', 'main', 'conclusion']
</script>
```

## 🎨 Stílusozás

A komponens a tartalmát `i18n-group` osztályú divbe csomagolja, kiegészítve a `groupClass` propban megadott további osztályokkal:

```css
.i18n-group {
  /* Your base styles */
}

.profile-section {
  /* Additional styles for profile sections */
}
```

## 🚀 Legjobb gyakorlatok

1. **Konzisztens előtagok**: Használj következetes és logikus előtagokat, amelyek az alkalmazásod szerkezetét tükrözik.

```vue
<i18n-group prefix="features.pricing">
<i18n-group prefix="features.security">
```

2. **Moduláris szervezés**: Csoportosítsd az összetartozó fordításokat értelmes előtagok alá.

```vue
<i18n-group prefix="checkout.payment">
<i18n-group prefix="checkout.shipping">
```

3. **Újrafelhasználható komponensek**: Készíts újrafelhasználható komponenseket saját fordítási csoportjukkal.

```vue
<!-- UserProfile.vue -->
<i18n-group prefix="user.profile">

<!-- UserSettings.vue -->
<i18n-group prefix="user.settings">
```

4. **Egymásba ágyazott csoportok**: Kerüld a mély beágyazást a világosság érdekében.

```vue
<!-- Good -->
<i18n-group prefix="shop.product">

<!-- Avoid -->
<i18n-group prefix="shop.category.product.details.specs">
```
