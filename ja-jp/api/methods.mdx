---
title: "メソッド"
description: "nuxt-i18n-micro のすべてのランタイムメソッド。"
---

# 🛠️ メソッド

このページでは、nuxt-i18n-micro が提供するすべての利用可能なメソッドを紹介します。ナビゲーションしやすいよう、機能ごとに整理されています。

## 📊 API 概要

プラグインが注入するすべてのヘルパーで、`PluginsInjections` インターフェースから読み取られます — したがって、このリストは実際に利用可能なものからずれることはありません。

{/* generated:methods-index — do not edit; run `pnpm run docs:generate` */}

| ヘルパー | シグネチャ | 目的 |
| --- | --- | --- |
| [`$_t`](#_t) | `(route: RouteLocationNormalizedLoaded) => (key: string, params?: Params, defaultValue?: string \| null) => CleanTranslation` | `$t` を特定のルートにバインドし、現在のページ外(例: 別のルートへのリンクをレンダリングするレイアウト)で翻訳します。 |
| [`$_ts`](#_ts) | `(route: RouteLocationNormalizedLoaded) => (key: string, params?: Params, defaultValue?: string \| null) => string` | `$ts` を特定のルートにバインドします。 |
| [`$defaultLocale`](#defaultlocale) | `() => string \| undefined` | 設定されたデフォルトロケールのコード。 |
| [`$getI18nConfig`](#geti18nconfig) | `() => ModuleOptionsExtend` | ランタイムから見た解決済みのモジュール設定。 |
| [`$getLocale`](#getlocale) | `(route?: RouteLocationNormalizedLoaded \| RouteLocationResolvedGeneric) => string` | アクティブなロケールのコード。 |
| [`$getLocaleName`](#getlocalename) | `() => string \| null` | 設定内のアクティブなロケールの `displayName`。存在しない場合は `null`。 |
| [`$getLocales`](#getlocales) | `() => Locale[]` | 設定されたすべてのロケールと、そのメタデータ。 |
| [`$getRouteName`](#getroutename) | `(route?: RouteLocationNamedRaw \| RouteLocationResolvedGeneric, locale?: string) => string` | ロケールプレフィックスを除いたルート名 — 翻訳がキー付けされる名前。 |
| [`$has`](#has) | `(key: string) => boolean` | キーがアクティブなロケールで解決されるかどうか。 |
| `$i18nStrategy` | `PathStrategy` | ロケールをパスとの間で解決するアクティブなルーティング戦略。 |
| [`$loadPageTranslations`](#loadpagetranslations) | `(locale: string, routeName: string, translations: Translations) => Promise<void>` | ビルド時にキーが不明なコンテンツ用に、ランタイムでページ用の翻訳を読み込みます。 |
| [`$localePath`](#localepath) | `(to: RouteLocationNamedRaw \| RouteLocationResolvedGeneric \| string, locale?: string) => string` | 指定または現在のロケール向けにパスを解決します。 |
| [`$localeRoute`](#localeroute) | `(to: RouteLocationNamedRaw \| RouteLocationResolvedGeneric \| string, locale?: string) => RouteLocationResolved` | 指定または現在のロケール向けにルートを解決します。 |
| [`$mergeTranslations`](#mergetranslations) | `(newTranslations: Translations) => void` | ランタイムでアクティブなロケールに翻訳をマージし、読み込まれている内容を上書きします。 |
| [`$resolveTranslations`](#resolvetranslations) | `() => Translations` | 現在アクティブなロケールとルート向けにメモリ内にあるすべての翻訳をツリーとして返します。 |
| [`$setI18nRouteParams`](#seti18nrouteparams) | `(value: I18nRouteParams) => I18nRouteParams` | 現在のルートにロケール別のパラメーターを設定し、動的セグメントを言語ごとに変えられるようにします。 |
| [`$setMissingHandler`](#setmissinghandler) | `(handler: MissingHandler \| null) => void` | 未解決のキーごとに呼び出されるコールバックをインストールします。 |
| [`$setTranslation`](#settranslation) | `(key: string, value: unknown) => void` | アクティブな辞書内の `key` の値を置き換えます。 |
| [`$switchLocale`](#switchlocale) | `(locale: string) => void` | 現在のページを別のロケールへナビゲートします。 |
| [`$switchLocalePath`](#switchlocalepath) | `(locale: string) => string` | ナビゲーションなしで、別のロケールにおける現在のページのパス。 |
| [`$switchLocaleRoute`](#switchlocaleroute) | `(locale: string) => RouteLocationRaw` | ナビゲーションなしで、別のロケールにおける現在のページのルートオブジェクト。 |
| [`$switchRoute`](#switchroute) | `(route: RouteLocationNamedRaw \| RouteLocationResolvedGeneric \| string, toLocale?: string) => void` | 別のルートへナビゲートします。アクティブなロケールを維持するか、`toLocale` へ切り替えます。 |
| [`$t`](#t) | `(key: string, params?: Params, defaultValue?: string \| null) => CleanTranslation` | キーを翻訳し、`params` を補間します。 |
| [`$tc`](#tc) | `(key: string, params: number \| Params, defaultValue?: string) => string` | 複数形化を伴う翻訳。 |
| [`$td`](#td) | `\{ (value: Date \| number \| string, options?: Intl.DateTimeFormatOptions): string (value: Date \| number \| string, key: string, overrides?: Intl.DateTimeFormatOptions): string (value: Date \| number \| string, key: string, locale: string, overrides?: Intl.DateTimeFormatOptions): string \}` | `Intl.DateTimeFormat` を使用してアクティブなロケール向けに日付をフォーマットします。 |
| [`$tdr`](#tdr) | `(value: Date \| number \| string, options?: Intl.RelativeTimeFormatOptions) => string` | `Intl.RelativeTimeFormat` を使って日付を相対時間(「3 日前」)としてフォーマットします。 |
| [`$tn`](#tn) | `\{ (value: number, options?: Intl.NumberFormatOptions): string (value: number, key: string, overrides?: Intl.NumberFormatOptions): string (value: number, key: string, locale: string, overrides?: Intl.NumberFormatOptions): string \}` | `Intl.NumberFormat` を使用してアクティブなロケール向けに数値をフォーマットします。 |
| [`$ts`](#ts) | `(key: string, params?: Params, defaultValue?: string) => string` | `$t` と同様ですが、常に文字列を返します: オブジェクトや配列の値はそのまま返されず、文字列化されます。 |

{/* /generated:methods-index */}

<Info>
`$defineI18nRoute` と `$clearCache` は `useNuxtApp()` 上でメインの i18n プラグインによって提供されますが、`useI18n()` コンポーザブルからは**返されない**ため、上記の表には含まれていません。(`$getI18nConfig` は両方から利用可能です。)以下のセクションを参照してください。
</Info>

### `$getI18nConfig`

{/* generated:method:$getI18nConfig — do not edit; run `pnpm run docs:generate` */}

```ts
() => ModuleOptionsExtend
```

ランタイムから見た解決済みのモジュール設定。

{/* /generated:method:$getI18nConfig */}

```typescript
const nuxtApp = useNuxtApp()
const { localeCookie, strategy, hooks } = nuxtApp.$getI18nConfig()
```

ビルド時や非 Nuxt コンテキストでは、代わりに `#build/i18n.strategy.mjs` の `getI18nConfig()` を使用してください。

<Info>
`serverTranslationPreload` は、サーバー専用の非公開設定(`#i18n-internal/config`)でのみ公開される非公開のモジュールオプションです。公開ランタイム API の一部ではなく、予告なく変更される可能性があります。
</Info>

## 🌍 ロケール管理

ロケール情報を取得・管理するためのメソッド。

### `$getLocale`

{/* generated:method:$getLocale — do not edit; run `pnpm run docs:generate` */}

```ts
(route?: RouteLocationNormalizedLoaded | RouteLocationResolvedGeneric) => string
```

アクティブなロケールのコード。特定のルートが属するロケールを取得するにはルートを渡します。

{/* /generated:method:$getLocale */}

```typescript
const locale = $getLocale()
// Output: 'en' (assuming the current locale is English)
```

### `$getLocaleName`

{/* generated:method:$getLocaleName — do not edit; run `pnpm run docs:generate` */}

```ts
() => string | null
```

設定内のアクティブなロケールの `displayName`。存在しない場合は `null`。

{/* /generated:method:$getLocaleName */}

```typescript
const locale = $getLocaleName()
// Output: 'English'
```

### `$getLocales`

{/* generated:method:$getLocales — do not edit; run `pnpm run docs:generate` */}

```ts
() => Locale[]
```

設定されたすべてのロケールと、そのメタデータ。

{/* /generated:method:$getLocales */}

```typescript
const locales = $getLocales()
// Output: [{ code: 'en', iso: 'en-US', dir: 'ltr' }, { code: 'fr', iso: 'fr-FR', dir: 'ltr' }]
```

### `$defaultLocale`

{/* generated:method:$defaultLocale — do not edit; run `pnpm run docs:generate` */}

```ts
() => string | undefined
```

設定されたデフォルトロケールのコード。

{/* /generated:method:$defaultLocale */}

```typescript
const defaultLocale = $defaultLocale()
// Output: 'en'
```

## 🔍 翻訳メソッド

翻訳の取得と管理を行うコアメソッド。

### `$t`

{/* generated:method:$t — do not edit; run `pnpm run docs:generate` */}

```ts
(key: string, params?: Params, defaultValue?: string | null) => CleanTranslation
```

キーを翻訳し、`params` を補間します。キーが不足している場合は `defaultValue` を返し、
デフォルトが指定されていない場合はキー自体を返します。

{/* /generated:method:$t */}

```typescript
const welcomeMessage = $t('welcome', { username: 'Alice', unreadCount: 5 })
// Output: "Welcome, Alice! You have 5 unread messages."
```

<Warning>
`$t` は `CleanTranslation`(`string | number | boolean | Translations | PluralTranslations | null`)を返します。キーが JSON 内の**ネストされたオブジェクト**を指している場合(例: JSON が `\{ "header": \{ "title": "Hi" \} \}` を含むときの `$t('header')`)、返り値は文字列ではなくその**オブジェクト**になります。Vue のテンプレートで直接使用(<code v-pre>\{\{ $t('header') \}\}</code>)すると `[object Object]` としてレンダリングされます。

**避ける方法:**

- より具体的なキーを使う: `$t('header.title')` → `"Hi"`
- `$ts()` を使う。常に文字列を返します(非文字列に対して `.toString()` を呼び出します)
- プログラム的な用途で意図的にサブオブジェクトへアクセスするために `$t` をネストキーと共に使う
</Warning>

### `$ts`

{/* generated:method:$ts — do not edit; run `pnpm run docs:generate` */}

```ts
(key: string, params?: Params, defaultValue?: string) => string
```

`$t` と同様ですが、常に文字列を返します: オブジェクトや配列の値はそのまま返されず、文字列化されます。

{/* /generated:method:$ts */}

```typescript
const welcomeMessage = $ts('welcome', { username: 'Alice', unreadCount: 5 })
// Output: "Welcome, Alice! You have 5 unread messages."
```

### `$_t` と `$_ts`

{/* generated:method:$_t — do not edit; run `pnpm run docs:generate` */}

```ts
(route: RouteLocationNormalizedLoaded) => (key: string, params?: Params, defaultValue?: string | null) => CleanTranslation
```

`$t` を特定のルートにバインドし、現在のページ外(例: 別のルートへのリンクをレンダリングするレイアウト)で翻訳します。

{/* /generated:method:$_t */}

{/* generated:method:$_ts — do not edit; run `pnpm run docs:generate` */}

```ts
(route: RouteLocationNormalizedLoaded) => (key: string, params?: Params, defaultValue?: string | null) => string
```

`$ts` を特定のルートにバインドします。`$_t` を参照してください。

{/* /generated:method:$_ts */}

`$t` と `$ts` のルートバインド版。**ルート**を最初に受け取り、そのルートのロケールとページコンテキストに固定された翻訳関数を返します。

- **アクセス**: `useNuxtApp()`(`useI18n()` から `$_t` / `$_ts` として再エクスポートもされます)

SSR やトランジション中のアクティブなルートが `router.currentRoute` と異なる場合に使用します — 例えば `<i18n-t>`、`<i18n-group>` の内部、または特定の `route` オブジェクト向けのコンテンツをレンダリングするときです。

```typescript
import { useRoute, useNuxtApp } from '#imports'

const route = useRoute()
const { $_t, $_ts } = useNuxtApp()

const $t = $_t(route)
const title = $t('page.title')

// String-safe variant
const label = $_ts(route)('page.label')
```

<Tip>

</Tip>

### `$tc`

{/* generated:method:$tc — do not edit; run `pnpm run docs:generate` */}

```ts
(key: string, params: number | Params, defaultValue?: string) => string
```

複数形化を伴う翻訳。`params` はカウント自体、または `count` を含むオブジェクトのいずれかです。

{/* /generated:method:$tc */}

**翻訳形式**: 形式は `|` で区切ります。プレースホルダーは**各**形式に配置します:

```json
{
  "apples": "no apples | one apple | {count} apples",
  "cart": "no items for {name} | one item for {name} | {count} items for {name}"
}
```

```typescript
$tc('apples', 0) // "no apples"
$tc('apples', 1) // "one apple"
$tc('apples', 10) // "10 apples"

// count + other params (second argument must be an object)
$tc('cart', { count: 10, name: 'Alice' }) // "10 items for Alice"
```

<Warning>

</Warning>

**コンポーネントによる代替** — `<i18n-t keypath="cart" :plural="count" :params="\{ name \}" />`(内部で `count` を `params` にマージします)。

<Tip>

</Tip>

### `$mergeTranslations`

{/* generated:method:$mergeTranslations — do not edit; run `pnpm run docs:generate` */}

```ts
(newTranslations: Translations) => void
```

ランタイムでアクティブなロケールに翻訳をマージし、読み込まれている内容を上書きします。

{/* /generated:method:$mergeTranslations */}

```typescript
$mergeTranslations({
  welcome: 'Bienvenue, {username}!',
})
// Output: Updates the translation cache with the new French translation
```

### `$resolveTranslations`

{/* generated:method:$resolveTranslations — do not edit; run `pnpm run docs:generate` */}

```ts
() => Translations
```

現在アクティブなロケールとルート向けにメモリ内にあるすべての翻訳をツリーとして返します。
`$t()` が今解決できる内容の読み取り専用ビューです。

{/* /generated:method:$resolveTranslations */}

```typescript
const tree = $resolveTranslations()
// { aaa: { bbb: 'ccc' }, ddd: 1111 } — the same dictionary $t() reads from
```

インメモリの辞書全体が必要な場合(DevTools、テスト、ランタイムエディター)に使います。返される値は読み取り専用として扱い、値の変更は `$setTranslation` を通じて行い、パッチのマージは `$mergeTranslations` を使ってください。

### `$setTranslation`

{/* generated:method:$setTranslation — do not edit; run `pnpm run docs:generate` */}

```ts
(key: string, value: unknown) => void
```

アクティブな辞書内の `key` の値を置き換えます。これはマージではなく置換です。
既存の兄弟要素を残す必要がある場合は `$mergeTranslations` を使用してください。

{/* /generated:method:$setTranslation */}

```typescript
$setTranslation('aaa', { fff: 'ggg' }) // replaces the whole `aaa` subtree
$setTranslation('aaa', 'text')          // replaces `aaa` with a string
$setTranslation('ddd', 1111)            // scalar at the top level
$setTranslation('aaa.bbb', 'nested')    // dotted path
```

これは**置換**であり、マージではありません。同じキー配下の既存の兄弟要素は削除されます。兄弟要素を失わずにパッチを適用するには、`$mergeTranslations` を使用してください。

### `$setMissingHandler`

{/* generated:method:$setMissingHandler — do not edit; run `pnpm run docs:generate` */}

```ts
(handler: MissingHandler | null) => void
```

未解決のキーごとに呼び出されるコールバックをインストールします。削除するには `null` を渡します。

{/* /generated:method:$setMissingHandler */}

**型定義**:

```typescript
type MissingHandler = (locale: string, key: string, routeName: string, instance?: unknown, type?: string) => void
```

```typescript
// Set a custom handler
$setMissingHandler((locale, key, routeName) => {
  console.error(`Missing translation: ${key} in ${locale} for route ${routeName}`)
  // Send to Sentry or other error tracking service
  // Sentry.captureMessage(`Missing translation: ${key}`)
})

// Remove the handler
$setMissingHandler(null)
```

**ユースケース**:

- Sentry、LogRocket などのエラー追跡サービスへの不足翻訳のログ
- 不足翻訳に関する分析情報の収集
- 不足翻訳キーに対するカスタムエラーハンドリング

## 🔢 数値と日付の書式設定

ロケール規約に従って数値と日付を書式設定するためのメソッド。

### `$tn`

{/* generated:method:$tn — do not edit; run `pnpm run docs:generate` */}

```ts
{ (value: number, options?: Intl.NumberFormatOptions): string (value: number, key: string, overrides?: Intl.NumberFormatOptions): string (value: number, key: string, locale: string, overrides?: Intl.NumberFormatOptions): string }
```

`Intl.NumberFormat` を使用してアクティブなロケール向けに数値をフォーマットします。キーを指定すると設定内の名前付きフォーマットが選ばれます。

{/* /generated:method:$tn */}

```typescript
// Inline options
const formattedNumber = $tn(1234567.89, { style: 'currency', currency: 'USD' })
// "$1,234,567.89"

// Named format (from nuxt.config i18n.numberFormats)
const price = $tn(1000, 'currency')
const priceDe = $tn(1000, 'currency', 'de')
const compact = $tn(1000, 'currency', { notation: 'compact' })
```

未知の名前付きフォーマットキーは、デフォルトの `Intl.NumberFormat` オプションにフォールバックします。開発時(クライアント)、`missingWarn` が有効(デフォルト)の場合は `console.warn` が出力されます。

**設定例**:

```ts
export default defineNuxtConfig({
  i18n: {
    numberFormats: {
      en: {
        currency: { style: 'currency', currency: 'USD' },
        decimal: { style: 'decimal', minimumFractionDigits: 2, maximumFractionDigits: 2 },
      },
      de: {
        currency: { style: 'currency', currency: 'EUR' },
      },
    },
  },
})
```

**ユースケース**:

- 適切なロケール形式で通貨、パーセンテージ、または小数として数値を書式設定
- アプリ全体で名前付きフォーマットを再利用(Vue I18n 移行時の互換性)

### `$td`

{/* generated:method:$td — do not edit; run `pnpm run docs:generate` */}

```ts
{ (value: Date | number | string, options?: Intl.DateTimeFormatOptions): string (value: Date | number | string, key: string, overrides?: Intl.DateTimeFormatOptions): string (value: Date | number | string, key: string, locale: string, overrides?: Intl.DateTimeFormatOptions): string }
```

`Intl.DateTimeFormat` を使用してアクティブなロケール向けに日付をフォーマットします。キーを指定すると設定内の名前付きフォーマットが選ばれます。

{/* /generated:method:$td */}

```typescript
const formattedDate = $td(new Date(), { weekday: 'long', year: 'numeric', month: 'long', day: 'numeric' })
// "Friday, September 1, 2023"

const short = $td(new Date(), 'short')
const longDe = $td(new Date(), 'long', 'de')
```

未知の名前付きフォーマットキーは、デフォルトの `Intl.DateTimeFormat` オプションにフォールバックします。開発時(クライアント)、`missingWarn` が有効(デフォルト)の場合は `console.warn` が出力されます。

**設定例**:

```ts
export default defineNuxtConfig({
  i18n: {
    datetimeFormats: {
      en: {
        short: { year: 'numeric', month: 'short', day: 'numeric' },
        long: { year: 'numeric', month: 'long', day: 'numeric', weekday: 'long' },
      },
    },
  },
})
```

**ユースケース**:

- ユーザーのロケールに合わせた形式で日付を表示
- アプリ全体で短い / 長い / 日時のプリセットを共有

### `$tdr`

{/* generated:method:$tdr — do not edit; run `pnpm run docs:generate` */}

```ts
(value: Date | number | string, options?: Intl.RelativeTimeFormatOptions) => string
```

`Intl.RelativeTimeFormat` を使って日付を相対時間(「3 日前」)としてフォーマットします。

{/* /generated:method:$tdr */}

```typescript
const relativeDate = $tdr(new Date(Date.now() - 1000 * 60 * 5))
// Output: "5 minutes ago" in the 'en-US' locale
```

## 🔄 ルートとロケールの切り替え

ロケールとルート間を切り替えるためのメソッド。

### `$switchLocale`

{/* generated:method:$switchLocale — do not edit; run `pnpm run docs:generate` */}

```ts
(locale: string) => void
```

現在のページを別のロケールへナビゲートします。

{/* /generated:method:$switchLocale */}

```typescript
$switchLocale('fr')
// Prefix strategies: navigates to the French version of the route
// no_prefix: updates locale and translations without changing the URL path
```

<Tip>
[FAQ — URL を変更せずにロケールを切り替える](/guides/faq#-switch-locale-without-changing-the-url) を参照してください。プレフィックス戦略では、リンクターゲットのみが必要な場合は `$switchLocalePath()` を使用してください。
</Tip>

### `$switchLocaleRoute`

{/* generated:method:$switchLocaleRoute — do not edit; run `pnpm run docs:generate` */}

```ts
(locale: string) => RouteLocationRaw
```

ナビゲーションなしで、別のロケールにおける現在のページのルートオブジェクト。

{/* /generated:method:$switchLocaleRoute */}

```typescript
// on /en/news
const routeFr = $switchLocaleRoute('fr')
// Output: A route object with the new locale applied, e.g., { name: 'localized-news', params: { locale: 'fr' } }
```

### `$switchLocalePath`

{/* generated:method:$switchLocalePath — do not edit; run `pnpm run docs:generate` */}

```ts
(locale: string) => string
```

ナビゲーションなしで、別のロケールにおける現在のページのパス。

{/* /generated:method:$switchLocalePath */}

```typescript
// on /en/news
const routeFr = $switchLocalePath('fr')
window.location.href = routeFr
// Output: url with new locale applied, e.g., '/fr/nouvelles'
```

### `$switchRoute`

{/* generated:method:$switchRoute — do not edit; run `pnpm run docs:generate` */}

```ts
(route: RouteLocationNamedRaw | RouteLocationResolvedGeneric | string, toLocale?: string) => void
```

別のルートへナビゲートします。アクティブなロケールを維持するか、`toLocale` へ切り替えます。

{/* /generated:method:$switchRoute */}

**例**:

<CodeGroup>
```typescript String Path
// Switches to the given path with the current locale
$switchRoute('/about')
```

```typescript String Path with Locale
// Switches to the given path with French locale
$switchRoute('/about', 'fr')
```

```typescript Named Route
// Switches to a named route with the current locale
$switchRoute({ name: 'page' })
```

```typescript Named Route with Locale
// Switches to a named route and changes the locale to Spanish
$switchRoute({ name: 'page' }, 'es')
```
</CodeGroup>

## 🌐 ルート生成

ローカライズされたルートとパスを生成するためのメソッド。

### `$localeRoute`

{/* generated:method:$localeRoute — do not edit; run `pnpm run docs:generate` */}

```ts
(to: RouteLocationNamedRaw | RouteLocationResolvedGeneric | string, locale?: string) => RouteLocationResolved
```

指定または現在のロケール向けにルートを解決します。

{/* /generated:method:$localeRoute */}

```typescript
const localizedRoute = $localeRoute({ name: 'index' })
// Output: A route object with the current locale applied, e.g., { name: 'index', params: { locale: 'fr' } }
```

### `$localePath`

{/* generated:method:$localePath — do not edit; run `pnpm run docs:generate` */}

```ts
(to: RouteLocationNamedRaw | RouteLocationResolvedGeneric | string, locale?: string) => string
```

指定または現在のロケール向けにパスを解決します。

{/* /generated:method:$localePath */}

```typescript
const localizedPath = $localePath({ name: 'news' })
// Output: path with current (or specified) locale applied, e.g., '/en/nouvelles'
```

## 🔍 ルート情報

ルート情報と名前を取得するためのメソッド。

### `$getRouteName`

{/* generated:method:$getRouteName — do not edit; run `pnpm run docs:generate` */}

```ts
(route?: RouteLocationNamedRaw | RouteLocationResolvedGeneric, locale?: string) => string
```

ロケールプレフィックスを除いたルート名 — 翻訳がキー付けされる名前。

{/* /generated:method:$getRouteName */}

```typescript
const routeName = $getRouteName(routeObject, 'fr')
// Output: 'index' (assuming the base route name is 'index')
```

## 🚦 ルート設定

ルートの動作とアクセス制御を設定するためのメソッド。

### `$defineI18nRoute`

{/* generated:method:$defineI18nRoute — do not edit; run `pnpm run docs:generate` */}

```ts
(routeDefinition: DefineI18nRouteConfig) => Promise<void>
```

コンポーネント内からページ単位のロケール設定と翻訳を登録します。
アクティブなロケールにマージされ、ロケール変更時に再適用されます。

{/* /generated:method:$defineI18nRoute */}

> [!IMPORTANT]
> `$defineI18nRoute` は**define プラグイン**によって提供され、`useNuxtApp()` からのみ利用可能です — `useI18n()` の戻り値オブジェクトには**含まれません**。
> `script setup` 内では必ず `useNuxtApp()` から分割代入してください。`$defineI18nRoute(...)` を素のグローバルとして呼び出すと、SSR / プリレンダリング中に `"$defineI18nRoute is not defined"` がスローされます。

**パラメーター**:

- **locales**: `string[] | Record<string, Record<string, string>>` — ルートで利用可能なロケール
- **localeRoutes**: `Record<string, string>` — 任意。特定のロケール向けのカスタムルート
- **disableMeta**: `boolean | string[]` — 任意。すべてまたは特定のロケール向けに i18n メタタグを無効化

**基本例**:

```typescript
import { useNuxtApp } from '#imports'

const { $defineI18nRoute } = useNuxtApp()

$defineI18nRoute({
  locales: ['en', 'fr', 'de'],
  localeRoutes: {
    en: '/welcome',
    fr: '/bienvenue',
    de: '/willkommen',
  },
  disableMeta: false,
})
```

> 📖 **詳細な使用例、設定形式、およびベストプラクティスについては、[コンポーネント単位の翻訳ガイド](/guides/per-component-translations) を参照してください。**

### `$setI18nRouteParams`

{/* generated:method:$setI18nRouteParams — do not edit; run `pnpm run docs:generate` */}

```ts
(value: I18nRouteParams) => I18nRouteParams
```

現在のルートにロケール別のパラメーターを設定し、動的セグメントを言語ごとに変えられるようにします。
head がレンダリングされる前、SSR 中に呼び出してください。

{/* /generated:method:$setI18nRouteParams */}

```typescript
// in pages/news/[id].vue
// for en/news/1-first-article
const { $switchLocaleRoute, $setI18nRouteParams, $defineI18nRoute } = useNuxtApp()
$defineI18nRoute({
  localeRoutes: {
    en: '/news/:id()',
    fr: '/nouvelles/:id()',
    de: '/Nachricht/:id()',
  },
})
const { data: news } = await useAsyncData(`news-${params.id}`, async () => {
  let response = await $fetch("/api/getNews", {
    query: {
      id: params.id,
    },
  });
  if (response?.localeSlugs) {
    response.localeSlugs = {
      en: {
        id: '1-first-article'
      }
      fr: {
        id: '1-premier-article'
      }
      de: {
        id: '1-erster-Artikel'
      }
    }
    $setI18nRouteParams(response?.localeSlugs);
  }
  return response;
});
$switchLocalePath('fr') // === 'fr/nouvelles/1-premier-article'
$switchLocalePath('de') // === 'de/Nachricht/1-erster-Artikel'
```

## 💻 使用例

### 基本的なコンポーネントでの利用

```vue
<template>
  <div>
    <p>{{ $t('key2.key2.key2.key2.key2') }}</p>
    <p>Current Locale: {{ $getLocale() }}</p>

    <div>
      {{ $t('welcome', { username: 'Alice', unreadCount: 5 }) }}
    </div>
    <div>
      {{ $tc('apples', 10) }}
    </div>

    <div>
      <button v-for="locale in $getLocales()" :key="locale.code" :disabled="locale.code === $getLocale()" @click="() => $switchLocale(locale.code)">
        Switch to {{ locale.code }}
      </button>
    </div>

    <div>
      <NuxtLink :to="$localeRoute({ name: 'index' })"> Go to Index </NuxtLink>
    </div>
  </div>
</template>

<script setup>
import { useI18n } from '#imports'

const { $getLocale, $switchLocale, $getLocales, $localeRoute, $t, $tc } = useI18n()
</script>
```

### useNuxtApp との組み合わせ

```typescript
import { useNuxtApp } from '#imports'

const { $getLocale, $switchLocale, $getLocales, $localeRoute, $t } = useNuxtApp()
```

### useI18n コンポーザブルとの組み合わせ

```typescript
import { useI18n } from '#imports'

const { $getLocale, $switchLocale, $getLocales, $localeRoute, $t } = useI18n()
// or
const i18n = useI18n()
```

## 🔧 キャッシュとユーティリティメソッド

### `$has`

{/* generated:method:$has — do not edit; run `pnpm run docs:generate` */}

```ts
(key: string) => boolean
```

キーがアクティブなロケールで解決されるかどうか。生のキーをレンダリングする代わりに、オプションのコピーで分岐するために使います。

{/* /generated:method:$has */}

同一ロケール内のページ遷移中、v3 は離脱ページの翻訳をこの辞書へ自動的にディープマージし、遷移が終了するまで保持します — したがって前ページのキーが一時的に `true` を返すことがあります。`previousPageFallback` オプションはありません。この挙動は組み込みです。[FAQ — ページ遷移](/guides/faq#-why-do-translations-break-during-page-transitions-especially-with-defineasynccomponent) を参照してください。

```typescript
if ($has('welcome')) {
  console.log($t('welcome'))
} else {
  console.log('Key not found')
}
```

### `$clearCache`

{/* generated:method:$clearCache — do not edit; run `pnpm run docs:generate` */}

```ts
() => void
```

キャッシュされたすべてのチャンクを、クライアントおよびアクティブな辞書から削除します。

{/* /generated:method:$clearCache */}

**アクセス**: `useNuxtApp().$clearCache`。`PluginsInjections` ではなく `NuxtAppOnlyInjections` で宣言されているため、型は付いていますが `useI18n()` のヘルパーオブジェクトには意図的に含まれません:

```typescript
const { $clearCache } = useNuxtApp()
// All cached translations are removed; next render will re-fetch them
$clearCache()
```

### `$loadPageTranslations`

{/* generated:method:$loadPageTranslations — do not edit; run `pnpm run docs:generate` */}

```ts
(locale: string, routeName: string, translations: Translations) => Promise<void>
```

ビルド時にキーが不明なコンテンツ用に、ランタイムでページ用の翻訳を読み込みます。

{/* /generated:method:$loadPageTranslations */}

```typescript
await $loadPageTranslations('en', 'about', {
  title: 'About Us',
  description: 'Learn more about our company',
})
```

## 🧭 `useI18nLocale` コンポーザブル

ロケール状態管理のための一元化されたコンポーザブル。`useState('i18n-locale')` や `useCookie('user-locale')` を直接操作する代わりにこちらを使用してください。

```typescript
const {
  setLocale, // (locale: string) => void — updates state + cookie
  getLocale, // () => string | null — from state or cookie
  getPreferredLocale, // () => string | null — validated against locales list
  getEffectiveLocale, // (route, getLocaleFromRoute) => string
  resolveInitialLocale, // (options) => string
  isValidLocale, // (locale) => boolean
  locale, // Ref<string | null> — reactive state
  localeCookie, // CookieRef — reactive cookie
  syncLocale, // (locale) => void — sync to cookie only
  validLocales, // string[] — list of valid locale codes
} = useI18nLocale()
```

### 主要メソッド

| メソッド                  | 説明                                                 |
| ----------------------- | ----------------------------------------------------------- |
| `setLocale(locale)`     | `useState` とクッキーの両方をアトミックにロケールに設定        |
| `getLocale()`           | state またはクッキーから現在のロケールを返す                 |
| `getPreferredLocale()`  | `locales` リストに対して検証されたロケール、または `null` を返す  |
| `isValidLocale(locale)` | ロケールコードが設定された `locales` リストに含まれるかを確認 |

### カスタムプラグインでの使い方

```typescript
// plugins/i18n-loader.server.ts
export default defineNuxtPlugin({
  name: 'i18n-custom-loader',
  enforce: 'pre',
  order: -10,
  setup() {
    const { setLocale } = useI18nLocale()
    // Detect locale from headers, domain, etc.
    setLocale('de')
  },
})
```

詳細な例については [カスタム言語検出](/guides/custom-language-detection) を参照してください。
