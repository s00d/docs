---
title: "רכיב `<i18n-t>`"
description: "הרכיב <i18n-t> מעבד תרגומים עם אינטרפולציה מבוססת slot, כך שתוכל להזריק רכיבי Vue ו־markup עשיר למפתחות שפה."
---

# 🌍 רכיב `<i18n-t>`

הרכיב `<i18n-t>` ב־`Nuxt I18n Micro` הוא רכיב תרגום גמיש התומך בהזרקת תוכן דינמי דרך slots. הוא מאפשר לך לאנטרפל תרגומים עם רכיבי Vue מותאמים או תוכן HTML, ומאפשר תרחישי לוקליזציה מתקדמים.

## ⚙️ Props

{/* generated:component:i18n-t — do not edit; run `pnpm run docs:generate` */}

| Prop | טיפוס | ברירת מחדל | תיאור |
| --- | --- | --- | --- |
| `keypath` **\*** | `string` | — | מפתח תרגום לעיבוד. |
| `plural` | `number \| string` | — | ספירה הבוחרת את צורת הרבים. הגדר אותו כדי להשתמש ב־`$tc` במקום ב־`$t`. |
| `tag` | `string` | `'span'` | האלמנט לעיבוד. |
| `params` | `Record<string, string \| number \| boolean>` | `() => (\{\})` | ערכי אינטרפולציה המוחלפים לתוך התרגום. |
| `defaultValue` | `string` | `''` | מעובד במקום הרכיב כאשר `hideIfEmpty` מדחיק תרגום ריק. |
| `html` | `boolean` | `false` | עיבוד התרגום כ־HTML במקום כטקסט. רק לתוכן שאתה שולט בו. |
| `hideIfEmpty` | `boolean` | `false` | אין לעבד כלום כאשר התרגום מפוענח למחרוזת ריקה. |
| `customPluralRule` | `PluralFunc` | `null` | כלל רבים עבור אלמנט זה בלבד, שעוקף את זה שהוגדר. |
| `number` | `number \| string` | — | מעצב את הערך עם `$tn` ומעביר אותו לתרגום בתור `\{number\}`. |
| `date` | `Date \| string \| number` | — | מעצב את הערך עם `$td` ומעביר אותו לתרגום בתור `\{date\}`. |
| `relativeDate` | `Date \| string \| number` | — | מעצב את הערך עם `$tdr` ומעביר אותו לתרגום בתור `\{relativeDate\}`. |

**\*** נדרש.

{/* /generated:component:i18n-t */}

רק אחד מ־`plural`, ‏`number`, ‏`date` ו־`relativeDate` חל בכל פעם; כאשר מוגדרים כמה, הראשון ברשימה זו מנצח.

### דוגמאות לכל prop

```vue
<!-- keypath: the translation to render -->
<i18n-t keypath="feedback.text" />

<!-- plural: selects the plural form -->
<i18n-t keypath="items" :plural="10" />

<!-- params: interpolated into the translation -->
<i18n-t keypath="greeting" :params="{ name: 'Ada' }" />

<!-- tag: the element to render -->
<i18n-t keypath="feedback.text" tag="p" />

<!-- number / date / relativeDate: formatted, then interpolated -->
<i18n-t keypath="cart.total" :number="1234.5" />
<i18n-t keypath="post.published" :date="post.createdAt" />
<i18n-t keypath="post.ago" :relative-date="post.createdAt" />

<!-- hideIfEmpty with a fallback -->
<i18n-t keypath="promo.banner" hide-if-empty default-value="Nothing to show" />
```

```json
{
  "items": "Nothing|You have {count} item|You have {count} items"
}
```

## 🛠️ דוגמאות שימוש

### שימוש בסיסי

```vue
<i18n-t keypath="feedback.text" />
```

זה מעבד את התרגום עבור `feedback.text` בתוך תג `<span>`.

### שימוש ב־slots לתוכן דינמי

הרכיב `<i18n-t>` תומך בשימוש ב־slots להזרקת רכיבי Vue או תוכן אחר באופן דינמי לחלקים ספציפיים של התרגום.

```vue
<i18n-t keypath="feedback.text">
  <template #link>
    <nuxt-link :to="{ name: 'index' }">
      <i18n-t keypath="feedback.link" />
    </nuxt-link>
  </template>
</i18n-t>
```

בדוגמה זו, ה־placeholder ‏`\{link\}` במחרוזת התרגום `feedback.text` מוחלף על ידי הרכיב `<nuxt-link>`, שמכיל בעצמו רכיב תרגום נוסף.

### רבים

```vue
<i18n-t keypath="items.count" :plural="itemCount" />
```

זה מיישם אוטומטית כללי רבים בהתבסס על ערך `itemCount`.

## 🚀 תכונות נוספות

### Slot ברירת מחדל

אם לא סופק slot ספציפי, ניתן להתאים אישית את התרגום דרך slot ברירת המחדל, המספק את המחרוזת המתורגמת המלאה:

```vue
<i18n-t keypath="welcome.message">
  <template #default="{ translation }">
    {{ translation.replace('Nuxt', 'Vue') }}
  </template>
</i18n-t>
```

### עיבוד מותנה

אל תעבד כלום אם מחרוזת התרגום ריקה על ידי שימוש ב־prop ‏`hideIfEmpty`.

```vue
<i18n-t keypath="optionalMessage" :hideIfEmpty="true"></i18n-t>
```

### כלל רבים מותאם

השתמש בפונקציה מותאמת כדי לטפל ברבים.

```vue
<i18n-t
  keypath="items"
  :plural="itemCount"
  :customPluralRule="
    (key, count, params, locale, t) => {
      const raw = t(key, params)
      if (!raw) return null
      return count === 1 ? 'One item' : `${count} items`
    }
  "
/>
```

### דוגמה מתקדמת עם Slots

השתמש ב־slots כדי להתאים אישית את אופן עיבוד תוכן התרגום.

```vue
<i18n-t keypath="welcomeMessage">
  <template #default="{ translation }">
    <strong>{{ translation }}</strong>
  </template>
</i18n-t>
```
