---
title: "שימוש בתרגומים בקומפוננטות"
description: "השתמשו ב-$t() ובתרגומים בקומפוננטות Vue."
---

# שימוש בתרגומים בקומפוננטות

לאחר שהמודול הותקן והוגדר, ניתן להציג תרגומים על ידי קריאה ל-`$t('your_key')` בתוך תבניות ה-Vue והסקריפטים שלכם.

## 1. קבצי תרגום ברמת root וספציפיים לעמוד

1. תרגומי **root-level** נמצאים ישירות תחת תיקיית `/locales` (למשל `/locales/en.json`, `/locales/fr.json`). בזמן build, הם ממוזגים אוטומטית לכל קובץ ספציפי לעמוד, מה שהופך אותם לזמינים בכל העמודים.
2. תרגומים **ספציפיים לעמוד** נמצאים תחת `/locales/pages/...`, בהתאמה למבנה תיקיית Nuxt pages שלכם:

```text
locales/
├── en.json
├── fr.json
└── pages/
    └── dir1/
        ├── en.json
        └── fr.json

```

## 2. שימוש בתבנית עם `$t`

בקבצי `.vue` שלכם, תוכלו להשתמש ישירות ב-`$t` בתבנית:

```vue
<template>
  <div>
    <!-- Root-level translation (available on all pages) -->
    <p>{{ $t('test_key') }}</p>
    <!-- Displays: Hello World! -->

    <!-- Page-specific translation (if this page is /dir1) -->
    <p>{{ $t('local_key') }}</p>
    <!-- Displays: This is a page-specific translation for /dir1. -->

    <!-- Nested key -->
    <p>{{ $t('nested.key.deeper') }}</p>
    <!-- Displays: Nested translation here -->

    <!-- Placeholder usage -->
    <p>{{ $t('greeting', { name: 'Alice' }) }}</p>
    <!-- Displays: Hello, Alice! -->
  </div>
</template>

<script setup lang="ts">
// No need to import anything from 'nuxt-i18n-micro'.
// The plugin automatically provides `useI18n` and `$t`.
const { $t } = useI18n()

// You can now use $t in your script, for example:
console.log($t('test_key')) // "Hello World!"
</script>
```

<Tip>
`$t` יכול להחזיר **אובייקטים** אם המפתח מצביע על צומת JSON מקונן (למשל `$t('header')` → `\{ title: "Hi" \}`). בתבניות זה מרונדר כ-`[object Object]`. השתמשו ב-`$ts` כאשר אתם צריכים מחרוזת מובטחת, או ספקו מפתח ספציפי יותר כמו `$t('header.title')`. ראו [הפניית API — $t לעומת $ts](/api/methods#t) לפרטים.
</Tip>

## 3. החלפת ה-locale הנוכחי

כדי לשנות שפות באפליקציה שלכם, ניתן לאחזר את `$locale` מ-`useI18n()` ולהקצות לו קוד חדש:

```vue
<template>
  <div>
    <button @click="$switchLocale('en')">English</button>
    <button @click="$switchLocale('fr')">Français</button>
  </div>
</template>

<script setup lang="ts">
// The plugin auto-injects these references
const { $t, $switchLocale } = useI18n()
</script>
```

## 4. מפתחות מקוננים ו-placeholders

- **מפתחות מקוננים** משתמשים בסימון נקודה, למשל `nested.key.deeper`.
- **Placeholders** מאפשרים לכם להכניס ערכים דינמיים:
  ```json
  { "greeting": "Hello, {name}!" }
  ```
  ```vue
  <p>{{ $t('greeting', { name: 'Alice' }) }}</p>
  <!-- Displays: Hello, Alice! -->
  ```

## 5. טיפים ושיטות עבודה מומלצות

- **מפתחות מתנגשים**: אם קובץ ספציפי לעמוד מכיל את אותו מפתח כמו קובץ ברמת root, לגרסה הספציפית לעמוד יש עדיפות (תרגומי root ממוזגים כבסיס בזמן build).
- **השבתת קבצי עמוד**: אם אתם מעדיפים לא להשתמש בתרגומים ספציפיים לעמוד, תוכלו להגדיר `disablePageLocales: true` בקונפיגורציית Nuxt. כך, רק קבצי `/locales/*.json` יהיו בשימוש.
- **ללא ייבוא ישיר**: אתם לא צריכים לבצע `import \{ useI18n \} from 'nuxt-i18n-micro'`; תכונת ההזרקה האוטומטית של המודול מטפלת בזה עבורכם.

זהו! עם שלבים אלה, תוכלו **להתחיל לאחזר תרגומים** במהירות מקבצי JSON ברמת root או ספציפיים לעמוד ו**להציג אותם** באפליקציית Nuxt שלכם באמצעות `$t`.

## משאבים נוספים

כדי לחקור תכונות מתקדמות יותר, הקפידו לבדוק:

1. **קומפוננטת `<i18n-t>`**  
   הקומפוננטה `<i18n-t>` היא חלופה חזקה ל-`$t`, שימושית במיוחד למצבים הכוללים אלמנטים מקוננים, שימוש מתקדם ב-placeholders ומבני תרגום מורכבים יותר.

- **תיעוד**: [i18n-t](/api/components/i18n-t)

2. **Composable `useI18n`**  
   ה-composable `useI18n` מציע סט מלא של פונקציות ומאפיינים reactive לניהול תרגומים, טיפול בהחלפת locale, עיצוב ועוד.

- **תיעוד**: [useI18n](/api/composables/use-i18n)
