---
title: "`<i18n-group>` 组件"
description: "Vue 组件 <i18n-group> 会把翻译分组在共享的键前缀之下，让嵌套标签和重复的段落在模板中保持简洁。"
---

# 🌍 `<i18n-group>` 组件

Nuxt I18n Micro 中的 `<i18n-group>` 组件提供了一种便捷方式，将翻译分组到共同前缀之下，从而减少重复并改善代码组织。

## ⚙️ 属性

{/* generated:component:i18n-group — do not edit; run `pnpm run docs:generate` */}

| 属性 | 类型 | 默认值 | 描述 |
| --- | --- | --- | --- |
| `prefix` **\*** | `string` | — | 插槽内每次查找都会基于该键前缀进行解析。 |
| `groupClass` | `string` | `''` | 附加到包裹元素上的额外 class。 |

**\*** 必填。

### 插槽

| 插槽 | 绑定 | 描述 |
| --- | --- | --- |
| `default` | `prefix`, `t` | 接收 `prefix` 以及一个已限定到该前缀的 `t` 函数。 |

{/* /generated:component:i18n-group */}

## 🎯 插槽

默认插槽会接收 `prefix` 以及一个已限定到它的 `t` 函数，因此内部的键都相对于该前缀书写：

```vue
<i18n-group prefix="checkout">
  <template #default="{ t }">
    <h2>{{ t('title') }}</h2>
    <p>{{ t('summary', { count: 3 }) }}</p>
  </template>
</i18n-group>
```

## 🛠️ 使用示例

### 基本用法

```vue
<template>
  <i18n-group prefix="product.details">
    <template #default="{ t }">
      <h1>{{ t('title') }}</h1>
      <p>{{ t('description') }}</p>
      <div class="price">{{ t('price', { value: 99.99 }) }}</div>
    </template>
  </i18n-group>
</template>
```

翻译文件：

```json
{
  "product": {
    "details": {
      "title": "Premium Product",
      "description": "This is a premium product",
      "price": "Price: ${value}"
    }
  }
}
```

### 使用自定义 class

```vue
<template>
  <i18n-group prefix="user.profile" group-class="profile-section">
    <template #default="{ t }">
      <div class="user-info">
        <h2>{{ t('title') }}</h2>
        <p>{{ t('bio') }}</p>
        <div class="stats">
          <span>{{ t('stats.followers') }}</span>
          <span>{{ t('stats.following') }}</span>
        </div>
      </div>
    </template>
  </i18n-group>
</template>
```

翻译文件：

```json
{
  "user": {
    "profile": {
      "title": "User Profile",
      "bio": "User biography goes here",
      "stats": {
        "followers": "Followers: {count}",
        "following": "Following: {count}"
      }
    }
  }
}
```

### 结合动态内容

```vue
<template>
  <i18n-group prefix="blog.post">
    <template #default="{ t }">
      <article>
        <h1>{{ t('title') }}</h1>
        <div class="meta">
          {{ t('meta.author', { name: author }) }} |
          {{ t('meta.date', { date: publishDate }) }}
        </div>
        <div v-for="(section, index) in sections" :key="index">
          <h2>{{ t(`sections.${index}.title`) }}</h2>
          <p>{{ t(`sections.${index}.content`) }}</p>
        </div>
      </article>
    </template>
  </i18n-group>
</template>

<script setup>
const author = 'John Doe'
const publishDate = '2024-01-01'
const sections = ['intro', 'main', 'conclusion']
</script>
```

## 🎨 样式

该组件会把内容包在一个带有 `i18n-group` class 的 div 中，并附加 `groupClass` 属性指定的额外 class：

```css
.i18n-group {
  /* Your base styles */
}

.profile-section {
  /* Additional styles for profile sections */
}
```

## 🚀 最佳实践

1. **一致的前缀**：使用与应用结构一致且合乎逻辑的前缀

```vue
<i18n-group prefix="features.pricing">
<i18n-group prefix="features.security">
```

2. **模块化组织**：把相关的翻译分组在有意义的前缀下

```vue
<i18n-group prefix="checkout.payment">
<i18n-group prefix="checkout.shipping">
```

3. **可复用组件**：为组件配套自己的翻译分组

```vue
<!-- UserProfile.vue -->
<i18n-group prefix="user.profile">

<!-- UserSettings.vue -->
<i18n-group prefix="user.settings">
```

4. **嵌套分组**：避免嵌套过深，保持清晰度

```vue
<!-- Good -->
<i18n-group prefix="shop.product">

<!-- Avoid -->
<i18n-group prefix="shop.category.product.details.specs">
```
