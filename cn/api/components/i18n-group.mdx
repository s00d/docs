---
title: "`<i18n-group>` 组件"
description: "使用共享键前缀对翻译进行分组。"
---

# 🌍 `<i18n-group>` 组件

Nuxt I18n Micro 中的 `<i18n-group>` 组件提供了一种便捷的方式,可将翻译分组到同一个前缀之下,从而减少重复并改善代码组织。

## ⚙️ 属性

{/* generated:component:i18n-group — do not edit; run `pnpm run docs:generate` */}

| 属性 | 类型 | 默认值 | 说明 |
| --- | --- | --- | --- |
| `prefix` **\*** | `string` | — | 插槽内每次查找都会拼接的键前缀。 |
| `groupClass` | `string` | `''` | 附加到外层元素上的额外 class。 |

**\*** 必填。

### 插槽

| 插槽 | 绑定 | 说明 |
| --- | --- | --- |
| `default` | `prefix`, `t` | 接收 `prefix` 以及作用域限定于该前缀的 `t` 函数。 |

{/* /generated:component:i18n-group */}

## 🎯 插槽

默认插槽会接收 `prefix` 以及作用域限定于该前缀的 `t` 函数,插槽内的键都相对于该前缀编写:

```vue
<i18n-group prefix="checkout">
  <template #default="{ t }">
    <h2>{{ t('title') }}</h2>
    <p>{{ t('summary', { count: 3 }) }}</p>
  </template>
</i18n-group>
```

## 🛠️ 示例用法

### 基本用法

```vue
<template>
  <i18n-group prefix="product.details">
    <template #default="{ t }">
      <h1>{{ t('title') }}</h1>
      <p>{{ t('description') }}</p>
      <div class="price">{{ t('price', { value: 99.99 }) }}</div>
    </template>
  </i18n-group>
</template>
```

翻译文件:

```json
{
  "product": {
    "details": {
      "title": "Premium Product",
      "description": "This is a premium product",
      "price": "Price: ${value}"
    }
  }
}
```

### 使用自定义 class

```vue
<template>
  <i18n-group prefix="user.profile" group-class="profile-section">
    <template #default="{ t }">
      <div class="user-info">
        <h2>{{ t('title') }}</h2>
        <p>{{ t('bio') }}</p>
        <div class="stats">
          <span>{{ t('stats.followers') }}</span>
          <span>{{ t('stats.following') }}</span>
        </div>
      </div>
    </template>
  </i18n-group>
</template>
```

翻译文件:

```json
{
  "user": {
    "profile": {
      "title": "User Profile",
      "bio": "User biography goes here",
      "stats": {
        "followers": "Followers: {count}",
        "following": "Following: {count}"
      }
    }
  }
}
```

### 使用动态内容

```vue
<template>
  <i18n-group prefix="blog.post">
    <template #default="{ t }">
      <article>
        <h1>{{ t('title') }}</h1>
        <div class="meta">
          {{ t('meta.author', { name: author }) }} |
          {{ t('meta.date', { date: publishDate }) }}
        </div>
        <div v-for="(section, index) in sections" :key="index">
          <h2>{{ t(`sections.${index}.title`) }}</h2>
          <p>{{ t(`sections.${index}.content`) }}</p>
        </div>
      </article>
    </template>
  </i18n-group>
</template>

<script setup>
const author = 'John Doe'
const publishDate = '2024-01-01'
const sections = ['intro', 'main', 'conclusion']
</script>
```

## 🎨 样式

该组件会将其内容包裹在一个 class 为 `i18n-group` 的 div 中,并附加通过 `groupClass` 属性指定的任何 class:

```css
.i18n-group {
  /* Your base styles */
}

.profile-section {
  /* Additional styles for profile sections */
}
```

## 🚀 最佳实践

1. **保持前缀一致**:使用与你的应用结构相符、逻辑一致的前缀

```vue
<i18n-group prefix="features.pricing">
<i18n-group prefix="features.security">
```

2. **模块化组织**:将相关翻译按有意义的前缀分组

```vue
<i18n-group prefix="checkout.payment">
<i18n-group prefix="checkout.shipping">
```

3. **可复用组件**:创建带有各自翻译分组的可复用组件

```vue
<!-- UserProfile.vue -->
<i18n-group prefix="user.profile">

<!-- UserSettings.vue -->
<i18n-group prefix="user.settings">
```

4. **嵌套分组**:避免过深嵌套的分组,以保持清晰

```vue
<!-- Good -->
<i18n-group prefix="shop.product">

<!-- Avoid -->
<i18n-group prefix="shop.category.product.details.specs">
```
