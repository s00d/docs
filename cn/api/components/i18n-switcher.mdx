---
title: "`<i18n-switcher>` 组件"
description: "用于切换应用语言的下拉组件。"
---

# 🌍 `<i18n-switcher>` 组件

`Nuxt I18n Micro` 中的 `<i18n-switcher>` 组件提供了一个用户友好的下拉界面,用于在应用的各语言之间切换。该组件高度可定制,可与你的应用设计和布局无缝融合。

配置为 `disabled: true` 的语言会**被排除**在下拉列表之外(它们仍可通过 `$getLocales()` 用于 SEO/head 标签)。只有可切换的语言才会带有 `NuxtLink` 和 `hreflang`。

## ⚙️ 属性

{/* generated:component:i18n-switcher — do not edit; run `pnpm run docs:generate` */}

| 属性 | 类型 | 默认值 | 说明 |
| --- | --- | --- | --- |
| `customLabels` | `Record<string, string>` | `() => (\{\})` | 按语言 code 提供的显示名称,会覆盖 locale 配置中的 `displayName`。 |
| `customWrapperStyle` | `CSSProperties` | `() => (\{\})` | 外层包裹的内联样式。 |
| `customButtonStyle` | `CSSProperties` | `() => (\{\})` | 用于打开下拉菜单的按钮的内联样式。 |
| `customDropdownStyle` | `CSSProperties` | `() => (\{\})` | 下拉面板的内联样式。 |
| `customItemStyle` | `CSSProperties` | `() => (\{\})` | 下拉菜单中每一项的内联样式。 |
| `customLinkStyle` | `CSSProperties` | `() => (\{\})` | 项内链接的内联样式。 |
| `customActiveLinkStyle` | `CSSProperties` | `() => (\{\})` | 当前活动语言链接的内联样式。 |
| `customDisabledLinkStyle` | `CSSProperties` | `() => (\{\})` | 当*当前*语言在配置中为 `disabled: true` 时,按钮的内联样式。 |
| `customIconStyle` | `CSSProperties` | `() => (\{\})` | 按钮上箭头图标的内联样式。 |

### 插槽

| 插槽 | 绑定 | 说明 |
| --- | --- | --- |
| `before-button` | — | 位于用于打开下拉菜单的按钮之前的内容。 |
| `before-selected-locale` | — | 按钮内当前语言标签之前的内容。 |
| `after-selected-locale` | — | 按钮内当前语言标签之后的内容。 |
| `before-dropdown` | — | 按钮与下拉面板之间的内容。 |
| `before-dropdown-items` | — | 位于下拉面板顶部、语言列表上方的内容。 |
| `before-item` | `locale` | 每个语言条目之前的内容。 |
| `before-link-content` | `locale` | 语言链接内标签之前的内容。 |
| `after-link-content` | `locale` | 语言链接内标签之后的内容。 |
| `after-item` | `locale` | 每个语言条目之后的内容。 |
| `after-dropdown-items` | — | 位于下拉面板底部、语言列表下方的内容。 |
| `after-dropdown` | — | 下拉面板之后的内容。 |

{/* /generated:component:i18n-switcher */}

默认情况下所有部分都不带样式:`custom*Style` 属性接受内联样式对象;若仅调整样式还不够,可使用下面的插槽完全替换其标记。

## 🛠️ 示例用法

### 基本用法

```vue
<template>
  <i18n-switcher />
</template>
```

以上代码会渲染一个使用默认样式和行为的语言切换器。

### 自定义标签与内联样式

```vue
<template>
  <i18n-switcher
    :customLabels="{ en: 'English', fr: 'Français' }"
    :customWrapperStyle="{ backgroundColor: '#f8f9fa', padding: '10px' }"
    :customButtonStyle="{ backgroundColor: '#007bff', color: '#fff', borderRadius: '4px' }"
    :customDropdownStyle="{ border: '1px solid #007bff', boxShadow: '0 2px 8px rgba(0, 0, 0, 0.1)' }"
    :customItemStyle="{ margin: '5px 0', padding: '5px' }"
    :customLinkStyle="{ padding: '8px 16px', color: '#333', textDecoration: 'none' }"
    :customActiveLinkStyle="{ color: 'green', fontWeight: 'bold', backgroundColor: '#f0f0f0' }"
    :customIconStyle="{ fontSize: '20px', color: '#007bff' }"
  />
</template>
```

该示例展示了一个使用自定义标签和内联样式的完整定制的语言切换器。

## 🔌 插槽

切换器的每一个视觉部分都可以被替换。这些插槽已在上面的属性表中列出,每个插槽都可以接收模板内容:

```vue
<i18n-switcher>
  <template #before-button>
    <span>🌐</span>
  </template>

  <template #before-item="{ locale }">
    <img :src="`/flags/${locale.code}.svg`" :alt="locale.code">
  </template>

  <template #after-dropdown-items>
    <a href="/help/languages">Missing a language?</a>
  </template>
</i18n-switcher>
```

名称以 `-item`、`-link-content` 结尾或以 `before-item` 开头的插槽会收到它们所对应的 `locale` 对象。

## 🎨 样式概览

`<i18n-switcher>` 组件使用简单的内联样式,可通过 props 传入的自定义样式轻松覆盖。以下是默认样式概览:

- **Wrapper**:控制下拉菜单的定位。
- **Button**:切换按钮的样式。
- **Dropdown**:下拉列表的样式。
- **Items**:列表中每一项的样式。
- **Links**:项内链接的样式。
- **Icon**:下拉指示图标的样式。

你可以通过对应的 props 传入自定义样式来覆盖它们,使语言切换器与你的应用 UI 无缝融合。
