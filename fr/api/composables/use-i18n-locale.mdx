---
title: "useI18nLocale"
description: "État centralisé de la locale, cookies et synchronisation."
---

# useI18nLocale

## Signature

{/* generated:symbol:useI18nLocale — do not edit; run `pnpm run docs:generate` */}

```ts
useI18nLocale(): { getEffectiveLocale: (route: unknown, getLocaleFromRoute: GetLocaleFromRoute) => string; getLocale: () => string | null; getLocaleWithServerFallback: (serverLocale?: string | null) => string | null; getPreferredLocale: () => string | null; hashCookie: any; isValidLocale: (locale: string | null | undefined) => locale is string; locale: any; localeCookie: any; resolveInitialLocale: (options: ResolveInitialLocaleOptions) => string; setLocale: (locale: string | null) => void; syncLocale: (locale: string | null) => void; validLocales: string[] }
```

Point d'entrée centralisé pour la gestion des locales i18n.
Combine `useState('i18n-locale')`, le cookie de locale et les utilitaires de synchronisation.

{/* /generated:symbol:useI18nLocale */}

## Utilisation

```vue
<script setup>
const {
  locale,
  localeCookie,
  hashCookie,
  validLocales,
  getPreferredLocale,
  getLocale,
  getLocaleWithServerFallback,
  getEffectiveLocale,
  resolveInitialLocale,
  setLocale,
  syncLocale,
  isValidLocale,
} = useI18nLocale()

// Set locale (updates state and cookies)
setLocale('de')

// Get current locale (state → cookie)
const current = getLocale()

// Get valid preferred locale
const preferred = getPreferredLocale()
</script>
```

## API

| Propriété/Méthode                              | Type                                | Description                                                                          |
| ----------------------------------------------- | ----------------------------------- | ------------------------------------------------------------------------------------ |
| `locale`                                        | `Ref<string \| null>`               | État réactif de la locale (`useState('i18n-locale')`)                                |
| `localeCookie`                                  | `Ref`                               | Cookie de locale (quand l'option `localeCookie` est activée)                         |
| `hashCookie`                                    | `Ref`                               | Cookie utilisé lorsque `hashMode` est actif                                          |
| `validLocales`                                  | `string[]`                          | Codes de locale activés depuis la configuration du module (exclut `disabled`)        |
| `getLocale()`                                   | `() => string \| null`              | Locale actuelle : état → cookie (ou cookie de hash en mode hash)                     |
| `getPreferredLocale()`                          | `() => string \| null`              | Locale depuis l'état/cookie **validée** contre `validLocales`, ou `null`             |
| `getLocaleWithServerFallback(serverLocale?)`    | `(serverLocale?) => string \| null` | État → cookie → contexte serveur optionnel (utilisé pendant l'init `no_prefix`)      |
| `getEffectiveLocale(route, getLocaleFromRoute)` | `(route, fn) => string`             | Locale pour charger les traductions : le mode hash préfère l'état ; sinon, lit depuis la route |
| `resolveInitialLocale(options)`                 | `(options) => string`               | Résout la locale : état → `serverLocale` → route ; synchronise l'état au besoin      |
| `setLocale(locale)`                             | `(locale: string \| null) => void`  | Définit la locale et la synchronise avec les cookies                                 |
| `syncLocale(locale)`                            | `(locale: string \| null) => void`  | Synchronise uniquement les cookies (pas de mise à jour de l'état)                    |
| `isValidLocale(locale)`                         | `(locale) => boolean`               | Vérifie si la chaîne se trouve dans `validLocales`                                   |

### Options de `resolveInitialLocale`

```typescript
interface ResolveInitialLocaleOptions {
  route: unknown
  serverLocale?: string | null
  getLocaleFromRoute: (route?: unknown) => string
}
```

Utilisé en interne par le plugin principal au démarrage. Les plugins personnalisés peuvent l'appeler lors de la mise en œuvre de flux de détection alternatifs.

### `getEffectiveLocale`

```typescript
const { getEffectiveLocale } = useI18nLocale()
const { $getLocale } = useNuxtApp()

const locale = getEffectiveLocale(route, (r) => $getLocale(r))
```

Renvoie la locale utilisée pour charger les traductions et formater les messages pour un objet route donné.

## Définition programmatique de la locale

Préférez `useI18nLocale()` à l'utilisation directe de `useState('i18n-locale')` :

```ts
// plugins/i18n-loader.server.ts
import { defineNuxtPlugin } from '#imports'

export default defineNuxtPlugin({
  name: 'i18n-custom-loader',
  enforce: 'pre',
  order: -10,

  setup() {
    const { setLocale } = useI18nLocale()
    const detectedLocale = 'ja' // Your detection logic here
    setLocale(detectedLocale)
  },
})
```

<Tip>
`setLocale()` ne met à jour que la préférence de locale (état + cookie). Pour recharger également le contexte de traduction actif côté client, utilisez `$switchLocale()` depuis `useNuxtApp()` ou `useI18n()`. Cela est particulièrement important pour `no_prefix`, où l'URL ne change pas. Voir [FAQ — changer de locale sans changer l'URL](/guides/faq#-switch-locale-without-changing-the-url).
</Tip>

## Voir aussi

- [Détection de langue personnalisée](/guides/custom-language-detection) — configurer l'auto-détection
- [Stratégie](/guides/routing-strategies) — stratégies de routage et priorité des locales
- [Méthodes — section useI18nLocale](/api/methods#-usei18nlocale-composable) — recoupement avec l'API du plugin d'exécution
